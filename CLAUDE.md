# CLAUDE.md

이 파일은 Claude Code(claude.ai/code)가 이 저장소에서 작업할 때 참고하는 안내 문서입니다.

## 프로젝트 개요

**PasteFlow** — 순차 붙여넣기 자동화 클립보드 매니저. Windows 10/11 전용. 복사한 순서대로 Ctrl+Shift+V를 누를 때마다 다음 항목이 붙여넣어지는 **항상 활성** 방식. PyQt6 기반. 전체 요구사항은 `PRD.md` 참고.

## 명령어

```bash
# 앱 실행
python -m pasteflow.main

# 의존성 설치
pip install -r requirements.txt

# 테스트 실행
pytest tests/

# 단독 실행 .exe 빌드 (진입점·onefile·windowed·아이콘·버전 메타데이터는 spec에 인코딩)
pyinstaller PasteFlow.spec    # 산출물: dist/PasteFlow-{버전}.exe
```

> 모델이 실제로 되는지는 **설정창 `연결 테스트` 버튼**이 그 자리에서 실호출해 확인한다
> (v1.42.0). 빌드타임 전수 스윕(`tools/sweep_models.py` + `model_matrix.json`)은 폐기됐다.

---

## 프로젝트 구조

```
pasteflow/
├── main.py                 # 진입점, 앱 초기화 및 모듈 오케스트레이션
├── clipboard_monitor.py    # 클립보드 감시 (WM_CLIPBOARDUPDATE)
├── paste_queue.py          # 순차 붙여넣기 큐 & 포인터 관리 (핵심)
├── paste_interceptor.py    # Ctrl+Shift+V 감지 + 패널 토글 단축키 감지 (핵심)
├── hotkey_manager.py       # RegisterHotKey 유틸 (현재 미사용, 구조 유지)
├── database.py             # SQLite CRUD (clipboard_items, settings)
├── models.py               # ClipboardItem 데이터 모델
├── crypto.py               # DPAPI 시크릿 보호 (API 키 암호화 저장)
├── uia.py                  # 커서 아래 요소 hit-test — 창-스코프 rect_in_window_at(최말단 자식 HWND를 루트로 MSAA accHitTest 하강, 창 밖 오배치 과대사각형은 담긴 창으로 보정=HWP 편집영역/툴바 대응, 전용 WinDLL로 argtypes 격리 — 마그네틱 캡처가 사용) + 점-기반 rect_at(MSAA AccessibleObjectFromPoint+UIA 폴백 — 현재 캡처 미사용), comtypes
├── ocr_engine.py           # OCR 추상화 — winocr(WinRT) 동기 래퍼 + 게이트웨이 AI API(OCR 전용, v1.6x부터 — AI 질의 경로 제거)
├── gdrive.py               # 구글 드라이브 OAuth (루프백+PKCE·TokenCache·커넥터 도구 스펙) — v1.6x에서 PasteFlow 앱 자체는 더 이상 쓰지 않는다(우클릭 "AI에게 질문" 등 AI 질의 기능 통째 제거). `webchat/`(별개 도구)이 계속 import하므로 모듈은 남겨둔다 — 지우면 webchat이 깨진다.
├── web_open.py             # 질문을 브라우저에서 직접 열기 — 구글 검색 AI 모드(udm=50) URL + 이미지 첨부 시 클립보드 주입, 키 주입 전 브라우저 포그라운드 검사
├── ai_palette.py           # AI 팔레트(Alt+` 자유질문창) 타겟 목록 — 기본은 Google AI 모드 하나뿐(v1.6x, 실사용 결과 가장 견고했음), URL 빌더·keyword 매칭, 설정창에서 사용자가 추가/편집
├── gif_recorder.py         # GIF 녹화 — 캡처 오버레이 select_only로 영역만 받아 라이브 연속 grab → Pillow 애니메이션 GIF (GifRecorder + 정지 컨트롤러 + encode_gif). GDI 커서 합성(composite_cursor)도 여기 있고 video_recorder.py가 재사용한다. 저수준 조각(sample_cursor·cursor_hotspot·blit_icon_at)은 capture_overlay.py의 정지 캡처 '커서 포함'(Space 토글)도 공유한다.
├── video_recorder.py       # 영상(MP4) 녹화 — GIF와 같은 select_only 흐름을 공유하되 프레임을 cv2.VideoWriter로 즉시 파일에 흘려써 메모리 상한이 없다(VideoRecorder, gif_recorder의 _RecordController·composite_cursor 재사용)
├── stt_engine.py           # 음성 입력(STT) — Recorder(sounddevice 녹음+RMS 음량)·transcribe(게이트웨이 Gemini 계열 화이트리스트, ocr_engine 패턴 재사용)·마이크 장치 목록/기본값 조회
└── ui/
    ├── panel.py            # 전체 클립보드 패널
    ├── image_preview.py    # 이미지 미리보기 팝업 (다중 창 지원, Space로 인라인 주석 편집 진입)
    ├── image_annotator.py  # 이미지 주석 편집기 (QGraphicsScene — 도형·선·화살표·펜·텍스트·번호)
    ├── text_preview.py     # 텍스트 미리보기 팝업
    ├── toast.py            # 우하단 스택형 토스트 (복사 알림·시작·OCR)
    ├── paste_hud.py        # 순차 붙여넣기 진행 HUD (큐 목록·포인터 실시간)
    ├── settings_dialog.py  # 설정 화면
    ├── ocr_overlay.py      # OCR 영역 선택 오버레이
    ├── capture_overlay.py  # 마그네틱 영역 캡처 오버레이 (Snipaste식 입력-소유 오버레이 — 얼린 최상위창+창-스코프 요소 스냅·자유드래그·크로스모니터 합성. select_only 모드=자르지 않고 사각형만 emit → GIF 녹화용. Space로 '실제 커서 포함' 토글 + 그 Space·ESC를 삼키는 세션 전용 저수준 키보드 훅)
    ├── ai_query.py         # AI 팔레트 질문 입력 다이얼로그 (Alt+` 자유질문 — v1.6x부터 우클릭 "AI에게 질문"·비교·기록 없이 이 경로 하나뿐)
    ├── stt_indicator.py    # 음성 입력 녹음 중 표시되는 음량 반응 이퀄라이저 pill (커서 옆, 클릭하면 녹음 종료)
    └── tray.py             # 시스템 트레이

tests/
├── test_models.py
├── test_database.py
├── test_paste_queue.py
├── test_ocr_engine.py
├── test_web_open.py
├── test_ai_palette.py
├── test_text_preview.py
└── test_crypto.py

tools/
└── make_icon.py
```

---

## 아키텍처

### 모듈 역할

- **`main.py`** — 오케스트레이션 레이어. 모든 모듈을 연결하고 클립보드 모니터 → DB → 큐 → UI 간 이벤트 흐름 관리. 단일 인스턴스 보장(Windows 뮤텍스), 시작 알림 토스트 표시. 순차 붙여넣기 시 진행 HUD(`PasteHud`)를 표시·실시간 갱신하고 큐 소진/중단 시 fade-out한다(`_update_paste_ui`/`_on_paste_queue_done`). 실제 복사(클립보드 모니터 경로) 시 우하단 스택 토스트 표시(`_on_copy_toast` — 설정 `notify_on_copy`로 on/off, OCR 결과는 자체 토스트가 있어 `_persist_clipboard_item`으로 우회). **큐 클리어 공통 경로(`_clear_queue_ui`)**: 큐를 비우고 트레이·패널 하이라이트를 초기화하는 헬퍼 — '큐를 언제 비울지' 정책의 **포기/클리어** 범주(일반 Ctrl+V·큐 소진 완료·HUD ✕ 취소·우클릭 큐 해제·Ctrl+Shift+P 단발)가 공유한다. **히스토리 삭제 시 큐 동기화(`_on_delete_item`, 2026-08-01)**: 큐는 캡처 시점 스냅샷을 DB와 별개로 들고 있어, 히스토리에서 항목을 지워도 큐엔 자동 반영이 안 된다(캡처 3개 중 1개를 히스토리에서 지웠는데 순차 붙여넣기 진행 HUD는 여전히 3개로 표시되던 버그). `_on_delete_item`이 `db.delete_item()` 뒤 `queue.remove_item(item_id)`를 호출해 큐에서도 함께 제거하고, 성공했으면 트레이·HUD를 그 자리에서 갱신(큐가 완전히 비면 `paste_hud.dismiss()`로 즉시 닫음) — 전체 클리어(`_clear_queue_ui`)와 달리 항목 하나만 제거하는 것이라 별도 경로. **일반 Ctrl+V 큐 비우기**: 인터셉터의 `on_plain_paste` 콜백 → `_bridge.plain_paste` 시그널 → 메인 스레드 슬롯 `_on_plain_paste()`가 `_clear_queue_ui()` 호출(`queue.clear()`는 `mark_plain_paste()`와 동일하게 `_reset_unlocked`). **큐 소진 완료**: `_on_paste_queue_done()`이 `_clear_queue_ui()` + `paste_hud.finish()` — 소진 후에도 큐가 남아 패널 우클릭이 '큐 해제'로 뜨는 찌꺼기 방지(Ctrl+Shift+V 신호 경로와 Ctrl+Shift+[ 직접 호출이 공유). **큐 idle 시간 주입**: 시작 시 DB `queue_idle_reset_sec`(기본 10초)를 읽어 `queue.set_idle_reset_sec()`로 주입하고, 설정 변경 시 즉시 반영. 시작 시 HKCU `Run` 키 실제 등록 상태를 DB `auto_start`에 동기화(`_sync_auto_start_from_registry`). **자동 시작 등록 방식**: HKCU `Software\Microsoft\Windows\CurrentVersion\Run`에 `PasteFlow` 값을 등록(`_set_auto_start`). 등록되는 명령은 **항상 `wscript.exe "%LOCALAPPDATA%\PasteFlow\autostart_launcher.vbs"`** 한 형태로, 이 launcher VBS는 `_write_autostart_launcher_vbs()`가 매번 새로 생성하며 `WScript.Sleep _AUTOSTART_DRIVE_WAIT_SEC*1000`(기본 2초, 2026-08-05 15초→2초 축소) 대기 후 실제 PasteFlow를 hidden 모드로 실행한다. 옛 15초는 코드가 구글 드라이브 마운트 경로에 있어 그 마운트를 기다리는 목적이었으나, 지금은 코드 경로가 로컬 디스크(`Dev\Paste_flow`)라 그 이유가 더 이상 적용되지 않는다(실측 확인) — 2초는 로그온 초반 COM/WScript.Shell 초기화 여유만 남긴 값. 실행 대상은 빌드 모드별로 분기: exe 빌드는 `sys.executable` 절대경로, 스크립트 모드는 `pythonw.exe` + `run.pyw` 절대경로 — `run.pyw`가 `os.chdir`로 working directory를 자기 위치로 설정하고 예외를 `%LOCALAPPDATA%\PasteFlow\logs\error.log`에 기록한다. **주의**: `python.exe`나 `-m pasteflow.main` 형태로 등록하면 (1) 콘솔 창이 뜨고 (2) Run 키의 working dir이 시스템 기본(`C:\Windows\System32` 등)이라 `ModuleNotFoundError`가 발생하므로 사용 금지. **로컬 데이터 경로**: DB(`pasteflow.db`)·로그·launcher VBS 모두 `%LOCALAPPDATA%\PasteFlow\` 아래 저장. **모든 설정은 로컬 DB에만 저장** — 다중 PC `settings.json` 공유는 v1.4.0에서 폐기(DPAPI가 PC/계정 바인딩이라 시크릿 동기화와 양립 불가). `_resolve_db_path()`가 로컬 DB 부재 + 레거시 Drive DB 존재 시 1회 복사 마이그레이션 수행. **시크릿 처리**: `_SECRET_KEYS = {ocr_gemini_api_key_gateway, gdrive_client_secret, gdrive_refresh_token, ai_profiles}`(`ai_profiles`는 JSON 안에 각 프로필 api_key를 품어 통째 암호화 — v1.56.0 API 프로필)는 DPAPI(`pasteflow/crypto.py`)로 암호화해 DB에 저장 — 저장 포맷 `enc:v1:<base64(CryptProtectData blob)>`. 읽기는 `_get_secret(key)`(= `unprotect ∘ db.get_setting`)로 복호화 후 사용, 쓰기는 `_on_settings_changed`에서 `_SECRET_KEYS`인 키만 `crypto.protect()`로 암호화한 뒤 저장. `crypto.protect/unprotect`는 빈값·이미 암호화된 값(`is_protected`)을 passthrough라 idempotent, `unprotect` 실패 시 `""`+경고(크래시 방지, 사용자는 설정창에서 재입력). **`_migrate_secrets(db)`**: 시작 시 1회 호출돼 ① `_SECRET_KEYS` 평문 → 암호화(`is_protected` 가드로 idempotent), ② 코드 어디서도 참조되지 않는 고아 설정 키(`_ORPHAN_KEYS`) DELETE. ⚠ `_ORPHAN_KEYS`에는 옛 잔재 4종(`ocr_api_key`·`ocr_base_url`·`hotkey_settings`·`panel_always_on_top`)에 더해, **v1.50.0에서 삭제된 두 마이그레이션(`_migrate_split_gemini_keys`·`_migrate_compare_backend`)이 이전 후 지우던 키 7종**(`ocr_gemini_api_key`·`ocr_gemini_model`·`ocr_gemini_model_cache` + `ai_compare_model_{a,b}_{official,gateway}`)이 들어간다 — 그 마이그레이션이 사라진 지금 아무도 안 지우므로 여기서 purge하지 않으면 **암호화 이전 시절의 평문 API 키(`ocr_gemini_api_key`)가 DB에 영구히 남는다**. **official 백엔드 제거 마이그레이션**(`_migrate_drop_official_backend`): 시작 시 1회 호출돼 v1.50.0에서 사라진 official 잔재(`ocr_gemini_api_key_official`·`ocr_gemini_model_official`·`ocr_gemini_model_cache_official`·`ocr_model_official`·`ocr_gemini_backend`·`ocr_backend`·`ai_compare_backend_{a,b}` = `_OFFICIAL_KEYS`)를 DELETE한다. ⚠ **비교 모델(2·3)이 official을 가리키고 있었다면 그 모델명도 함께 비운다** — 그냥 두면 official 전용 모델명이 게이트웨이로 날아가 404가 나는데 사용자는 "왜 안 되지"만 보게 된다(조용한 실패 대신 눈에 보이는 빈칸). backend 키를 지우고 나면 다음 실행엔 판정 재료가 없어 아무것도 안 지우므로 idempotent. Named Pipe IPC(`\\.\pipe\PasteFlow_IPC`) 서버 운영 — 두 번째 인스턴스 실행 시 패널 토글 신호 수신 후 해당 인스턴스 즉시 종료. **드래그 붙여넣기 헬퍼 함수** (panel.py의 `drag_to_app_requested` 시그널 처리): `_find_deepest_child()` — 커서 위치 최하위 자식 HWND 재귀 탐색; `_get_explorer_subfolder_at_cursor()` — SysListView32에서 커서 위치 서브폴더 경로 반환(크로스 프로세스 LVM_HITTEST); `_get_explorer_folder()` — CabinetWClass HWND → 드롭 대상 폴더 경로; `_get_desktop_path()` — 사용자 바탕화면 경로; **바탕화면 드롭 위치 지정**(2026-08-25): 가짜 드래그(파일시스템 직접 쓰기)라 Explorer가 실제 드롭 좌표를 몰라 저장된 파일이 항상 자기 기본 배치 규칙(빈 격자 순서 등)으로 떨어지던 문제 — `_find_desktop_listview()`(Progman 또는 WorkerW 아래 `SHELLDLL_DefView\SysListView32` 탐색, 배경화면 변경으로 리스트뷰가 별도 WorkerW로 옮겨간 경우까지 대응)와 `_position_desktop_icon(lv_hwnd, filename, screen_pt)`(`_get_explorer_subfolder_at_cursor`와 동일한 크로스 프로세스 `VirtualAllocEx`+`WriteProcessMemory`/`ReadProcessMemory` 기법으로 `LVM_SETITEMPOSITION32` 호출 — Explorer 자신이 실제 드래그 때 쓰는 것과 같은 메시지)로 저장 직후 아이콘을 커서 좌표로 재배치한다. ⚠ **Windows "파일 확장자 숨기기" 기본 설정 때문에 리스트뷰 아이템 텍스트에 확장자가 없을 수 있어**(실측 확인) 파일명 일치는 `os.path.splitext` 로 확장자를 뗀 이름끼리 비교한다 — 그냥 전체 파일명으로 비교하면 항상 매칭 실패. ⚠ 크로스 프로세스 텍스트 버퍼를 반복문에서 재사용할 때 이전(더 긴) 문자열의 잔여 바이트가 널 뒤에 남을 수 있어(실측 확인) `rstrip('\x00')`이 아니라 **첫 널에서 잘라야** 한다. `main.App._position_dropped_desktop_icon(filename, screen_pt)`이 `_on_drag_to_app`에서 바탕화면 저장 성공 직후 호출 — Explorer가 새 파일을 리스트뷰에 반영할 때까지 지연(실측 약 1~1.2초)이 있어 150ms 간격 최대 10회 재시도 후 조용히 포기(실패해도 무해, 시각적 편의일 뿐). **전제**: 바탕화면 "아이콘 자동 정렬"이 꺼져 있어야 위치가 유지된다(켜져 있으면 Explorer가 다시 정렬 — Windows 기본값은 대개 꺼져 있음). `_image_data_to_png_bytes()` — image_data(PNG/JPEG/GIF/WebP/CF_DIB)를 PNG bytes로 인메모리 변환. PIL이 직접 못 여는 raw CF_DIB는 BMP 파일 헤더를 조립해 인식시킨다(`_create_thumbnail`과 동일 기법). `_save_image_to_folder`·이미지 AI 질의가 공유 — 영역 캡처(Alt+F2)처럼 raw DIB로 저장된 이미지도 정상 처리; `_save_image_to_folder()` — `_image_data_to_png_bytes`로 변환한 PNG를 폴더에 파일로 저장; `_save_image_to_drop_temp()` — `%TEMP%\PasteFlow\`에 임시 PNG 저장 후 절대경로 반환(Alt+드래그·`Ctrl+Shift+P`/`Ctrl+Shift+[` 이미지→경로 단축키 공용 — 우클릭 "파일로 저장 후 경로 복사"는 2026-08-02부터 캡처 저장 폴더를 쓰도록 분리됐다, 아래 `_copy_image_as_path_for_item` 참고); `_read_image_from_clipboard()` — 현재 클립보드에서 이미지 bytes 반환(PNG 우선, 없으면 CF_DIB raw — 둘 다 `_save_image_to_drop_temp`가 그대로 처리). **CF_HDROP 폴백**(v1.53.0): 둘 다 없고 클립보드가 **이미지 파일 1개**(확장자 `.png/.jpg/.jpeg/.bmp/.gif/.webp/.tiff/.tif`)면 그 **파일 바이트를 읽어** 반환한다 — 탐색기 파일 복사는 PNG/CF_DIB를 클립보드에 안 올리고 CF_HDROP만 올리므로, 이게 없으면 탐색기 이미지를 핀(Alt+F3)할 때 이미지 대신 **경로 텍스트가 렌더**됐다. `clipboard_monitor`의 히스토리 캡처와 같은 규칙이라 "패널에 이미지로 남는 것 = 핀에서 이미지로 뜨는 것"이 일치한다; `_activate_and_send_ctrl_v(hwnd, sender=None)` — AttachThreadInput으로 포그라운드 잠금 우회 후 키 주입. `sender`(인자 없는 콜러블, 기본 `_send_ctrl_v_plain`)로 실제 주입 함수 교체 가능 — Alt+드래그처럼 호출 시점에 수정키가 눌린 경로는 `interceptor._release_modifiers_and_send_ctrl_v`를 넘긴다. AttachThreadInput은 **타겟 창 스레드**(`GetWindowThreadProcessId(hwnd)`)에 건다 — 드래그 시점 포그라운드는 PasteFlow 패널 자신(같은 스레드)이라 거기 거는 건 무효였고, 타겟 스레드에 붙어야 `SetForegroundWindow`+`SetFocus`가 포커스까지 첫 시도부터 넘긴다. 주입 직전 `GetAsyncKeyState(VK_MENU)`로 **Alt가 물리적으로 떨어질 때까지 QTimer 폴링**(25ms 간격·최대 1.5초) 후 Ctrl+V를 보낸다 — 드롭 순간 눌린 Alt는 가상 KEYUP으로 안 떨어지므로(`GetAsyncKeyState`는 물리 키 기준) 그냥 보내면 타겟이 `Ctrl+Alt+V`로 오인. **알려진 한계(cold 창)**: 타겟이 직전 비활성이던 Chromium 창은 프로그램적 활성화로 렌더러 텍스트 입력칸 포커스가 복원되지 않아 합성 Ctrl+V가 불안정(IME까지 겹치면 `ㅍ`로 입력되기도). 합성 클릭·간격 둔 Ctrl+V 등으로 시도했으나 불안정해 포기 — 사용자가 타겟 창을 먼저 클릭해 활성(warm) 상태로 만든 뒤 드래그하면 안정적이다. `_start_foreground_tracker()`는 `SetWinEventHook(EVENT_SYSTEM_FOREGROUND)`으로 포그라운드 창을 연속 추적해 드래그 대상 창을 확인한다. **이미지→경로 단축키 슬롯**: `_on_image_to_path_hotkey()` — **소스는 라이브 클립보드가 아니라 최신 히스토리 항목**(`db.get_recent_items(limit=1)[0]` — Ctrl+V의 '마지막 복사물'에 대응). 경로 텍스트는 히스토리에 안 남으므로(`_set_clipboard`의 self_triggered) 원본 이미지가 최신 자리에 유지돼 **이 키를 여러 번 눌러 같은 이미지를 무한히 경로로 붙일 수 있다**(Ctrl+V의 무한 반복과 대칭). 최신 항목이 이미지면 `_save_image_to_drop_temp()`로 PNG 저장(같은 항목 반복 시 `_img_to_path_cache=(item_id, path)`로 임시 PNG 재사용 → 디스크 재저장 회피, 새 이미지 복사 시 item.id가 달라 자동 캐시 미스) → 경로 텍스트 `ClipboardItem` 생성 → `interceptor._set_clipboard()` (규칙 5: `_self_triggered` 자동 처리) → 50ms 후 `interceptor._send_clean_key(VK_V)`로 현재 포그라운드 창에 Ctrl+V 주입 → `_clear_queue_ui()`로 큐 클리어(단발은 큐가 아닌 '이탈'이라 일반 Ctrl+V와 동일 취급 — 큐 기반은 Ctrl+Shift+[). 성공 시 저장한 PNG의 **썸네일을 토스트에 함께 표시**(`ToastNotification(..., icon="", image_path=saved_path)`) — Claude Code CLI 등은 붙여넣은 경로를 `[Image #N]` 라벨로만 보여줘 실제 이미지가 안 보이므로, "의도한 이미지가 맞는지" 그 자리에서 시각 확인하기 위함. 썸네일이 카테고리를 대신하므로 `🔤` 아이콘은 생략(`icon=""`). **`_activate_and_send_ctrl_v`를 쓰지 않는 이유**: 발화 시점에 사용자가 Ctrl+Shift를 누른 상태인데 `_send_ctrl_v_plain`은 수정키 해제 없이 단순 Ctrl down/V/Ctrl up만 보내 OS가 `Ctrl+Shift+V`로 인식한다. `_send_clean_key`는 Ctrl+Shift+V 순차 붙여넣기와 동일하게 수정키 해제·복원·입력기 전환 마스킹을 처리한다. 최신 항목이 이미지가 아니면 토스트만 표시. **클립보드 복원(2026-08-01)**: 주입 250ms 후 클립보드를 원본 이미지로 되돌린다(`interceptor._set_clipboard(item)`) — 복원이 없으면 실제 클립보드엔 경로 텍스트만 남아, 이 단축키 다음에 일반 Ctrl+V를 누르면 이미지가 아니라 경로가 또 붙는 문제가 있었다(사용자 리포트: "이미지가 아니라 경로가 한번더 붙여지네"). 복원도 self_triggered라 히스토리 중복 없음 — 두 단축키(경로/이미지)가 독립 기능으로 쓰인다. 복원 전 250ms 창 안에 수동 Ctrl+V를 누르면 그 순간엔 여전히 경로가 붙는 한계는 남는다. **순차 경로 붙여넣기 슬롯**: `_on_seq_image_to_path_hotkey()`(기본 Ctrl+Shift+[) — 위 단발 슬롯의 **큐 버전**. `queue.get_next()`로 다음 항목을 꺼내(Ctrl+Shift+V와 큐·포인터 공유) **이미지면** `_save_image_to_drop_temp(image_data)`로 PNG 저장→경로 텍스트 항목을 `_set_clipboard`+50ms 후 `_send_clean_key(VK_V)`로 주입(썸네일 토스트) + 위와 동일하게 250ms 후 클립보드를 원본 이미지로 복원, **이미지가 아니면** 원본 그대로 `_set_clipboard`+주입(Ctrl+Shift+V와 동일). 캡처(Alt+F2)가 이미 큐에 이미지로 쌓이므로 캡처 여러 장을 순서대로 경로로 붙일 수 있다. summary 항목은 `db.get_item`으로 전체 로드(이미지 항목은 image_data 인라인이라 대개 불필요). 끝에 `_update_paste_ui()`로 진행 HUD 갱신, 큐 소진 시 `_on_paste_queue_done()`(큐 클리어 + HUD 페이드 — Ctrl+Shift+V 소진과 동일, 찌꺼기 방지). 큐가 비면 토스트만(단발 폴백은 Ctrl+Shift+P 담당 — '순차/일반'을 키로 구분). **OCR 워커**: `_start_ocr_worker(png_bytes)`는 영역 캡처 OCR(AI OCR 단축키 → `_on_ocr_region_captured`) 전용이다. ⚠ 2026-08-02: panel/preview 우클릭의 이미지 항목 OCR("텍스트 추출(OCR)" 메뉴, `_on_ocr_image_item`/`_on_ocr_image_by_id`)은 실사용 근거가 없어 제거했다 — 캡처는 여전히 이 워커 하나만 쓴다. **OCR 연결 워밍업**(2026-07-31): `__init__`에서 `self.db` 생성 직후 데몬 스레드로 `_prewarm_ocr_connection()`을 실행 — `_resolve_gemini_cfg()`로 크리덴셜을 읽어 `ocr_engine.warm_connection(api_key, base_url)`을 호출한다. 실측 원인: `openai` 패키지 지연 임포트(1.4~3.4초, 프로세스당 최초 1회)와 OCR 호출마다 `openai.OpenAI()`를 새로 만들어 TCP/TLS 핸드셰이크를 매번 치르던 구조가 겹쳐, 이 개선 전엔 **첫 OCR이 8~9초·이후 2~3초**였다(2026-07-31 사용자 실측). `warm_connection`이 앱 시작 시 임포트+클라이언트 생성+가벼운 `/models` 조회 1회로 DNS/TCP/TLS까지 미리 끝내고, `ocr_engine._get_client`의 `(api_key, base_url)`별 클라이언트 캐시(아래 `ocr_engine.py` 항목 참고)가 실제 OCR 호출에서 이 캐시된 커넥션을 재사용 — 적용 후 첫 OCR도 ~1초대로 줄었다(실사용자 실측). 크리덴셜이 비었거나 워밍업이 실패해도 조용히 무시(최선 노력형, 실제 OCR 경로엔 영향 없음). **OCR 엔진 고정**: OCR은 v1.28.0부터 **항상 AI(Mindlogic 게이트웨이) API**로 처리한다(WinRT 엔진 분기 제거 — `_start_ocr_worker`가 `kind="gemini"` 고정). `_start_ocr_worker`가 게이트웨이 크리덴셜(`ocr_gemini_api_key_gateway` — 암호화 저장 → `_get_secret`으로 복호화 / `ocr_gemini_base_url`)과 OCR 모델을 골라 `OcrEngine`에 주입. **설정 해석 공통화**: 이 key/url/model 해석 로직을 `_resolve_gemini_cfg(purpose="ai"|"ocr") → (api_key, base_url, model)` 헬퍼로 추출해 OCR 워커와 AI 질의 워커가 공유한다(DB 접근은 `_lock`으로 직렬화되어 워커 스레드 호출 안전). **모델만 용도별로 갈린다**(v1.39.0 — OCR/AI 모델 분리): `purpose="ocr"` → `ocr_model_gateway`(비전 가능 모델만 고를 수 있는 슬롯), `purpose="ai"` → 기존 `ocr_gemini_model_gateway`. api_key·base_url은 계속 공유(같은 계정·엔드포인트). 분리 이유: 한 모델을 공유하면 답변용 고가 모델(`claude-opus-4-8` 등)이 OCR에도 쓰여 텍스트 추출 한 번에 과금이 커지고, 반대로 텍스트 전용 모델(`solar-pro2` 등)을 AI용으로 고르면 OCR이 400으로 깨진다. OCR 슬롯이 비면 AI 모델로 폴백(마이그레이션 전 사용자 무중단). **`_migrate_split_ocr_ai_model(db)`**: 시작 시 1회 호출돼 기존 `ocr_gemini_model_gateway` 값을 `ocr_model_gateway`에 복사(동작 변화 0에서 시작). 새 키가 이미 있으면 건드리지 않아 idempotent — 사용자가 OCR 모델만 싼 것으로 바꾼 뒤 재시작해도 덮어쓰지 않는다. 옛 키는 AI 질의가 계속 쓰므로 삭제하지 않는다. 호출 순서 주의: `_migrate_drop_official_backend`보다 **먼저** 와야 한다(뒤엣것이 비교 슬롯의 backend 키를 지운다). **AI 자유질문(`` alt+` `` 단축키) = 타겟 팔레트**: `_on_ask_ai_hotkey()`가 `_open_ai_dialog()`를 호출한다 — DB `ai_palette_sites`를 매번 다시 읽어(`ai_palette.load_sites`) `AiQueryDialog(sites=...)`로 넘기므로 설정창에서 타겟을 바꾸면 앱 재시작 없이 바로 반영된다. 자세한 선택 방식(키워드 접두어·Tab 순환)은 `ui/ai_query.py`·`ai_palette.py` 항목 참조. v1.6x에서 우클릭 "AI에게 질문"·여러 모델 비교·기록·구글 드라이브 연동을 통째로 제거해(실사용 결과 Google AI 텍스트검색만 견고했음 — 사용자 판단, 2026-07-29), 이제 API 키를 쓰는 경로는 OCR 하나뿐이다. **진행/결과 표시(커서 모니터 정중앙 칩)**: `_start_cursor_progress(prefix, icon, anchor)`가 지속형 토스트(`ToastNotification(anchor=..., center=True)` — 클릭 통과, 앵커가 속한 모니터 정중앙)로 경과시간을 0.5초마다 갱신(`_tick_cursor_progress`), OCR 전용이다. 옛 점(`●··`) 애니메이션은 `●`(넓음)·`·`(좁음) 폭 차이로 매 틱 칩 폭이 바뀌며 재중심(`_place_anchored`)이 좌우로 흔들려 제거(v1.27.0 — 경과시간만으로 "작업 중" 충분). 종료는 둘로 갈린다: `_stop_cursor_progress`(칩 즉시 fade — 캡처 등 결과를 별도로 안 보여주는 경로), `_finish_cursor_progress(message)`(칩을 결과 메시지로 전환 후 1.5초 뒤 fade — OCR은 `✓ 인식 앞부분…` 앞 24자 미리보기로 "제대로 됐나" 확인). 모델 폴백(not_found → `_FALLBACK_CHAIN`) 발생 시 `_bridge.ocr_fallback`를 재사용해 알림(토스트 아이콘 `🔤`). **브라우저 경로(`_open_in_browser`/`_inject_to_google`)**: 타겟 팔레트가 고른 것이 `google_ai` kind면 API에 묻지 않고 크롬에서 연다(왜인지는 `web_open.py` 항목 — 실시간 수치는 API 검색이 구조적으로 못 잡는다). `_open_ai_dialog`의 `finished` 콜백이 `dialog.get_result()`로 받은 `(site, question)`의 `site['kind']`를 보고 갈린다: `google_ai`는 이 브라우저 경로로, 사용자가 추가한 `url` 타겟은 `_open_palette_url`(`ai_palette.build_url`+`web_open.open_url`)로, 그 외(옛 설정에 남은 미지원 kind)는 토스트로 안내한다. **텍스트만이면 URL 경로**(`web_open.google_ai_url` — 견고), **첨부 이미지가 있으면 주입 경로**(`_inject_to_google`, 이미지는 1장만 — 구글 AI 모드 확인됨): 이미지는 URL에 못 실으므로 AI 모드 빈 화면을 열고 `_BROWSER_LOAD_MS`(3500ms, 이미지가 있으면 `_BROWSER_LOAD_IMAGE_EXTRA_MS`=1500ms 추가) 뒤부터 **클립보드를 이미지 → 질문 텍스트 순으로 갈아끼우며 `_send_clean_key(VK_V)`, 마지막에 `send_plain_key(VK_RETURN)`**를 QTimer 체인으로 쏜다(`_INJECT_STEP_MS`=700ms 간격, `sleep`으로 UI를 막지 않는다). 클립보드 교체는 `_set_clipboard`라 `_self_triggered`가 서므로 임시 클립보드가 히스토리에 쌓이지 않는다(금지 조항 5와 같은 경로). ⚠ **매 단계 직전 `web_open.is_browser_foreground()`로 확인하고 아니면 즉시 중단한다** — 로드가 늦거나 사용자가 그 사이 다른 창을 클릭하면 Ctrl+V·Enter가 그 창에 들어가기 때문(주입 경로가 URL 경로보다 본질적으로 약한 지점). 로드 완료 시점을 프로그램이 알 수 없어(브라우저 밖에서 DOM을 못 본다) 고정 지연에 기댈 수밖에 없는 것이 이 방식의 한계다. **`is_browser_foreground()`의 한계**: 이 검사는 "크롬 창이 앞에 있는가"만 보고 "그 탭의 페이지가 로딩을 마쳐 입력칸에 포커스가 갔는가"는 못 본다 — 크롬은 새 탭이 뜨자마자 포그라운드가 되므로, 첫 붙여넣기(이미지, 텍스트보다 먼저 감) 시점에 구글 AI 모드가 아직 로딩 중이면 그 Ctrl+V는 허공으로 사라지고, 700ms 뒤 텍스트 붙여넣기는 그새 로딩이 끝나 성공하는 비대칭 실패가 실제로 보고됐다(2026-07-29 — "이미지만 빠지고 텍스트+Enter는 항상 성공"). 위 두 상수를 늘려 완화했으나 근본적으로는 여전히 고정 지연 도박이다(더 견고히 하려면 크롬 창 제목 폴링이나 UIA로 포커스 여부를 직접 확인하는 방법이 남아 있음, 미구현).
- **`gdrive.py`** — **구글 드라이브 OAuth 크리덴셜 조달(v1.51.0 도입).** ⚠ **v1.6x에서 PasteFlow 앱 자체는 이 모듈을 더 이상 쓰지 않는다** — 우클릭 "AI에게 질문"과 함께 드라이브 연동(AI 질의가 내 드라이브 문서를 근거로 답하는 기능)을 통째로 제거했다. `main.py`의 배선(`self._gdrive_tokens`·`_gdrive_creds`)·설정창 `구글 드라이브 (선택)` 그룹은 제거됐고, `_SECRET_KEYS`의 `gdrive_client_secret`/`gdrive_refresh_token`도 1회 마이그레이션으로 DB에서 purge된다(`main._migrate_drop_ai_query_feature`). **그런데도 파일은 지우지 않는다** — `webchat/`(별개 로컬 도구, PasteFlow 앱과 무관하게 실행)가 `from pasteflow import gdrive`로 이 모듈을 그대로 import해 자기 드라이브 검색 기능에 쓴다(`webchat/README.md` 참고). 지우면 webchat이 깨진다. 모듈 자체 로직은 그대로 유지: `authorize()`(임의 포트 루프백 서버 + 브라우저 동의 → refresh token, PKCE 사용 — 데스크톱 앱의 client_secret은 진짜 비밀이 아니므로 코드 가로채기 방어는 PKCE가 담당), `refresh_access_token()`, `TokenCache`(만료 2분 전 자동 갱신 + 락, 자격증명 지문이 바뀌면 캐시 폐기), `drive_tool(token)`(게이트웨이 Responses API용 도구 스펙 — `connector_id="connector_googledrive"`). **새 외부 의존성 0**(표준 라이브러리 `http.server`·`webbrowser`·`urllib`만). ⚠ 동의 화면이 "테스트" 상태면 구글이 refresh token을 7일 만에 만료시키므로 Cloud Console에서 "프로덕션"으로 게시해야 한다.
- **`web_open.py`** — **질문을 브라우저에서 직접 열어 답을 보게 한다.** API로 못 가져오는 것을 위한 경로이자, v1.6x부터 AI 팔레트(Alt+`)의 유일한 기본 타겟(Google AI 모드)이 타는 경로다. **왜(2026-07-14 실측)**: "오늘 SK하이닉스 주가"를 API 검색 경로에 세 번 물었더니 현재가를 **한 번도 못 맞혔다**(205만·208만·175만 — 실제 191.3만). 병목은 모델이 아니라 **수단**이다 — 실시간 시세는 페이지 본문 텍스트에 없고 JS가 그리는 값이라, 크롤링된 텍스트를 읽는 웹 검색 도구는 영영 볼 수 없다(긁히는 숫자는 뉴스 기사에 박제된 과거 값이나 낡은 캐시뿐 = 오답의 정체). 반면 구글 검색 **AI 모드를 브라우저에서 열면** 구글이 금융 피드를 직접 물고 있어 정답이 차트째로 나온다(같은 질문에 1,913,000원 정확). 그래서 이 모듈은 답을 '가져오지' 않고 **사용자가 직접 보게** 한다. ⚠ **답은 브라우저에 뜨고 끝난다** — 프로그램이 그 텍스트를 못 읽으므로 답변창 렌더는 이 경로에 적용되지 않는다(되가져오려면 크롬 확장이 필요 — 별개 작업). **URL 프리필 실측**: 구글 AI 모드 `?q=…&udm=50` → 질문이 **자동 전송되고 답이 렌더된다**(로그인 불필요, `google_ai_url`). **`google_ai_home_url()`** — 질문 없이 AI 모드를 여는 URL(`?udm=50`). **이미지 첨부 주입의 시작점**이다: 이미지는 URL에 못 실지만 구글 입력칸이 Ctrl+V로 이미지를 받고, 이 화면은 **로드 직후 입력칸(TEXTAREA)에 포커스가 자동으로 간다**(2026-07-14 실측) — 그래서 창을 띄운 뒤 바로 Ctrl+V를 쏘면 그 칸에 꽂힌다. **`is_browser_foreground()`** — 키 주입 전 안전 검사(포그라운드 창 클래스명이 `Chrome_WidgetWin*`·`MozillaWindowClass`인지). ⚠ **이 검사가 없으면 사고가 난다**: 페이지 로드가 늦거나 사용자가 그 사이 다른 창을 클릭하면 우리가 쏜 Ctrl+V·Enter가 **엉뚱한 앱**(메모장·터미널)에 그대로 들어간다. 주입은 URL 방식과 달리 "지금 앞에 누가 있는가"에 전적으로 의존하므로, 확인되지 않으면 아무것도 하지 않는다. **`open_url()`은 기본 브라우저를 쓴다**(실패해도 예외 없이 `False`) — 사용자가 이미 로그인해 둔 프로필이 그대로 물려야 재인증 없이 열리기 때문. 브라우저를 새로 띄우는 자동화 도구(Playwright 등)를 쓰면 그 세션이 없어 로그인 화면부터 만난다(그래서 기각 — **새 외부 의존성 0**, 표준 라이브러리 `webbrowser`만). URL 빌더는 순수 함수라 `tests/test_web_open.py`가 고정. ⚠ v1.6x에서 드라이브 검색(`drive_search_url`)·ChatGPT/Claude/Gemini 주입(`chatgpt_home_url` 등)은 팔레트 타겟과 함께 제거됐다(AI 질의 기능 통째 정리 — 실사용 결과 Google AI 텍스트검색만 견고했음).
- **`ai_palette.py`** — **AI 팔레트(Alt+` 자유질문창)의 타겟 목록·라우팅 데이터.** 자유질문의 질문 텍스트를 여러 목적지 중 하나로 보내는 "사이트 라우터" 팔레트. 각 타겟은 `{label, keyword, kind, url}` dict, `kind` ∈ `url`(사용자 웹사이트 — `{q}`에 질의를 넣어 여는 일반형)/`google_ai`(main.py의 기존 배관을 그대로 탄다, **항상 정확히 1개인 고정 타겟** — 2026-07-29 설정창 개편). **`ensure_google_ai(sites)`**: Google AI가 정확히 1개가 되도록 정규화(없으면 맨 앞에 `DEFAULT_SITES[0]` 삽입, 여럿이면 첫 번째만 남기고 나머지 제거·멱등) — 설정창이 종류 선택 드롭다운을 없애고 Google AI를 삭제 불가 고정 행으로 바꾸면서(`ui/settings_dialog.py`의 `_PaletteSiteRow(fixed=...)`), 옛 설정(드롭다운 시절 0개·여러 개로 저장됐을 수 있음)을 로드 시점(`_load_palette_sites`)에 보정하려고 추가했다. ⚠ **v1.6x에서 `drive`·`api`·ChatGPT/Claude/Gemini 주입 kind를 전부 제거했다** — 실사용 결과 Google AI 텍스트검색만 견고했고, 나머지는 크롬 즐겨찾기로 충분해 과도했다(사용자 판단, 2026-07-29). API 키를 쓰는 질의 기능(`api` kind·우클릭 "AI에게 질문"·비교·기록·드라이브 연동)도 이 정리와 함께 통째로 사라져, 이제 API 키는 OCR 전용이다. `load_sites(raw_json)` — DB `ai_palette_sites` 설정값을 파싱, 비었거나 깨졌으면 `DEFAULT_SITES`(Google AI 하나뿐)로 폴백. `build_url(template, query)` — `{q}` 자리에 `quote_plus`로 인코딩한 질의를 넣고, 자리표시자가 없으면 쿼리스트링(`?q=`/`&q=`)으로 붙인다. `match_keyword(sites, text)` — 등록된 `keyword`+공백으로 문장이 시작하면 그 타겟 인덱스와 접두어를 뗀 나머지 질의를 돌려준다(Chrome 커스텀 검색엔진 키워드 관례와 동일). 새 외부 의존성 0(stdlib `json`/`urllib.parse`만). 순수 함수라 `tests/test_ai_palette.py`가 고정. `ui/ai_query.py`(팔레트 UI)·`ui/settings_dialog.py`("빠른 검색" 편집 그룹 — 2026-07-29 이전엔 "AI 팔레트 타겟"이었으나, Google AI 외 일반 URL 타겟도 담는 목록이라 더 정확한 이름으로 개명)·`main.py`(`_open_ai_dialog`/`_open_palette_url`)가 공유한다.
- **`gif_recorder.py`** — **GIF 녹화 (MVP).** 캡처 오버레이(`capture_overlay`)가 '얼린' 스크린샷 한 장을 잘라내는 것과 달리, 녹화는 영역을 정한 뒤 그 자리를 시간에 걸쳐 연속 grab한다. 그래서 오버레이의 **`select_only` 모드**로 사각형만 받고(오버레이는 얼림 구조라 녹화 부적합) 여기서 라이브 캡처 루프를 돈다. **`GifRecorder`**(매니저): QTimer로 fps(기본 12)마다 `screen.grabWindow(0, x, y, w, h)`로 지정 영역을 QImage 프레임에 누적(좌표는 rect를 논리 전역→화면 로컬로 변환). `max_seconds`(기본 15)로 프레임 상한을 강제해 메모리 폭주 방지. `stop()`/최대길이 도달 시 `finished(frames, interval_ms)`, ESC 폴링(`GetAsyncKeyState`)·빈 프레임 시 `cancelled()` emit. **`_RecordController`**: 녹화 중 화면에 떠 있는 `● REC`·경과시간·`■ 정지` 컨트롤 바(항상 위·비활성 표시). 녹화 영역 '밖'(위, 공간 없으면 아래)에 배치해 프레임에 안 잡히게 한다(⚠ 전체 화면 높이 영역은 위·아래 공간이 없어 겹칠 수 있음 — MVP 한계). **`encode_gif(frames, path, interval_ms, max_width=800, max_colors=256)`**: QImage→PIL 변환 후 max_width 초과 시 비율 유지 다운스케일(GIF 용량 억제), 프레임별 팔레트 양자화, `save_all=True`+`optimize=True`+`disposal=2`로 저장. main이 `ThreadPoolExecutor`로 워커에서 호출(UI 블로킹 방지). **배선**: 단축키(`ctrl+shift+g`, 설정 가능) → `_on_record_gif_hotkey`(이미 GIF·영상 어느 쪽이든 녹화 중이면 무시 — `_recording_in_progress()`)가 `_recording_mode="gif"`로 표시하고 `capture_overlay.start(select_only=True)` → `region_selected` → `_on_record_region_selected`가 `_recording_mode`로 분기해 `_start_gif_recording`(150ms 지연 후 녹화 시작 — 오버레이 잔상이 첫 프레임에 안 잡히게) → `_on_record_finished`(인코딩 워커) → `_on_gif_saved`(캡처 폴더에 `rec_<ts>.gif` 저장 + **파일 경로를 클립보드 텍스트로** 복사 — GIF는 클립보드에 애니메이션으로 못 올라가므로 이미지→경로 패턴과 동일하게 파일/경로로 넘긴다. ⚠ 단 이미지→경로(Ctrl+Shift+P)와 달리 **히스토리·큐에도 저장한다**(`_persist_clipboard_item`) — 그쪽은 히스토리에 이미 있는 원본 이미지의 파생 표현이라 굳이 또 안 남기지만, GIF 경로는 원본이 따로 없는 새 콘텐츠라 안 남기면 다음 복사 시 흔적 없이 사라진다(2026-08-03) + 첫 프레임 썸네일 토스트). fps·최대길이는 DB 키(`gif_fps`·`gif_max_seconds`). **커서 합성(`composite_cursor(qimg, screen, local_rect)`, 2026-08-03)**: `screen.grabWindow()`(Qt/BitBlt)는 마우스 커서를 안 담는다. 커서 아이콘을 별도 추출해 알파 블렌딩하는 대신, **캡처된 프레임을 GDI 비트맵에 그대로 실어(`SetDIBits`) 그 위에 `DrawIconEx`로 커서를 그린 뒤 다시 꺼낸다(`GetDIBits`)** — Windows가 실제 배경 위에 직접 합성하므로 모노크롬(AND/XOR 마스크)이든 최신 32bpp ARGB 커서든 형식 무관하게 정확하다. 좌표 변환(물리 커서 위치 → 캡처 영역의 로컬 물리 픽셀)은 `capture_overlay._monitor_phys_origin`(모니터 물리 원점 + `QScreen` DPR)을 그대로 재사용 — 이 정확한 변환 공식이 이미 검증돼 있어 새로 유도하지 않았다. ⚠ **argtypes 충돌 방지를 위해 전용 `ctypes.WinDLL("user32"/"gdi32")` 인스턴스**를 쓴다(공유 `ctypes.windll.*`에 걸면 다른 모듈의 argtypes 설정과 충돌해 `ArgumentError`가 난다는 `uia.py`의 교훈을 그대로 따름). 설정 `gif_show_cursor`(기본 켜짐, 설정창 "GIF/영상 녹화 시 마우스 커서 표시" 체크박스)로 GIF·영상 녹화 공통 on/off — 화면 조작을 보여주는 용도상 커서 없는 쪽이 오히려 드물어 기본 ON으로 정했다. **한계(MVP)**: 선택이 시작된 단일 모니터 한정(크로스 모니터 미지원 — 세컨더리 모니터 grab 좌표는 미실측).
- **`video_recorder.py`** — **영상(MP4) 녹화(2026-08-03 도입).** GIF 녹화와 같은 영역선택 흐름(`capture_overlay`의 `select_only`)을 공유하지만, 프레임을 리스트에 모았다가 끝에 인코딩하는 대신 **매 틱 `cv2.VideoWriter.write()`로 즉시 디스크에 흘려쓴다** — 메모리에 안 쌓이므로 GIF(최대 15초 프레임 상한)보다 훨씬 길게(기본 상한 600초) 녹화해도 안전하고 파일 용량도 훨씬 작다. `_RecordController`(정지 버튼 위젯)와 `composite_cursor`(커서 합성)는 `gif_recorder.py` 것을 그대로 import해 재사용 — 같은 UX·같은 커서 합성 로직을 두 벌 두지 않기 위함. **`VideoRecorder.start(rect, path)`**: 선택 영역 크기(논리)×DPR로 물리 픽셀 출력 크기를 정하고(일부 코덱이 짝수 폭/높이를 요구해 홀수면 1px 잘라냄) `cv2.VideoWriter_fourcc(*"mp4v")`로 라이터를 연다. ⚠ **`cv2.VideoWriter`는 코덱을 못 열어도 예외를 안 던지고 조용히 무효화되므로, `isOpened()`를 직접 확인해 실패 시 `RuntimeError`로 바꿔 호출자(`main._start_video_recorder_now`)가 토스트로 알릴 수 있게 한다.** `_qimage_to_bgr_array`: `QImage.Format_RGB32`가 메모리상 B,G,R,X(리틀 엔디언) 순서라 OpenCV의 기본 채널 순서(BGR)와 그대로 맞아 채널 스왑 없이 알파/패딩만 제거해 numpy 배열로 변환. **새 의존성**: `opencv-python-headless`(`requirements.txt`) — Pillow는 GIF 등 이미지 포맷 인코더일 뿐 비디오 컨테이너/코덱 먹싱을 지원하지 않아 GIF 경로를 재사용할 수 없었다. **왜 `-headless`인가**: 일반 `opencv-python`은 highgui용 Qt5 플러그인을 번들해 PyQt6와 같은 프로세스에서 플러그인 경로 충돌 위험이 있고, `VideoWriter`만 쓰므로 GUI 모듈이 애초에 불필요하다. **왜 mp4v인가(검토한 대안)**: h264 인코딩이 가능한 `imageio[ffmpeg]`(ffmpeg 바이너리 자체를 번들, 수십MB 추가)도 검토했으나, PyInstaller onefile exe 용량을 더 키우는 무거운 선택지라 재생 호환성 문제가 실사용에서 나오면 그때 전환하기로 하고 먼저 mp4v로 시작했다. **배선**: 단축키(`ctrl+shift+r`, 설정 가능) → `_on_record_video_hotkey`가 `_recording_mode="video"`로 표시 후 GIF와 같은 `capture_overlay.start(select_only=True)` → `region_selected` → `_on_record_region_selected`가 분기해 `_start_video_recording`(캡처 폴더에 `vid_<ts>.mp4` 경로 결정) → 150ms 후 `_start_video_recorder_now`(`VideoRecorder.start` 예외를 여기서 잡아 토스트) → `finished(path)` → `_on_video_saved`(GIF의 `_on_gif_saved`와 동일 패턴 — 경로를 클립보드 텍스트로 복사 + `_persist_clipboard_item`으로 히스토리·큐 저장 + 토스트, 별도 인코딩 워커가 없어 GIF보다 한 단계 짧다). **`extract_first_frame_png(path)`**(2026-08-04 도입): mp4 첫 프레임을 cv2로 다시 읽어 PNG bytes로 반환 — GIF는 `QPixmap(path)`가 파일을 직접 디코딩해 토스트 썸네일을 그리지만(Qt 내장 GIF 지원) mp4는 Qt가 못 읽으므로, 이미 녹화에 쓰던 cv2로 같은 결과를 만든다. `main._on_video_saved`가 이 PNG bytes를 `ToastNotification(..., image_bytes=thumb)`(`ui/toast.py`의 메모리 바이트 썸네일 경로, GIF의 `image_path` 경로와 대응)로 넘겨 GIF와 동일하게 저장 완료 토스트에 썸네일이 뜬다(추출 실패 시 조용히 `None` → 아이콘 `🎬`로 폴백). **한계**: GIF와 동일하게 선택이 시작된 단일 모니터 한정.
- **`stt_engine.py`** — **음성 입력(STT), 게이트웨이 오디오 입력 스파이크로 타당성 확인 후 도입(2026-08-02, 실측 근거·계획 전문은 `whispering-tidy-otter.md`).** ⚠ **오디오 입력은 모델 단위가 아니라 계열(family) 단위로 갈린다** — Gemini 8종 실호출 8/8 성공, GPT·Claude·Grok·Perplexity는 시도한 것 전부 400 에러(오디오 콘텐츠 자체를 거부). 그래서 STT 모델 화이트리스트는 개별 모델명이 아니라 `ocr_engine.family_of(name) == "Gemini"` 계열 필터(신규 Gemini 모델 자동 커버). **`Recorder`**: `sounddevice.InputStream`(신규 의존성, PortAudio 번들이라 추가 설치 불필요) + stdlib `wave`로 16kHz mono 16bit wav 인코딩, `latency="low"`로 지연 축소. 오디오 콜백에서 RMS를 `_level`(락 없는 단순 float, GIL 하 안전)에 갱신 → `get_level()`로 `ui/stt_indicator.py`의 이퀄라이저가 폴링. `start(device=...)`가 지정 마이크를 열되 없으면(USB 분리 등) 조용히 시스템 기본으로 폴백(`used_device`로 main이 폴백 여부 확인해 토스트). `list_input_devices()`는 **MME 호스트 API로 한정**(PortAudio가 같은 물리 장치를 호스트 API마다 중복 나열해 필터링 없인 같은 이름이 3~4번 겹침). **`transcribe(wav_bytes, api_key, base_url, model)`**: `ocr_engine.py`의 `_get_client`(캐시된 openai 클라이언트)·`_normalize_base_url` 패턴을 그대로 재사용(크리덴셜도 기존 AI 프로필 공유, 새 시크릿 키 없음), 폴백 체인 없음(Gemini 화이트리스트라 OCR과 폴백 의미가 다름). **속도 튜닝**: 기본 모델을 `gemini-2.5-flash`(평균 ~4.4s)에서 `gemini-3.1-flash-lite`(평균 ~2.6s, 인식 결과 동일)로 전환(`ocr_engine.STT_FALLBACK_DEFAULT`) — 5회 반복 실측으로 모델 선택이 주 병목임을 확인, max_tokens 축소는 0.2~0.4s 개선에 그쳐(노이즈 수준) 기각. **크레딧 확인**(`ocr_engine.get_credit_balance`, STT 도입과 함께 추가): 처음엔 `CERTIFICATE_VERIFY_FAILED`로 실패 — `urllib` 기본 SSL 컨텍스트가 사용자의 실제 게이트웨이 도메인 인증서를 못 믿었던 반면 `openai` SDK(`httpx`, `certifi` 번들)는 정상 접속되는 비대칭이 원인. `ssl.create_default_context(cafile=certifi.where())` 명시로 해결(`certifi`는 openai 전이 의존성이라 이미 설치돼 있었지만 직접 import하므로 `requirements.txt`에 명시 추가). 길이 상한 30초 자동 컷(토큰 비용·응답 지연 억제).
- **`models.py`** — `ClipboardItem` 데이터클래스 (id, content_type, text_content, image_data, html_content, rtf_content, preview_text, thumbnail, created_at, is_pinned, pin_order, extra_formats). `extra_formats`는 `{format_id: bytes}` dict — 노션 등 앱 전용 포맷 보존.
- **`database.py`** — SQLite(`pasteflow.db`). `clipboard_items`(50개 FIFO 히스토리)와 `settings` 두 테이블. 고정(pin) 항목은 50개 제한에서 제외. `history_order`는 DB 전용 컬럼(ClipboardItem 필드 아님) — 비고정 항목 표시 순서 관리(오름차순=최상단, `save_item`이 새 항목에 `MIN-1` 부여). `bump_history_to_top(item_id)` — 기존 비고정 항목을 `history_order=MIN-1`로 최상단 이동(복사·붙여넣기 시 "최상단=현재 클립보드" 유지, `is_pinned=0` 가드로 고정 항목 제외). **동시성**: 단일 커넥션을 모든 호출이 공유하며 `_lock`(RLock)으로 읽기·쓰기를 직렬화 — UI/훅/워커가 동시에 접근해도 안전(레이스로 인한 `InterfaceError` 방지). 외부에서 `with db._lock:` 블록을 잡고 다중 쿼리를 묶을 수 있다(`_migrate_secrets` 등에서 사용). ⚠ **`ai_history` 테이블**(v1.49.2 도입): v1.6x에서 우클릭 "AI에게 질문" 기능 자체를 제거하며 이 테이블에 쓰는 CRUD 메서드(`save_ai_conversation`/`update_ai_conversation`/`get_ai_history_list`/`get_ai_conversation`/`delete_ai_conversation`)도 함께 지웠다. 기존 사용자의 과거 대화 데이터는 사용자 콘텐츠라 삭제하지 않는다 — `CREATE TABLE IF NOT EXISTS`만 남아 새 설치에서도 빈 테이블이 생기지만, 어떤 코드도 더는 읽거나 쓰지 않는다.
- **`clipboard_monitor.py`** — `WM_CLIPBOARDUPDATE` Windows 이벤트 기반 백그라운드 감시. 텍스트, 이미지, HTML, RTF 캡처. `_self_triggered` 플래그로 자체 트리거 방지. `_compute_hash()`로 콘텐츠 해시를 계산해 직전 항목과 동일하면 중복 추가 방지(`content_hash == _last_hash` 비교). `_create_thumbnail()`로 이미지 데이터에서 썸네일 생성 — **PIL로 직접 열어 보고(PNG·JPEG·GIF·WebP·BMP·TIFF), 실패할 때만 raw CF_DIB로 보고 `_dib_to_bmp()`로 14바이트 BMP 파일 헤더를 조립**해 인식시킨다. 옛 "PNG가 아니면 전부 DIB" 분기는 탐색기에서 복사한 `.jpg`(**CF_HDROP 캡처 경로** — 이미지 파일을 복사하면 그 **파일 바이트를 그대로** `image_data`에 싣는다)에 가짜 BMP 헤더를 붙여 썸네일 생성이 실패했고, 패널이 썸네일 대신 `[이미지]` 텍스트로 폴백했다. **`_read_clipboard() -> list[ClipboardItem]`**(2026-07-31): 항상 리스트를 반환 — 단일 항목 경로도 길이 1 리스트로 흐르고, `_on_clipboard_changed`가 리스트를 순서대로 순회하며 항목마다 개별 중복 해시 체크 + `on_new_item` 콜백을 호출한다. **CF_HDROP 다중 파일 복사**: 탐색기에서 이미지 파일 **여러 개**를 한 번에 복사하면(`len(files) > 1`) 확장자가 이미지인 파일마다 각각 읽어 별도 `ClipboardItem`으로 반환 — 옛 코드는 `len(files) == 1`일 때만 처리해 다중 선택 복사가 통째로 무시됐다(파일마다 히스토리에 순서대로 쌓이도록 수정). 파일 포맷 판별은 모듈 함수 `is_encoded_image(data)`(시그니처 매칭 — raw DIB는 `biSize`=12/40/108/124로 시작해 어떤 시그니처와도 충돌하지 않는다)가 담당하며 `paste_interceptor._set_clipboard`도 이걸 공유한다. ⚠ `_dib_to_bmp`는 **BI_BITFIELDS(compression=3)일 때 헤더 뒤 RGB 마스크 3×DWORD(12B)를 픽셀 오프셋에 더해야 한다** — 빠뜨리면 픽셀 시작점이 밀려 색이 뒤집혀 읽힌다(실측: 빨강→파랑). **앱 전용 포맷 캡처 제외 화이트리스트**: `extra_formats` 캡처 시 포맷 이름이 `_OLE_CLIPBOARD_FORMATS`(`DataObject`·`Ole Private Data`·`Object Descriptor` 등 OLE `IDataObject` 마샬링 포맷)·`_HWP_NATIVE_FORMATS`(`Hwp Native`·`Hwp_Native_Info`)에 들면 저장하지 않는다. 이 포맷들은 실제 콘텐츠가 아니라 원본 프로세스를 가리키는 참조라 평면 바이트로 복원하면 stale 참조가 되고, 한글(HWP)은 붙여넣기 시 자기 네이티브·OLE 포맷을 평면 포맷(HTML/RTF/텍스트)보다 우선 채택하므로 죽은 참조를 읽어 **한글→한글 붙여넣기가 통째로 실패**한다(증상: 팝업/큐는 정상인데 아무것도 안 붙음). 제외하면 한글이 평면 포맷으로 폴백해 정상 붙여넣기 — 한글 전용 객체(수식·글상자 등) 충실도는 포기하는 트레이드오프. `DOCX Format`은 유지(Word 충실도 보호, 한글에서도 무해 확인). 기존 DB에 캡처돼 있던 옛 항목은 여전히 이 포맷을 품으므로 한글에 붙이면 실패할 수 있고, 수정 후 새로 복사한 항목부터 정상.
- **`paste_queue.py`** — 순차 붙여넣기 큐 관리. **두 가지 큐 초기화 트리거**:
  - **트리거 A — `mark_plain_paste()`**: 일반 Ctrl+V 감지 시 paste_interceptor 콜백이 호출 → 큐를 즉시 비움(`_reset_unlocked`). 시간 무관. 사용자 명시적 시그널이라 즉시 반영.
  - **트리거 B — idle timeout**: `add_item()`에서 마지막 복사(`_last_copy_time`)로부터 `idle_reset_sec`(기본 10초, `DEFAULT_IDLE_RESET_SEC`) 이상 경과한 새 복사면 이전 큐를 버리고 새 항목 1개로 시작. 첫 복사(`_last_copy_time==0`)는 대상 아님.
  - `add_item()` 리셋 조건 = `pointer > 0` OR idle 만료. 그 외엔 누적. 매 호출마다 `_last_copy_time = time.monotonic()` 갱신.
  - 추가 공개 메서드: `set_queue(items, pointer=0)` — 패널에서 특정 항목부터 시작할 때 큐를 직접 교체; `undo_last()` — 포인터를 1 감소시켜 마지막 붙여넣기를 1단계 되돌리기; `clear()` — 큐 및 포인터 초기화(사용자 명시적 호출, 우클릭 큐 해제); `set_idle_reset_sec(sec)` — 런타임 idle 값 변경(설정 다이얼로그에서 호출); `remove_item(item_id) -> bool`(v — 2026-08-01) — id로 큐에서 항목 제거, 히스토리 삭제와 큐 동기화용. 큐는 캡처 시점 스냅샷을 DB와 별개로 들고 있어 `main._on_delete_item`이 DB만 지우면 큐/진행 HUD엔 지운 항목이 그대로 남았다(캡처 3개 중 1개 삭제해도 HUD는 3개로 표시). 제거된 항목이 `pointer` 이전(이미 소진)이면 포인터도 함께 1 감소.
  - 내부 헬퍼 `_reset_unlocked()` — `clear()`와 `mark_plain_paste()`가 공유. 호출자가 이미 `_lock`을 잡은 상태여야 함.
- **`paste_interceptor.py`** — WH_KEYBOARD_LL 저수준 키보드 훅으로 단축키 감지:
  - **Ctrl+Shift+V**: 큐에서 다음 항목 가져오기 → 클립보드 교체 → `_send_clean_key(VK_V)` 호출(현재 눌린 수정키 해제 → Ctrl+V SendInput → 수정키 복원) (suppress). `_send_clean_key`는 수정키 해제 직전·복원 직후 미할당 키 `VK_MASK`(0xE8)를 톡 쳐서, V가 suppress돼 "벌거벗은 Ctrl+Shift"로 오인되어 Windows 입력기 전환(한컴↔MS) 팝업이 뜨는 것을 막는다. 붙여넣기 직전 summary 항목이면 DB에서 전체 데이터 로드(인터셉터 생성 시 주입된 `get_full_item` 콜백 호출 → 실제 DB 메서드는 `db.get_item`).
  - **일반 Ctrl+V 관찰**(suppress 안 함 — fall through로 통과): `ctrl && !shift && !alt && vk==V`일 때 `KBDLLHOOKSTRUCT.flags & LLKHF_INJECTED(0x10)`로 PasteFlow 자체 주입(Ctrl+Shift+V·direct_paste·send_ctrl_v_to)을 제외하고, 물리 키이면 `on_plain_paste()` 콜백 호출. main이 이를 `_bridge.plain_paste` 시그널 emit으로 연결해 메인 스레드 슬롯 `_on_plain_paste()`에서 `queue.mark_plain_paste()` + 패널/트레이 큐 표시 갱신을 한 번에 수행. CLAUDE.md 금지 조항 1(Ctrl+V suppress 금지) 준수. 한계: `Ctrl+Insert`·`Shift+Insert` paste와 AHK 등 매크로 주입 paste는 감지 안 됨.
  - **패널 토글 단축키** (기본 `ctrl+space`, 설정 가능): 패널 열기/닫기. `set_panel_hotkey()`로 런타임 변경 가능. RegisterHotKey 대신 WH_KEYBOARD_LL을 사용하므로 탐색기 등 모든 포그라운드 앱에서 동작.
  - **이미지→경로 단축키** (기본 `ctrl+shift+p`, 설정 가능): suppress. `on_image_to_path` 콜백 호출 → main이 **최신 히스토리 이미지**를 임시 PNG로 저장하고 절대경로 텍스트로 클립보드 교체 후 포그라운드 창에 자동 Ctrl+V(경로가 히스토리에 안 남아 같은 이미지를 여러 번 붙일 수 있음 — Ctrl+V의 무한 반복과 대칭). Claude Code CLI 등 "경로 텍스트"를 첨부로 받는 앱에 한 키로 붙여넣기 위한 진입점. `set_image_to_path_hotkey()`로 런타임 변경 가능.
  - **순차 경로 붙여넣기 단축키** (기본 `ctrl+shift+[`, 설정 가능): suppress. **이미지→경로의 '순차 버전'** — `on_seq_image_to_path` 콜백 호출 → main `_on_seq_image_to_path_hotkey`가 **순차 붙여넣기(Ctrl+Shift+V)와 같은 큐·포인터를 공유**하며 큐에서 다음 항목을 꺼내 **이미지면 임시 PNG 경로 텍스트로**, 아니면 원본 그대로 붙여넣는다. 캡처(Alt+F2)가 이미 큐에 이미지로 쌓이므로 캡처 여러 장을 이 키로 순서대로 경로 텍스트로 붙일 수 있다(예: 캡처1·2 → 이 키 두 번 → 경로1·2). 큐 소진 시 토스트만 표시(현재 클립보드 폴백은 일반 Ctrl+Shift+P가 담당 — '순차/일반'을 키로 구분). `set_seq_image_to_path_hotkey()`로 런타임 변경. 기본값 `[`(VK_OEM_4 `0xDB`)는 `_SPECIAL_KEY_MAP`에 있어 파싱되고, 설정창 녹화기(`_qt_key_to_name`)는 `Key_BracketLeft`/`Key_BraceLeft`(Shift 시 `{`)를 모두 `[`로 매핑해 재지정을 지원. Ctrl+Shift 조합이라 언어전환 마스킹은 기존 `_send_clean_key` 로직이 그대로 처리.
  - **AI 자유질문 단축키** (기본 `alt+1`, 2026-08-03부터 — 예전 기본값은 `` alt+` ``였다, 설정 가능): suppress. `on_ask_ai` 콜백 호출 → main이 컨텍스트 없이 즉석에서 AI 질문 입력창을 띄운다(상시 켜진 PasteFlow에서 한 키로 AI 호출). `set_ask_ai_hotkey()`로 런타임 변경 가능. 백틱(`` ` ``)은 여전히 `_SPECIAL_KEY_MAP`에 `0xC0`(VK_OEM_3)로 매핑돼 파싱되므로 사용자가 원하면 다시 그 조합으로 되돌릴 수 있다.
  - **영역 캡처 + Gemini 질문창 첨부 단축키**(`Gemini(캡처)`, 기본 `alt+2`, 2026-08-03부터 — 예전 기본값은 `` win+` ``였다, 설정 가능, 2026-08-02 사용자 요청으로 기능 도입): suppress. 영역 캡처(Alt+F2)와 완전히 같은 오버레이를 띄우되(`_capture_overlay.start()`), main이 `_capture_then_ask` 플래그를 세워 `_on_capture_region`이 저장까지 끝낸 이미지를 곧장 `_open_ai_dialog(initial_image_png=...)`로 Gemini 질문창에 첨부한다(같은 캡처 흐름을 두 벌 만들지 않기 위한 설계) — 사용자는 질문만 타이핑하면 된다. 캡처가 ESC·우클릭으로 취소되면 `cancelled` 시그널이 `_on_capture_cancelled`를 통해 이 플래그를 반드시 되돌린다(안 그러면 다음 일반 Alt+F2가 엉뚱하게 질문창을 연다). **`` win+` `` 기본값을 `alt+2`로 바꾼 이유(2026-08-03)**: Windows Terminal이 `` win+` ``를 자체 "퀘이크 모드" 전역 단축키로 기본 등록해 두는 게 마이크로소프트 공식 GitHub 이슈에도 등재된 잘 알려진 동작이라, Windows Terminal이 깔린 PC에서는 설정창에서 이 조합을 녹화하려 해도 터미널이 먼저 반응해 캡처 자체가 안 되는 충돌이 있었다(사용자 리포트: "단축키 설정에서 윈도우 백틱 누르면 터미널만 열림"). Win 조합 자체는 여전히 파싱 가능(`set_capture_ask_hotkey` — STT의 `set_stt_hotkey`와 같은 `mod_tokens` 구조, 이쪽은 항상 일반키가 있는 트리거형) — dispatch에서 `win_pressed`(`_mod_win` 자체 추적)까지 비교하지만, 기본값은 다른 앱과 안 겹치는 `alt+2`로 옮겼다. ⚠ **Win+일반키에서 그 일반키를 통째로 suppress하면 Windows가 "Win만 눌렸다 떼짐"으로 오인해 좌하단 시작 메뉴가 뜬다**(Alt 단독 탭이 메뉴바 포커스로 해석되는 것과 동일 부류 — 위 STT의 Alt+R 처리와 같은 원인, 2026-08-02 사용자 실측: "단축키 누를 때 윈도우 버튼만 누른 것처럼 좌하단 윈도우창 열림"). 그 STT 처리와 동일하게 미할당 가상키(`VK_MASK`)를 더미로 눌렀다 떼서 "Win 단독"이 아니라 "Win+뭔가"로 보이게 만들어 해결 — 이 라이브 dispatch 경로의 방어책이라, Win 조합을 직접 골라 쓰는 사용자에게는 지금도 유효하다.
  - **음성 입력(STT) 단축키** (기본 `ctrl+win`, 설정 가능, 2026-08-02 도입 — 상세·근거는 `stt_engine.py` 항목과 `whispering-tidy-otter.md` 참고): 다른 단축키와 달리 **keydown(녹음 시작)·keyup(녹음 종료+전송) 둘 다 처리하는 푸시투토크**라 `_stt_active`로 진행 상태를 추적한다. 기본값이 Wispr Flow와 동일 제스처인 `Ctrl+Win`(일반키 없이 수식키 두 개만)이라 `set_stt_hotkey()`가 일반키가 없으면 `_stt_mod_only=True`로 표시 — 이땐 keydown에서 "필요한 수식키가 전부 눌렸는가"로 시작, keyup에서 그중 아무 수식키나 떼면 종료(둘 중 어느 쪽을 먼저 떼도 동일). **Ctrl·Win 둘 다 suppress하지 않는다** — 일반키를 통째로 삼키는 게 아니므로 "수식키 단독 탭"으로 오인될 상황 자체가 없어 시작 메뉴가 안 뜬다(아래 `alt+r` 시절의 부작용과 대조). 옛 기본값 `alt+r`(일반키 R이 있는 조합)이던 시절엔 R을 suppress하면 앱이 "Alt만 눌렸다 떼짐"을 봐 Windows가 메뉴바 포커스 진입으로 오해했고, 그 직후 주입한 Ctrl+V가 메뉴에 먹혀 입력창에 아무것도 안 들어갔다 — `_send_clean_key`의 Ctrl+Shift IME 마스킹과 동일 기법(미할당 키 `VK_MASK`를 더미로 눌렀다 뗌)으로 해결(위 Win+`` ` `` 캡처+질문 단축키가 겪은 "Win 단독 탭→시작 메뉴" 버그와 같은 뿌리). 녹음 자체는 `stt_engine.Recorder`(오디오 콜백에서 RMS 갱신), 종료 후 인식은 `stt_engine.transcribe()` → 결과를 OCR과 동일 경로(`_persist_clipboard_item`+`_send_clean_key`)로 처리. 끝자락 잘림 완화로 keyup 즉시 멈추지 않고 `_STT_TAIL_PAD_MS`(400ms) 여유 후 실제 정지(`_on_stt_stop`→`_finish_stt_recording` 2단계 분리).
  - **GIF 녹화 단축키** (기본 `ctrl+shift+g`, 설정 가능): suppress. `on_record_gif` 콜백 호출 → main이 캡처 오버레이를 `select_only`로 띄워 영역을 받고 그 자리를 라이브로 녹화해 GIF로 저장한다(`gif_recorder.py` 참조). `set_record_gif_hotkey()`로 런타임 변경 가능. 트리거 시 `AllowSetForegroundWindow(ASFW_ANY)`로 선택 오버레이·정지 컨트롤러가 포그라운드를 잡게 한다(캡처 트리거와 동일).
  - **suppress한 단축키의 keyup도 함께 막는다**: 훅이 keydown만 suppress하고 keyup을 흘려보내면, 브라우저가 포커스된 `<button>`을 **Space keyup에서 클릭 처리**하므로 Ctrl+Space(패널 토글)가 유튜브 재생/멈춤을 토글한다(Playwright 실측: 재생 버튼 포커스 상태에서 keyup만 보내도 paused 전환). suppress한 keydown의 vk를 `_suppress`에 기억해 그 짝 **물리** keyup도 막는다. ⚠ **주입된 keyup은 반드시 통과시킨다** — Ctrl+Shift+V는 V의 물리 keydown을 막은 직후 스스로 Ctrl+V를 SendInput하는데, 그 주입 keyup까지 삼키면 대상 앱이 'V가 눌린 채'로 남는다(자기충돌). `LLKHF_INJECTED`로 물리 키만 거른다. 훅에서 keyup을 무조건 차단하는 방식은 우리가 막지 않은 키의 keyup까지 삼켜 타이핑이 깨지므로 기각됐다.
  - **절대 일반 Ctrl+V 키 이벤트를 차단하지 않음.**
  - **이미지 클립보드 복원**: `_set_clipboard`는 `image_data`를 **세 갈래**로 등재한다 — ① **PNG**: `"PNG"` 등록 포맷 + `_image_to_dib`(PIL, 24bpp·투명은 흰 배경 합성)로 변환한 CF_DIB **병행** 등재("PNG"를 못 읽는 그림판·한글 등에서 붙여넣기 무반응이던 문제 해결, v1.21.0). ② **그 밖의 파일 포맷**(JPEG·GIF·WebP… — `clipboard_monitor.is_encoded_image`로 판별): `_image_to_dib`로 **변환해** CF_DIB로 등재. ③ **raw CF_DIB**: 그대로. ⚠ ②를 빠뜨리면(옛 코드의 `is_png` 이분법) 탐색기에서 복사한 JPEG **파일 바이트가 CF_DIB로 그대로 올라가** 받는 앱이 앞 40바이트를 `BITMAPINFOHEADER`로 오독하고 붙여넣기가 깨진다(썸네일 누락과 같은 뿌리 — 위 `clipboard_monitor` 항목). 변환은 훅 콜백 내 동기 실행(2560×1440 PNG 150~200ms·12MP JPEG 175ms 실측).
  - 추가 공개 메서드: `direct_paste(item)` — 순차 큐 포인터 변경 없이 즉시 붙여넣기(Enter·드래그 경로 사용); `send_ctrl_v_to(hwnd)` — 대상 윈도우 포커스 후 Ctrl+V 전송; `send_plain_key(vk)` — 수정키 없이 키 하나를 눌렀다 뗀다(v1.52.0, 브라우저 주입의 Enter용 — `_send_clean_key`는 `Ctrl+{key}` 조합이라 단독 키에 못 쓴다. 포그라운드 창에 그대로 들어가므로 호출자(`main._inject_to_google`)가 브라우저가 앞에 있는지 먼저 확인한다).
  - **설정창 단축키 녹화 중 Win키 보호**(`_suspended`, 2026-08-03 추가): 설정창에서 새 단축키를 녹화하는 동안(`suspend()`/`resume()`)은 훅이 아무 키도 가로채지 않고 그대로 통과시킨다 — 재바인딩 대상 옛 단축키가 발동하지 않게 하려는 기존 설계(커밋 `3117b2a`, 그때까진 문서화가 안 돼 있어 여기 함께 정리한다): `ui/settings_dialog.py`의 `HotkeyEdit.listening_changed` 시그널(녹화 시작/종료)이 `SettingsDialog.recording_active`로 모여 `main._on_hotkey_recording_active(active)`가 `active`면 `interceptor.suspend()`, 아니면 `resume()`을 호출한다(어느 `HotkeyEdit`이든 녹화를 시작하면 전체 훅이 멈춘다). 이 상태에서 사용자가 Win 포함 조합(예: Gemini(캡처)의 예전 기본값 `` win+` ``)을 녹화하려고 Win키만 먼저 누르면, 아무것도 가로채지 않으므로 Windows가 "Win 단독 눌림"으로 보고 실제 시작 메뉴(또는 Win 단축키를 등록해 둔 다른 앱)를 띄우고, 그 포커스 이탈이 `HotkeyEdit.focusOutEvent`를 타고 녹화 자체를 취소시켰다(2026-08-03 사용자 리포트). `_suspended` 블록 안에서도 물리(비주입) Win keydown이 오면 미할당 가상키(`VK_MASK`)를 더미로 눌렀다 떼 — 실제 키 전달(Qt·HotkeyEdit에는 그대로 보임)은 그대로 두고 Explorer 등의 "단독 Win" 판정만 무효화한다(위 `` win+` `` 실행 경로가 쓰던 것과 동일 기법을 녹화 중에도 적용). 짝을 이루는 수정: `HotkeyEdit.keyPressEvent`(`settings_dialog.py`)가 이 더미 키(매핑 안 되는 키)를 받아도 녹화를 취소하지 않고 계속 대기하도록 고쳤다 — 안 그러면 더미 키 자체가 방금 시작한 녹화를 끊어버린다. **`resume()` 안전망 이중화**(2026-08-03 후속): 위 `recording_active`는 각 `HotkeyEdit`의 `listening_changed(False)`로만 꺼지는데, 녹화 도중 설정창을 X버튼·`accept()`·`reject()` 등으로 닫으면 그 이벤트가 안 오는 경로가 있어 `_suspended`가 `True`로 영구 고정될 수 있었다(오프스크린 재현으로 확인 — 재현 후 재시작 전까지 전역 단축키 전부가 죽는다). `SettingsDialog.done()`(accept/reject 공통 경로)과 `closeEvent`(X버튼 등) 양쪽에서 `recording_active(False)`를 무조건 한 번 더 emit해 막는다(`resume()`은 상태 무관하게 `_suspended=False`라 idempotent).
- **`hotkey_manager.py`** — Win32 RegisterHotKey + 히든 윈도우 기반 단축키 유틸. 현재 패널 토글이 interceptor로 이동되어 실제 등록된 단축키 없음. `_SPECIAL_KEY_MAP`(VK 코드 매핑)은 paste_interceptor가 공유 사용.
- **`ocr_engine.py`** — OCR 추상화. `OcrEngine(language="ko").recognize(PIL.Image) → str` 동기 API. `kind` 파라미터로 엔진 선택(**단 앱 OCR 경로는 v1.28.0부터 항상 `kind="gemini"` — 설정에서 엔진 선택이 제거됨. 아래 `"winrt"` 엔진 코드는 남아 있으나 앱에서 호출되지 않는다**):
  - `"winrt"`(코드 잔존, 앱 미사용): winocr 패키지로 Windows WinRT OCR 래핑. 4096px 초과 이미지 자동 downscale, RGBA/L 모드 자동 변환, 언어팩 미설치 시 AssertionError → RuntimeError('언어팩') 변환.
  - `"gemini"`: Gemini API 직접 호출 (`_recognize_gemini()`). `base_url` 파라미터가 설정된 경우 OpenAI 호환 게이트웨이/프록시로 자동 분기 (`_recognize_openai_compat()`). `"openai_compat"`는 별도 kind 값이 아님. 모델명은 콜러가 `self.model`로 지정 — 빈 문자열이면 `gemini-3.1-flash-lite` 폴백. **`max_tokens=16384`** 사용: 게이트웨이가 reasoning(thinking) 토큰을 같은 max_tokens 예산에서 차감하므로 작게(2048) 잡으면 thinking을 많이 하는 모델(gemini-2.5-pro·gemini-3.1-pro-preview 등)에서 본문이 0~200자로 잘린다. 16384에서 6종 모델 모두 `finish_reason=stop`으로 정상 종료 확인. 청구는 실제 사용 토큰 기준이라 비용 영향 미세.
  - 모듈 함수 `_normalize_base_url(base_url)` — 사용자가 endpoint 전체 경로(`/chat/completions`, `/models`, `/completions`, `/embeddings`)나 trailing `/`를 붙여 입력해도 자동으로 SDK 표준 base 형태로 보정. `_get_client` 내부에서 호출되어 게이트웨이 경로(`_openai_compat_call`, `list_gemini_models`, 프로브) 전체가 공유.
  - **클라이언트 캐시**(`_get_client(api_key, base_url)`, 2026-07-31): 옛 코드는 OCR 호출마다 `openai.OpenAI()`를 새로 만들어 TCP/TLS 핸드셰이크를 매번 치렀다 — `(api_key, base_url)`별로 클라이언트를 캐싱(`_client_cache` + `_client_cache_lock`)해 커넥션 풀(keep-alive)을 재사용한다. `_openai_compat_call`·`list_gemini_models`·`probe_connection`/`probe_ocr_model`(경유) 전부 이 캐시를 공유. `warm_connection(api_key, base_url)`은 캐시된 클라이언트로 가벼운 `/models` 조회 1회를 실행해 DNS/TCP/TLS까지 미리 끝내는 워밍업 함수 — main.py `__init__`이 앱 시작 시 백그라운드 스레드(`_prewarm_ocr_connection`)로 호출한다(위 `main.py` 항목의 "OCR 연결 워밍업" 참고). 크리덴셜이 비었거나 실패해도 조용히 무시(최선 노력형).
  - 정적 메서드: `is_winrt_available()`, `is_winrt_language_supported(lang)`, `winrt_supported_languages()`, `list_gemini_models(api_key, base_url)` — **게이트웨이는 필터 없이 전 모델 반환**(v1.39.0 — 옛 `"gemini" in id` 필터 제거. 게이트웨이 호출 경로는 평범한 OpenAI 호환 chat.completions라 claude·gpt·grok·sonar 등도 그대로 동작하는데, 필터가 45종 중 6종만 노출시키고 있었다). **v1.41.0부터 유령 모델도 걸러내지 않는다** — 못 쓰는 모델은 설정창 콤보가 매트릭스를 보고 회색·비활성 + 🚫로 이유까지 표시한다(조용히 지우면 사용자가 "왜 없지?"를 알 수 없다). **`base_url`이 없으면 RuntimeError** — v1.50.0에서 공식(Google AI Studio) 경로가 제거돼 게이트웨이가 유일한 조회처다. 메서드 이름은 하위 호환으로 유지. 설정창 모델 새로고침에 사용.
  - **모델 라이브 프로브**(v1.42.0 — 옛 `model_matrix.json`(빌드타임 전수 스윕)과 그 조회 API 6종 `model_status`/`chat_capable`/`ocr_capable`/`ocr_is_weak`/`is_measured`/`matrix_model_names`, `tools/sweep_models.py`, `tools/sweep_raw/`, spec의 `datas` 항목을 **전부 대체**) — 어떤 모델이 실제로 되는지는 설정창 `연결 테스트`가 **선택된 두 모델을 그 자리에서 실호출해** 판정한다. 매트릭스를 버린 이유: 스윕 시점 스냅샷이라 게이트웨이가 라인업을 바꾸면 즉시 낡는데, 그 낡은 판정으로 콤보가 모델 **선택을 차단**(`setEnabled(False)`)했고 데이터가 exe에 번들돼 사용자가 고칠 수도 없었다. 낡은 `unknown`은 안전하게 열화(그냥 선택 가능)하지만 낡은 `fail`은 멀쩡한 모델을 영구히 못 고르게 만든다 — 이 비대칭이 결정적. `↻ 새로고침`은 여전히 **이름만** 가져온다(그게 API가 주는 전부).
    - `ProbeResult(status, detail)` — `status`: `ok` / `weak`(OCR이 이미지는 받았으나 글자를 못 읽음) / `fail`(이 모델·키로는 안 됨) / `retry`(429·503으로 **판정 불가**). **`fail`과 `retry`를 절대 섞지 말 것** — 섞으면 멀쩡한 모델을 나쁜 모델로 표시한다(옛 1차 스윕에서 공식 API 30종 중 21종이 429였고 그게 `fail`로 굳었다). 회귀 테스트 있음.
    - `probe_connection(api_key, base_url)` — 모델 목록 조회로 키·엔드포인트만 확인(어떤 모델도 호출 안 함).
    - `probe_ocr_model(api_key, base_url, model)` — `_probe_image_png()`(흰 바탕 큰 검은 `42`, 160×80)를 실제 OCR 경로로 1회. 응답에 `42`가 있으면 `ok`, 이미지는 받았는데 못 읽으면 `weak`, 이미지 자체를 거부(400)하면 `fail`.
    - ⚠ 프로브는 **`_call_with_fallback`을 거치지 않는다** — 폴백이 끼면 망가진 모델이 조용히 `gemini-2.5-flash`로 갈아타 ✓로 보고되어 테스트가 존재 이유를 잃는다.
    - 잃은 것: 옛 `ocr: L1`(작은 글씨 부정확) 등급 **하나뿐**. 정답 텍스트 대조가 필요해 라이브 프로브로는 `weak`(글자 못 읽음)까지만 잡는다. 차단이 아닌 경고였고 "품질은 사용자가 자기 캡처 대상으로 판단하는 게 정확"이라 감수. (옛 `🚫`는 프로브의 `fail`이, `📝`는 chat 프로브의 `weak`가, `❓`는 '테스트를 안 눌렀으면 아무 표시 없음'이 대신한다.)
  - 계열 그룹핑: `family_of(name)` — 모델 ID의 `/` 뒤 마지막 조각을 소문자화해 `_FAMILIES` 접두사와 매칭(`accounts/fireworks/models/gpt-oss-120b` → GPT, `meta-llama/Llama-4-…` → Llama). `group_models(candidates)` — `_FAMILIES` 고정 순서(마지막 `기타`)로 묶고 계열 안에서 대소문자 무시 알파벳순(`LGAI-…` 대문자 ID가 ASCII 순서로 튀어나오는 것 방지). **아무 모델도 걸러내지 않는다**(옛 `usable` 인자는 매트릭스와 함께 제거).
  - `select_fallback_model(failed_model)` — `_FALLBACK_CHAIN`(`gemini-2.5-flash` → `gemini-2.0-flash`)에서 실패 모델이 아닌 첫 항목, 없으면 `None`. 이 사슬만 상수로 남는 이유: 폴백은 **사용자가 볼 수 없는 자리**에서 일어나 후보를 실호출로 정할 기회가 없다.
  - **자동 폴백**(`_call_with_fallback(model, call)`) — `_recognize_gemini`/`_recognize_openai_compat`가 1차 호출에서 `_is_model_not_found` 휴리스틱(메시지에 `not found`·`model_not_found`·`404 ... model` 포함) 매칭 시 `select_fallback_model`로 1회 재시도. 인스턴스 필드 `last_used_model`(실제 응답 만든 모델) / `last_fallback_from`(원래 시도했다 실패한 모델, 폴백 없으면 `None`)에 결과 기록 → main 워커가 OCR 종료 후 `_bridge.ocr_fallback.emit(failed, used)` → 슬롯이 토스트 `{failed} → {used}로 폴백` 표시. DB의 `ocr_gemini_model` 자체는 수정하지 않음(잠깐 장애일 수 있음, 사용자 명시 선택 보존).
  - 호출자(main.py)가 `ThreadPoolExecutor(max_workers=1)`로 워커 스레드에서 실행해 UI 블로킹 방지.

### UI 컴포넌트 (`pasteflow/ui/`)

- **`panel.py`** — 고정 섹션 + 히스토리 패널 (검색 기능 없음 — 의도적으로 제거).
  - 항목 **단일 좌클릭**: 선택(하이라이트)만 — 선택은 **코랄 틴트 배경(`#4d3320`)+코랄 테두리**로 시인성 강조(테마 규칙 "코랄=선택·주목", 옛 회색 `surface2`만 바뀌던 낮은 시인성 개선). Ctrl+클릭/Shift+클릭으로 다중 선택.
  - **키보드 단축키(패널 포커스 시)**: `Ctrl+C`=복사(다중 선택 시 결합 복사 — `_kbd_copy`) / `c`=큐 토글(`_kbd_queue_toggle` — 수정자 없는 단일키. 복사는 `Ctrl+C`로만 두고 자주 쓰는 큐 토글에 단일키 `c`를 배정. 옛 `Alt+C` 큐 바인딩은 제거) / `Space`=미리보기 / `Enter`=붙여넣기(`_kbd_activate`→`paste_item_requested`) / `p`=고정 토글 / `s`=이미지 항목 파일로 저장 후 경로 복사(`_kbd_copy_path`, 2026-08-03 사용자 요청 — 우클릭 메뉴와 동일 동작, 이미지가 아니면 무시) / `Del`=삭제 / `↑↓`=포커스 이동. **항목 더블클릭-붙여넣기는 제거**(오발동으로 직전 포그라운드 창에 붙던 위험 방지 — `double_clicked` 시그널·핸들러 삭제, Enter·드래그가 대체). 빈영역 더블클릭=최소 크기 리셋은 별개로 유지.
  - **복사·붙여넣기 시 최상단 bump**: 히스토리 옛 항목을 복사(`_on_copy_item`)하거나 붙여넣기(Enter·드래그 → `_on_panel_paste`·`_on_drag_to_app`의 클립보드=항목 경로)하면 그 항목이 실제 클립보드가 되므로 `db.bump_history_to_top(id)`로 히스토리 최상단으로 올려 **"최상단 = 현재 클립보드" 불변식**을 지킨다(비고정 항목만, self-triggered `_set_clipboard`라 중복 항목은 안 생김 → 순서만 이동). 복사는 `notify_on_copy` 시 토스트도 함께. Alt+드래그(경로 텍스트)·이미지→탐색기 저장 경로는 클립보드가 항목 내용이 아니므로 bump 제외.
  - **우클릭 컨텍스트 메뉴**: "큐에 추가"(`queue_select_requested`) / 고정·해제 / 복사 / 수정(텍스트만) / 파일로 저장 후 경로 복사(이미지만 — `copy_image_as_path_requested` emit, main이 캡처 저장 폴더(설정 `capture_save_folder`, 기본 `<사진>\PasteFlow` — Alt+F2 영역 캡처와 동일 폴더)에 PNG 저장 후 절대경로를 클립보드 텍스트로 복사 + 저장한 PNG 썸네일 토스트 — `ToastNotification(..., icon="", image_path=saved_path)`로 "복사한 게 의도한 이미지 맞나" 시각 확인. 2026-08-02부터 `%TEMP%` 대신 이 폴더를 쓴다 — 관리 폴더가 둘로 나뉘는 것 방지, 이미지→경로 단축키(Ctrl+Shift+P 등)는 여전히 `%TEMP%` 사용) / Gemini에게 질문(이미지만 — `ask_ai_item_requested` emit, main의 `_on_ask_ai_item_by_id`가 DB 풀 로드 후 핀·미리보기와 공유하는 `_on_ask_ai_for_image` 코어로 위임) / 삭제 / 미리보기(이미지→`preview_image_requested` emit, 텍스트→`preview_text_requested` emit). 둘 다 main에서 받아 `ImagePreviewPopup.open_new(item, ...)` / `TextPreviewPopup.open_new(item, ...)` 호출 (동일 패턴 — 둘 다 `ClipboardItem`을 인자로 받음). ⚠ v1.6x에서 지웠던 "AI에게 질문" 항목(`ai_query_requested` 시그널)은 2026-08-03 `ask_ai_item_requested`로 재도입됐다 — 이번엔 답변을 PasteFlow 안에서 만들지 않고 이미지를 미리 첨부한 채 Gemini 질문창을 여는 것뿐이라(image_preview.py의 동명 기능과 동일 성격) v1.6x가 걷어낸 "AI 질의 기능 자체 제거" 방향과 충돌하지 않는다.
  - 항목 **드래그 → 외부 앱**: fake drag(DragCopyCursor) 방식으로 붙여넣기. 마우스업 시점에 `QApplication.keyboardModifiers() & AltModifier`로 Alt 눌림 여부를 캡처해 `drag_to_app_requested(item_id, cursor_pos, alt_held)` 시그널에 같이 전달.
    - **Alt+드래그 + 이미지**: 임시 PNG로 저장(`_save_image_to_drop_temp()`) 후 절대경로 텍스트를 클립보드에 넣고 `_activate_and_send_ctrl_v(root_hwnd, sender=interceptor._release_modifiers_and_send_ctrl_v)`로 SendInput(Ctrl+V). **마우스 업 시점에 사용자가 Alt를 여전히 누르고 있으므로** 기본 `_send_ctrl_v_plain`(수정키 해제 없음)을 쓰면 OS가 `Ctrl+Alt+V`로 오인해 붙여넣기가 일어나지 않는다 — `_release_modifiers_and_send_ctrl_v`가 눌린 Alt/Ctrl/Shift를 해제 후 Ctrl+V를 주입하고 Alt를 복원한다(Ctrl+Shift+P 단축키가 `_send_clean_key`를 쓰는 것과 동일한 이유). Windows Terminal의 claude CLI 등 "파일 경로 텍스트"를 첨부로 받는 앱 대응. WM_PASTE는 터미널이 무시하므로 무조건 SendInput 경로로 통일. 저장 실패 시 토스트 에러 + 종료(폴백 없음).
    - **이미지 + Explorer(`CabinetWClass`) / 바탕화면(`Progman`, `WorkerW`)**: PNG 파일로 저장(`_save_image_to_folder()`). 서브폴더 아이콘 위에 드롭 시 해당 폴더에 저장.
    - **Win32/WinUI3 앱**: `WM_PASTE`.
    - **Electron/Chromium 앱**: `AttachThreadInput+SendInput`.
  - 고정 항목 **드래그 → 재정렬**: fake drag 방식 (QDrag 미사용). 커서 아래 고정 항목 하이라이트 후 마우스 업 시 순서 교환.
  - 히스토리 항목 **드래그 → 재정렬**: `history_reorder_requested` 시그널 → main이 DB 업데이트.
  - **`update_queue_highlight()`**: 위젯 재생성 없이 색상만 업데이트하는 빠른 경로 (큐 상태 변경 시 사용).
  - **`clear_selection()`**(v1.58.0): 클릭 선택(`_selected_ids`)을 초기화. `PanelItemWidget._apply_bg_style()`은 `_is_selected`가 `in_queue`/`is_done` 큐 강조보다 **우선**해 코랄로 그리므로, 큐를 걸 때 클릭했던 항목(선택 상태가 남음)은 그 항목이 이미 소진(done)돼 큐 강조가 회색으로 바뀌어도 선택 강조 때문에 계속 코랄로 보인다 — "첫 항목만 안 끝난 것처럼" 헷갈림(2026-07-27 사용자 리포트). main이 두 지점에서 호출해 해소: ① `_clear_queue_ui()`(큐 소진·해제·일반 Ctrl+V 등 공통 클리어 경로) — 큐 전체가 끝나는 시점의 잔여 선택 정리, ② `_on_queue_select()`(큐를 거는 그 순간, 클릭으로 세운 앵커 항목 선택을 바로 해제) — 큐 진행 *중*에도 이미 소진된 항목이 즉시 회색으로 보이게 함(②가 없으면 큐가 다 끝나야만 지워져 진행 중엔 첫 항목이 계속 코랄로 남는다).
  - **`show_near_cursor()`**: 마우스 커서 우하단 +16px에 패널 표시. 화면 경계 초과 시 반전. 단축키/트레이로 패널을 열 때 사용.
  - **자동 닫기 토글 버튼(📌)**: 헤더 우측에 배치. ctypes `SetWindowPos(HWND_TOPMOST/NOTOPMOST)`로 TOPMOST 플래그만 변경(창 재생성·깜빡임 없음). 기본값: 자동 닫기 OFF(항상 위에 ON). `_auto_close` 플래그로 관리 — `False`이면 포커스를 잃어도 자동 닫히지 않음(`changeEvent` 조건: `not self._auto_close`). DB `settings`에 저장. `set_auto_close(value)` 메서드로 외부에서 설정.
  - **`panel_hidden` 시그널**: `hideEvent`에서 emit. (패널 자동 팝업이 제거되어 현재 main에서 소비하지 않음 — 향후 훅 용도로 시그널만 유지.)
  - **`auto_close_changed(bool)` 시그널**: 버튼 클릭 시 emit → main이 DB 저장.
  - **각 항목(PanelItemWidget)은 최대 5줄까지 표시**한다. 높이는 `label_h = visual_lines * fm.lineSpacing() + 8`, `widget_h = label_h + 12` 공식으로 계산. 창 리사이즈 시 `resizeEvent` + `_adjust_text_height()`로 동적 재계산. **레이블과 위젯 모두에 `setFixedHeight`를 명시적으로 설정**해야 한다(위젯에만 설정하면 레이블 높이가 따라오지 않아 클리핑 발생).
- **`image_preview.py`** — 이미지 미리보기 팝업. 다중 창 동시 표시 지원(`open_new(item, panel_geom, native=False)`로 생성 — text_preview와 동일하게 `ClipboardItem` 전체를 받음). 휠 줌, 드래그 이동, 더블클릭 닫기, ESC 닫기. 커서가 있는 모니터에 배치(`screenAt()`). **`native=True`**(핀 Alt+F3 경로가 사용)면 초기 줌을 원본 픽셀 1:1로 띄운다(화면 초과 시만 축소) — "캡처한 크기 그대로". 일반 미리보기는 `PREVIEW_MAX_W/H`(640×480)에 맞춰 축소. **cascade 위치**: `open_new`의 cascade offset은 "총 창 수"가 아니라 "새 앵커(커서/패널) 근처(`_CASCADE_NEAR`)에 이미 떠 있는 창 수"에만 비례 — 핀은 커서를 앵커로 쓰므로 커서를 옮겨 새로 핀하면 커서 바로 옆에 뜨고(드리프트 없음), 같은 자리 연속 핀만 어긋난다(겹침 방지). **우클릭 메뉴**: `복사` / `파일로 저장 후 경로 복사` / `Gemini에게 질문` / `주석 편집` / `닫기`(2026-08-02: 실사용 근거 0으로 OCR 항목 제거 — v1.6x에서 지웠던 "AI에게 질문"은 이미지를 미리 첨부해 AI 질문창을 여는 `Gemini에게 질문`으로 재도입됨). 시그널 `copy_requested(ClipboardItem)` → main의 `_on_copy_item`, `copy_as_path_requested(ClipboardItem)` → main의 `_copy_image_as_path_for_item`, `ask_ai_requested(ClipboardItem)` → main의 `_on_ask_ai_for_image`. **이 메뉴는 패널에서 연 미리보기(`_on_preview_image`)와 화면 핀(Alt+F3 `_on_pin_hotkey`) 양쪽에서 동일 연결** — 핀 항목은 DB id가 없으므로 main 핸들러는 id 기반 래퍼와 `ClipboardItem` 기반 코어(`_copy_image_as_path_for_item`)로 분리돼 있다. 메뉴는 `_effective_item()`을 대상으로 한다 — 씬에 주석이 있으면 `flatten_scene_to_png`로 평탄화한 임시 항목(id 없음), 없으면 원본 `self._item`(비파괴 — 벡터 주석은 씬에 남아 재편집·undo 가능). 평탄화본의 `복사`는 `copy_requested`(DB 항목 전제 — id 없으면 히스토리 미저장·무피드백) 대신 `annotated_copy_requested`(주석 편집 완료 복사와 동일: 클립보드+히스토리 저장+토스트) 경로로 emit한다(v1.21.0). **활성/비활성 테두리**: 활성(보고 있는 창)=코랄(`PEACH`, 주인공), 비활성=중립 회색(`SURFACE2`, 존재만 표시·안 튐) — QSS 동적 프로퍼티가 런타임에 재반영 안 돼 `_apply_active_style(active)`에서 스타일시트 직접 교체. **인라인 주석 편집**: `image_annotator._EditorMixin`을 상속해 같은 창에서 Space로 편집 모드 진입(main의 `_on_preview_image`는 이미 열린 창이면 닫지 않고 `toggle_edit_mode()` 호출). **툴바(chrome) 배치 = 하단 예약 strip 방식**(v1.18.0 도입, 이후 상단→하단으로 이전): chrome(타이틀바+툴바)을 레이아웃이 아니라 floating 자식(`_chrome`)으로 두고, 창은 `WA_TranslucentBackground` + root layout **bottom 마진**(`_chrome_h`)으로 **하단에 chrome 높이만큼 투명 strip을 항상 예약**한다(`_layout_chrome`이 `y=height-_chrome_h`에 배치, 뷰는 top 정렬). 뷰어 모드엔 strip이 투명이라 안 보이고(이미지만 떠 있는 느낌), 편집 토글은 그 예약 공간에 chrome을 show/hide(`_layout_chrome`)할 뿐이라 **창 크기·위치가 안 바뀐다 → 토글 시 잔상 없음 + 툴바가 이미지를 안 덮음**(이미지는 strip 위 배치). 도구별 floating 하위 메뉴(텍스트 옵션 바·도구 우클릭 미니패널)도 이 하단 툴바 기준 버튼 **위**로 띄운다(v1.34.0, 미니패널은 2026-07-31 추가). 팝업 본체 배경은 공유 스타일시트의 `QWidget{background:_BG}`가 strip을 덮지 않게 `_apply_active_style`에서 `QWidget#previewroot{background:transparent}`로 되돌린다(자식 chrome·뷰 배경은 불투명 유지). 툴바보다 좁은 이미지에선 편집 시 창 '폭만' 오른쪽으로 늘려 잘림 방지(세로·이동 없음). `show_preview`는 strip 높이만큼 창을 위로 올려 이미지가 의도 위치에 오게 보정. **이력**: 처음엔 토글 때 창을 키워 공간을 만들었으나(리사이즈+이동) translucent 프레임리스 창에서 잔상 발생 → "토글 시 지오메트리 불변"만이 해법이라 strip 상시 예약 방식으로 전환. 한계: 투명 strip은 클릭통과가 아니라 이미지 위 ~63px 클릭이 뒤 앱으로 안 넘어감. 완료 시 `annotated_copy_requested(bytes)`(→ main `_on_annotation_copy`: 클립보드+히스토리 저장 — **백그라운드 스레드**(2026-07-31) — 썸네일 생성·DIB 변환·DB 커밋을 UI 스레드에서 동기 실행하면 큰 이미지에서 최대 5초간 편집기가 멈췄던 문제를 GIF 인코딩 워커와 동일한 패턴(`threading.Thread` + 완료 시그널로 메인에 복귀)으로 해결) / `export_file_requested(bytes)`(→ main `_on_annotation_export`: PNG 파일 저장) emit.
- **`image_annotator.py`** — 이미지 주석 편집기(CleanShot/Snipaste 스타일). `ImagePreviewPopup`이 상속하는 `_EditorMixin`(도구·색·두께·undo·스포이드) + `_AnnotatorView`(QGraphicsView, 그리기 인터랙션) + 도형 아이템(`_RectItem`/`_EllipseItem`/`_LineItem`/`_PathItem`/`_ArrowItem`/`_BadgeItem`/`_TextItem`, 전부 `_HandleResizeMixin`)로 구성. **도구**(v1.33.0 순서·단축키 개편): 선택(Alt+1)·네모(Alt+2)·텍스트(Alt+3)·화살표(Alt+4)·원(Alt+5)·선(Alt+6)·펜(Alt+7)·번호(Alt+8) — `_TOOLS`/`_SHORTCUTS`(`Key_1`~`Key_8`+`AltModifier`), 옛 단일문자(V/R/E…)는 폐기. `QGraphicsScene` 기반이라 줌하면 주석이 이미지와 함께 스케일됨. **그리기 규칙**: 빈 영역 드래그로 생성(시작점→놓은 점 이동량<4px면 클릭으로 보고 폐기+선택 해제), 그린 직후 자동 선택(펜은 제외 — 연속 그리기). 펜은 기존 주석 위에서도 항상 그림(펜 선의 선택·이동은 펜이 아닌 다른 도구로). **펜 클릭 영역은 '획 위'만**(v1.38.0 — `_PathItem._base_shape`가 획을 스트로크한 밴드만 반환): Qt 기본 `QGraphicsPathItem.shape()`는 스트로크에 `addPath(원본 패스)`를 더해 닫힌 획의 안쪽 면까지 클릭 영역에 포함하는데, 그러면 도형을 빙 둘러 그린 펜 루프가 안쪽 빈 공간의 클릭을 통째로 가로채 안쪽 도형이 선택되지 않는다 → 밴드만 반환해 루프 안 도형이 정상 선택되게 함. Shift=정사각/정원/45° 스냅. **편집 단축키**: 화살표키로 선택 항목 이동(기본 10px·Shift/Ctrl=1px), Ctrl+A 전체 선택, Ctrl+C/V 주석 내부 복제(템플릿 clone, +12px cascade), Ctrl+Z undo(빈 텍스트 등 무의미 항목은 건너뜀). 휠(가운데)클릭 드래그=창 이동(`_win_drag_*` 재사용, 편집/뷰어 공용). **크기조절·회전 핸들**(v1.19.0): 선택 시 **우하단 파란 사각=크기조절**(네모·원은 좌상단 고정+가로/세로 독립 자유형, `_uses_freeform_resize()`가 False인 텍스트·번호는 기존대로 균일 스케일 — 2026-07-31 자유형 리사이즈 도입), **좌상단 코랄 원=회전**(중심 피벗은 그대로, 핸들 위치만 상단 중앙→좌상단 코너 안쪽으로 이동해 크기조절 점과 안 겹치게 함 — 2026-07-31, Shift=15° 스냅) — 둘 다 **선택돼 있으면 어떤 도구에서든 표시**(v1.38.0 — `_handle_active`/`_endpoint_active`/`_bend_active` 모두 도구 제한을 없애고 `isSelected()`만 확인, 텍스트 편집 중 예외. 도구 전환 없이 방금 그린 도형을 바로 이동·회전·크기조절 가능. 선택 도구는 러버밴드 다중선택을 계속 담당). 핸들 클릭 배달은 기존 '빈 영역 아님→super 배달' 경로(핸들이 shape에 포함돼 `_is_empty_area`가 False)를 그대로 타 그리기 도구에서도 동작. 회전은 `setTransformOriginPoint(center)`+`setRotation()`, clone(`_copy_common_to`)이 회전·피벗 복사, 평탄화는 씬 transform 그대로 렌더라 회전 반영. **경계 분리**: `_content_rect()`=타이트 경계(선택박스·핸들·텍스트 배경 기준), `boundingRect()`=`_content_rect ∪ 회전 핸들 영역`(상시 예약 → 선택 해제 시 핸들 잔상 방지, 얇은 도형은 좌우로도 핸들이 삐져나오므로 위뿐 아니라 전방향 합집합). boundingRect 여유분이 scale 의존이라 크기조절 mouseMove에서 `prepareGeometryChange()`로 갱신. arrow/badge는 기본 shape가 boundingRect 기반이라 `_base_shape()`를 content로 override(여유분이 클릭영역에 새는 것 방지). **핸들 크기 가변**(`_handle_px`): 도형·선·화살표는 획 두께에 비례(`_HANDLE_STROKE_FRAC=1.4`), 텍스트·번호는(획이 없어) 주석 작은 변에 비례(`_HANDLE_FRAC=0.22`) — 둘 다 씬 단위 `[_HANDLE_MIN, _HANDLE_MAX]`(2026-07-31 `[5,12]`→`[8,18]`로 확대, 사용자 피드백) 클램프 — 작은 주석에서 핸들이 거대해 보이던 문제 해결(item scale로 축소해도 표시 크기 기준이라 함께 작아짐). **핸들 잡기 영역(v1.38.0)**: 그려지는 점은 위 크기 그대로 두고 *잡기* 판정만 화면 고정 24px 지름(`_HIT_MIN_PX`)으로 분리(`_hit_pad_local`→`_inflate_to_hit`가 시각 rect를 그만큼 부풀림) — 얇은 화살표의 bend·끝점 점이 화면상 5~12px라 정확히 조준해야 손가락 커서가 되던 문제 해결(Figma식 '점은 작게, 판정은 넓게'). hover(`_bend_handle_at`·`_selected_endpoint_item`은 `selectedItems()` 직접 순회)·press·shape·boundingRect가 모두 이 확대 rect를 사용(boundingRect도 함께 넓혀야 확대 shape가 Qt 컬링에 안 걸림). 한계: boundingRect는 줌 변경 시 자동 재계산 안 돼 선택 후 크게 축소하면 확대 여유가 24px에 못 미칠 수 있음(확대만 하므로 기존보다 나쁘진 않음). `_HandleResizeMixin`의 공유 메커니즘이라 크기조절(우하단)·회전(좌상단) 핸들도 처음부터 같은 24px 잡기 판정을 쓴다(2026-07-31 — 시각 크기 그대로라 좁았던 호버·클릭 영역을 끝점 핸들과 동일하게 통일). 회전 원 지름=사각 변(반지름=`_handle_px/2`), 줄기는 "중심 거리"가 아니라 "도형~원 빈 간격(`_ROT_GAP=14`)" 고정이라 핸들 크기와 무관하게 보이는 줄기 길이 일정. **텍스트**: 작성 후 텍스트 도구 유지(연속 배치), Ctrl+Enter로 마무리, 빈 텍스트는 focusOut 시 정리. 새 텍스트 시작 시 다른 선택 해제, 크기 조절은 편집 중 텍스트가 있으면 그것만(`_font_size_targets`). T 활성 시 T 버튼 **위**에 **수평 옵션 바**(`_text_opts_bar` — 배경 스와치 투명/반투명검정/흰/회/검 직접 선택). **배경색은 작성 중에도 즉시 적용**(v1.34.0) — `_set_text_bg`가 `_font_size_targets`(작성 중 텍스트 우선, 없으면 선택 텍스트)를 대상으로 삼음(옛날엔 선택 상태에서만 됐음). 반투명 스와치 아이콘은 **체커보드 바탕**에 색을 얹어(v1.34.0) 불투명 회색과 헷갈리지 않게 함(`_bg_swatch_icon`, `bg.alpha()<255`). **번호(badge)**: 붙여넣기 시 새 번호 부여. **크기 스테퍼 폐기(v1.34.0)** — 글자·번호 크기 스테퍼(`_SizeStepper`)를 제거하고 크기는 주석 위 휠(`adjust_item_property`)로만 조절한다. **도구별 우클릭 미니패널(색·크기, 2026-07-31)**: 옛 전역 무지개 색 버튼 1개(모든 도구가 색 하나를 공유)와 화살표 전용 floating 방향 토글 버튼을 폐기하고, 네모·원·선·펜·화살표·텍스트·번호 각 아이콘을 **우클릭**하면 그 도구 전용 미니패널(`_ToolSettingsPopup`)이 뜬다 — 프리셋 7색+스포이드, 크기 슬라이더(두께/글자/번호 크기), 화살표는 추가로 **머리 크기 슬라이더**(두께에서 자동계산되던 옛 방식 폐기, 독립 조절·기본값 상향)와 **방향 토글**까지 담는다. 색·크기·화살표 머리·방향은 도구별로 완전히 독립(`_EditorMixin._tool_defaults` — 클래스 변수 dict, 도구 하나를 바꿔도 다른 도구는 그대로)이며 DB 설정 `annot_tool_defaults`(JSON 하나)로 영속화(재시작 후에도 유지, 옛 평면 키 3종 `annot_last_width`/`_font_size`/`_badge_size`는 `_ORPHAN_KEYS`로 정리). 각 도구 아이콘 자체도 그 도구의 색으로 칠해져(`_refresh_tool_icons`) 독립 상태를 시각적으로 드러낸다. ⚠ **미니패널은 `Qt.WindowType.Tool`+`WA_ShowWithoutActivating`**(paste_hud.py 비활성 창과 동일 패턴)이지 `Qt.Popup`이 아니다 — Popup으로 만들면 열려 있는 동안 마우스 입력(휠 포함)을 그랩해 Shift+휠 두께조절이 캔버스에 전혀 안 먹는 회귀가 있었다(2026-07-31 발견·수정). 대신 바깥 클릭 자동닫힘을 잃으므로 도구 전환(`set_tool`)·캔버스 클릭(`_AnnotatorView.mousePressEvent`) 시 `_hide_tool_popups()`가 명시적으로 닫는다(같은 아이콘 재우클릭 토글은 유지). **하위 옵션 바 배치**: 텍스트 옵션 바·도구 우클릭 미니패널 등 도구별 floating 하위 메뉴는 툴바가 창 **하단**(`image_preview._layout_chrome`)이라 버튼 **위**로 띄운다(아래로 두면 창 밖으로 잘림). **스포이드**: 화면 픽셀 색 따오기(`_ColorLoupe` 미리보기, ctypes GetPixel — 도구별 미니패널에서 진입). **화살표 곡선·테두리 연결점**(v1.30.0): `_ArrowItem`은 **3차 베지어**(제어점 2개 `_ctrl1`/`_ctrl2`, `None,None`=직선) — 선택 시 곡선 t=1/3·2/3 지점에 초록 조절 핸들 2개(`_bend_handle_rect(which)`), 드래그하면 해당 제어점을 닫힌해로 역산(`_solve_ctrl` — B(1/3)·B(2/3) 계수식)해 곡선이 커서를 정확히 지나감. A 도구 직선은 그대로 시작, 핸들 밀면 곡선화하고 두 제어점이 모두 직선(1/3·2/3) 위로 오면 직선 복귀. 머리 방향(head_at_end) 양쪽 곡선 트림(De Casteljau [0,te] 분할, tip 접선 `3·|P3−C2|`), `_content_rect`는 네 점(p1·c1·c2·p2) 볼록껍질. **테두리 스냅**(v1.33.0 — 옛 '변 중앙 4점 연결점 + 도구 무관 화살표 생성'을 폐기): **화살표 도구일 때만** 도형 테두리 어디서든(4방향 제한 없음) 최근접점에 스냅한다. 화살표 시작이 네모/원 테두리 근처(`_BORDER_SNAP_PX`=14 뷰px)면 그 최근접점으로 스냅하고 바깥 법선을 이탈 접선으로 삼아 **자동 S자**, 드래그 tip이 테두리 근처면 최근접점+안쪽 법선으로 수직 도착(양끝 다 자유면 직선). 최근접점·법선은 모듈 함수 `_nearest_border`(네모=변 클램프 `_rect_nearest` / 원=파라미터각 뉴턴 반복 `_ellipse_nearest`, 회전·스케일은 아이템 `mapFromScene`/`mapToScene` 왕복). 유휴 시 커서 근처 테두리 최근접점에 파란 마커를 예고하고 그리는 중엔 스냅된 시작·tip에 마커(`_AnnotatorView.drawForeground` + `_draw_snap_marker`). 단 커서가 선택된 화살표의 끝점/곡선 핸들 위면(이동·재스냅 모드, 손가락 커서) 이 예고 마커를 숨긴다(v1.38.0 — `_update_snap_preview`가 `_selected_endpoint_item`/`_bend_handle_at`를 확인. 끝점이 테두리에 붙어 생성-스냅점과 겹칠 때 큰 파란 점이 손가락 커서와 함께 남던 문제 방지). 발동은 `_border_snap_at`(가장 가까운 도형 테두리 선택), 그리기 갱신은 `_update_arrow_draw`. 도형+화살표는 여전히 독립 아이템(완전 연결 아님 — 함께 옮기려면 둘 다 선택). 도형+화살표를 함께 옮기려면 둘 다 선택해 이동(완전 연결 아님 — 화살표는 독립 아이템). **끝점 재스냅(v1.34.0)**: 이미 그린 화살표의 끝점 핸들을 도형 테두리에서 뗐다가 다시 근처로 가져가면 재스냅된다 — `_HandleResizeMixin._move_endpoint_with_snap`(화살표가 override)이 드래그 중 뷰의 `_border_snap_at`를 재조회하고, 스냅되면 `_recompute_snap_curve`가 생성 때(`_update_arrow_draw`)와 같은 공식(바깥 법선×`k=clamp(dist·0.5,30,200)`)으로 **양쪽 제어점을 재계산해 S자(수직 도착/이탈)까지 복원**한다(반대 끝이 여전히 테두리 위인지 `_endpoint_border_normal`로 재조회). 테두리 밖이면 끝점만 이동(수동으로 구부린 곡선은 delta 추종 유지). Shift(각도 스냅)와 상호배타, 선(line)은 `_connects_to_border=False`라 제외. **편집 중 텍스트 hover 커서(v1.34.0)**: `_editing_text_hover`가 편집 중(`textInteractionFlags≠NoTextInteraction`) 텍스트 위에서 **내부=I빔(캐럿)·테두리 band(화면 8px)=이동(`SizeAll`)**으로 분기(`_update_hover_cursor`) — 옛날엔 주석 위 전체가 이동 커서였음. 작은 텍스트박스는 전부 I빔(편집 우선), 편집 아닌(선택만) 텍스트는 예전처럼 전체 이동 커서. 씬→PNG 평탄화는 `flatten_scene_to_png(scene)`. main과는 시그널만 주고받고 실제 클립보드 복사/파일 저장은 main이 처리.
- **`text_preview.py`** — 텍스트 미리보기 팝업(평문 전용). 다중 창 동시 표시 지원(`open_new(item, panel_geom, editable=True)` — `ClipboardItem` 전체를 받아 우클릭 메뉴의 복사·수정에 활용). `_instances` 클래스 목록이 닫힐 때까지 참조를 유지(close 시 정리)하므로 별도 보관 없이 안전.
  - ⚠ **v1.6x에서 `markdown=True` 경로(옛 우클릭 "AI에게 질문" 답변창 전용) 전체를 삭제했다**(2026-07-29) — 그 기능이 이미 제거돼(main.py의 `_start_ai_worker`/`_start_compare_query` 등 호출부 소멸) 실행 경로가 없던 죽은 코드였다. 함께 제거: 턴 탭(Q1/Q2/…) 멀티턴 UI, 마크다운 렌더링(`QTextEdit`+`setMarkdown()`), 형광펜↔선택 모드 토글, 우상단 오버레이 버튼(닫기·최대화·최소화·미니핸들), 수동 리사이즈 그립, 답변 이미지로 복사. `center`/`initial_turn`/`model_title`/`pending_question`/`place_rect` 파라미터도 함께 사라졌다. 옛 구현은 git 이력(이 파일의 `d238c86` 이전 버전) 참고.
  - **표시 위젯**: `QPlainTextEdit` (QLabel+QScrollArea 아님). `setWordWrapMode(WrapAtWordBoundaryOrAnywhere)`로 공백 없는 긴 URL/해시도 문자 단위 wrap. QLabel은 word-boundary 없는 토큰을 절대 잘라주지 않아 폐기.
  - **"한 번에 다 보임" 정책**: 양쪽 스크롤바 영구 차단(`ScrollBarAlwaysOff`) + editor `FocusPolicy.NoFocus` — QPlainTextEdit 내부 가짜 vScroll(`vScrollMax≥1`)이 키보드(스페이스/PageDown)로 노출돼 빈 영역이 보이던 문제 차단. width는 `PREVIEW_INITIAL_MAX_W * scale` (zoom과 비례 확장, 화면 너비 cap), height는 화면 한계까지 자유 확장하여 모든 줄 표시.
  - **`LineWrapMode` 동적 전환**: 자연 너비가 popup width cap 안에 들어오면 `NoWrap` 강제 (QPlainTextEdit의 viewport에 내부 padding이 있어 textWidth를 맞춰도 sub-pixel 차이로 wrap이 새는 경우 발생 — 휠 줌마다 1↔2줄 깜빡임의 원인). 초과 시에만 `WidgetWidth` wrap.
  - **크기 계산**: 자연 너비/높이 측정에 독립 `QTextDocument` 사용 — QPlainTextEdit의 자체 document는 lazy layout이라 `setTextWidth` 직후 `size()`가 갱신 안 됨. `math.ceil(idealWidth())`로 sub-pixel 부족분 보정.
  - **전체 창 드래그**: 텍스트 부분 선택 미지원(`NoTextInteraction`). 부분 텍스트가 필요하면 우클릭 메뉴 `수정`으로 편집 다이얼로그에서 자연스럽게 선택. viewport + popup 본체 양쪽 모두에서 left-drag 이동 처리.
  - **우클릭 메뉴**: `전체 복사` / `수정` / `닫기` (패널 메뉴와 동일 명칭). 시그널 `copy_requested(ClipboardItem)` → main의 `_on_copy_item`, `edit_requested(item_id)` → main의 `_on_preview_edit_request`(`EditItemDialog` 띄우고 `_on_edit_item`으로 위임). QPlainTextEdit 기본 우클릭 메뉴는 `setContextMenuPolicy(NoContextMenu)`로 차단.
  - **활성/비활성 테두리**: 이미지 미리보기와 동일 정책(활성=코랄, 비활성=중립 회색). 프레임리스 최상위 위젯에 건 테두리가 자식에 가려지는 문제를 피하기 위해 내부 `popup_container`에 적용.
  - **`_clamp_to_screen(avail)`**: resize 후 popup이 화면 밖으로 나가면 안쪽으로 끌어들임.
- **`toast.py`** — 토스트 알림. `_ToastStack` 싱글턴이 활성 토스트를 코너 기준 위로 스택(최신=맨 아래)하고 닫힐 때 재정렬, 최대 5개 동시 표시(초과 시 가장 오래된 것 즉시 제거). **스택 모니터**: 스택 토스트(시작 알림·복사 알림 등 수동적 알림)는 항상 **주 모니터** 우하단에 고정(`_ToastStack._screen = primaryScreen`) — 예측 가능한 위치(커서를 따라 모니터를 옮겨다니지 않음). 능동적으로 기다리는 AI·OCR 진행/결과는 스택이 아니라 anchor/center 모드로 활성 모니터 중앙에 뜬다(역할 분리). (이력: 한때 커서 모니터 고정이었으나 시작·복사 알림은 주 모니터가 더 예측 가능해 되돌림.) `ToastNotification(message, icon, badge, badge_position, image_path, image_bytes, anchor, center)` — `badge_position`은 `"leading"`(아이콘과 본문 사이) 또는 `"trailing"`(본문 뒤, 기본). `image_path`가 주어지면 아이콘과 본문 사이에 그 PNG 파일의 썸네일(최대 `_THUMB_MAX`=96px, `QPixmap(path)` 직접 로드·로드 실패 시 조용히 생략)을 삽입 — 이미지→경로 붙여넣기 시 "의도한 이미지가 맞나" 시각 확인용. **`image_bytes`**(v1.53.0)는 디스크 경로 없이 **메모리 바이트**에서 같은 썸네일을 그린다 — 복사 알림처럼 파일이 없는 이미지 항목용. `icon`이 빈 문자열이면 아이콘 라벨 자체를 생략(썸네일이 카테고리 구분을 대신). `show_copy_toast(item, queue_count)`는 누적 큐 카운트를 `Q{n}` 형태 badge로 본문 앞(`leading`)에 배치해 2초간 표시하고, **이미지 항목이면 `preview_text`(`[이미지]`) 대신 썸네일을 띄운다**(v1.53.0 — 패널·경로 붙여넣기 피드백과 시각적 일관). ⚠ **소스는 `item.thumbnail`이 아니라 원본 `item.image_data`를 우선 쓴다** — thumbnail은 패널용 80×60 축소본이라 96px로 **확대**되며 뭉개진다(토스트가 어차피 96px로 줄이므로 원본을 주는 쪽이 선명하다. 캡처 토스트가 `image_path` 원본으로 선명한 것과 같은 이유). 원본이 raw CF_DIB면 QPixmap이 못 여므로 `clipboard_monitor._dib_to_bmp`로 BMP 파일 헤더를 조립해 로드하고(핀 항목은 thumbnail이 없어 이 변환이 없으면 아이콘만 떴다 — v1.54.0), 그래도 안 되면 thumbnail로 폴백. **아이콘(`📋`)은 2026-08-03부터 완전히 없앴다** — `Q{n}` 배지가 이미 "복사됨" 카테고리를 나타내 아이콘과 중복이었다(사용자 피드백: "텍스트 복사하면 맨 앞에 아이콘 나오는데 굳이 없어도 될듯"). 이미지 항목은 썸네일이 그 자리를 대신하고, 텍스트 항목은 배지+본문만으로 충분하다 — 렌더 실패한 이미지도 이제 아이콘 없이 배지+본문만 뜬다(옛 "아이콘도 썸네일도 없는 빈 토스트 방지"용 `📋` 폴백은 폐기). 같은 규칙으로 핀 툴바 복사(`_on_annotation_copy`)·AI 답변 이미지 복사(`_on_answer_image_copy`) 토스트도 썸네일을 싣고 `icon=""`을 쓴다. `reserve_bottom(px)`(HUD 등 우하단 위젯을 위해 스택 하단 여백 확보). 시작 알림·복사 알림 등 일반 토스트가 이 스택을 공유한다. **커서 앵커 모드**(`anchor=QPoint`): 우하단 스택 대신 그 지점을 기준으로 배치하고 `WindowTransparentForInput`로 클릭을 아래 앱에 통과시킨다(작업 방해 0). `center=True`면 앵커 옆(+16px·경계 반전) 대신 **앵커가 속한 모니터 정중앙**에 배치(`_place_anchored`). OCR·AI 진행/결과 칩이 이 모드를 쓴다(main `_start_cursor_progress`). **지속형 토스트**(`duration_ms=0`): 자동 fade-out 없이 호출자가 `dismiss()`로 닫는 모드 + `set_message(text)`로 본문 갱신(앵커 모드면 폭 변화 시 재중심) — AI 질의·OCR처럼 끝나는 시점을 미리 알 수 없는 작업의 진행 표시용. **OCR 토스트**: icon `🔤`로 통일, 본문에서 `OCR:` prefix는 제거(아이콘이 카테고리 구분을 담당). 모든 OCR 진입점(진행 중·결과·각종 에러)이 동일 아이콘을 사용해야 일관됨.
- **`paste_hud.py`** — 순차 붙여넣기 진행 HUD. 우하단 비활성 창(`WA_ShowWithoutActivating` — 포커스 미탈취), 큐 항목 목록을 `✓`(완료·흐림)·`▶`(다음·강조)·`·`(대기)로 표시하고 헤더에 `순차 붙여넣기 n/total`. 단일 인스턴스 재사용 — `show_progress(items, pointer)`로 표시·갱신, `finish()`로 1.2초 후 fade-out. 헤더 우측 **✕ 취소 버튼**(`_cancel_btn`, hover 시 코랄)은 `cancel_requested` 시그널을 emit → main `_on_cancel_paste_queue`가 `_clear_queue_ui()`(큐 비우기 + 표시 초기화) + `dismiss()`로 처리. **`dismiss()`**는 사용자 명시 취소용으로 `finish()`의 1.2초 linger 없이 즉시 fade-out(표시 중이 아니면 무시). 큐 10개 초과 시 "외 N개"로 축약. 표시 중 `toast.reserve_bottom()`을 호출해 복사 토스트가 HUD 위로 쌓이게 한다.
- **`stt_indicator.py`** — **음성 입력 녹음 중 이퀄라이저 pill**(2026-08-02, Wispr Flow와 나란히 써본 사용자 피드백 반영 — 상시 플로팅 마이크 pill+호버는 범위가 커 제외하고 "녹음 중에만 뜨는 이퀄라이저"만 착수). `SttIndicator`: 커서 옆에 뜨는 96×40 pill, 막대 5개가 `stt_engine.Recorder.get_level()`(RMS)에 반응(시간 기반 사인파로 막대마다 다른 위상 지터 + 50:50 스무딩, RMS가 대체로 낮은 값이라 3배 증폭). `WindowTransparentForInput`은 안 씀(클릭을 받아야 하므로 — 클릭하면 `stop_clicked` emit, 단축키를 뗀 것과 동일 효과). 배치는 `toast.py`의 커서 앵커 규칙 재사용. main의 `_on_stt_start`가 `winsound.MessageBeep()`(stdlib, 새 의존성 0)로 시작음 재생 + pill 생성 + 50ms 폴링 타이머로 음량 반영.
- **`tray.py`** — 시스템 트레이. 좌클릭 시 `panel_toggle_requested` 시그널 emit → main이 패널 토글. 우클릭 메뉴: `📋 패널 열기` / `⚙️ 설정` / `❌ 종료`. (v1.6x에서 `🕘 AI 기록` 항목·`ai_history_requested` 시그널 제거 — AI 기록 기능 자체가 사라짐.)
- **`settings_dialog.py`** — 단축키 커스터마이징(패널 토글, OCR, 이미지→경로 — `KEY_IMAGE_TO_PATH_HOTKEY` = `hotkey_image_to_path`, 기본 `ctrl+shift+p`, 순차 경로 붙여넣기 — `KEY_SEQ_IMAGE_TO_PATH_HOTKEY` = `hotkey_seq_image_to_path`, 기본 `ctrl+shift+[` — `_qt_key_to_name`에 `Key_BracketLeft`/`Key_BraceLeft`→`[` 캡처 매핑 추가, 화면 핀 — `KEY_PIN_IMAGE_HOTKEY` = `hotkey_pin_image`, 기본 `alt+f3`, 순차 핀 — `KEY_SEQ_PIN_HOTKEY` = `hotkey_seq_pin`, 기본 `alt+shift+f3`, 영역 캡처 — `KEY_CAPTURE_HOTKEY` = `hotkey_capture`, 기본 `alt+f2`(바로 아래 체크박스 `PrintScreen 키로도 실행` — `KEY_CAPTURE_USE_PRINTSCREEN` = `capture_use_printscreen`, 기본 꺼짐, 2026-08-12 도입 — 켜면 PrintScreen 단독 입력이 영역 캡처와 동일하게 발동), GIF 녹화 — `KEY_RECORD_GIF_HOTKEY` = `hotkey_record_gif`, 기본 `ctrl+shift+g`, 영상(MP4) 녹화 — `KEY_RECORD_VIDEO_HOTKEY` = `hotkey_record_video`, 기본 `ctrl+shift+r`(2026-08-03 도입, GIF 녹화와 같은 구조 — `video_recorder.py` 참고), AI 자유질문 — `KEY_ASK_AI_HOTKEY` = `hotkey_ask_ai`, 기본 `alt+1`(2026-08-03부터, 예전엔 `` alt+` ``) — `HotkeyEdit._qt_key_to_name`에 백틱(`Key_QuoteLeft`/`Key_AsciiTilde`→`` ` ``) 캡처 매핑은 사용자가 다시 그 조합으로 재녹화할 수 있도록 남겨둠, Gemini(캡처) — `KEY_CAPTURE_ASK_HOTKEY` = `hotkey_capture_ask`, 기본 `alt+2`(2026-08-03부터, 예전엔 `` win+` ``였으나 Windows Terminal 퀘이크 모드 기본 단축키와 충돌해 변경 — 아래 `paste_interceptor.py` 항목 참고)), 캡처 저장 폴더(`KEY_CAPTURE_FOLDER` = `capture_save_folder`, QLineEdit + 찾아보기 `QFileDialog`), 히스토리 제한, 순차 큐 자동 초기화 시간(`KEY_QUEUE_IDLE_RESET` = `queue_idle_reset_sec`, QSpinBox 1~3600초·기본 10초), 자동 시작, 복사 알림 on/off(`notify_on_copy`), GIF/영상 녹화 시 커서 표시 on/off(`KEY_GIF_SHOW_CURSOR` = `gif_show_cursor`, 기본 켜짐 — GIF·영상 녹화 공통, 2026-08-03), GIF 녹화 fps(`KEY_GIF_FPS` = `gif_fps`, 1~30·기본 12)·GIF 최대 길이(`KEY_GIF_MAX_SECONDS` = `gif_max_seconds`, 5~60초·기본 15초 — GIF는 프레임을 전부 메모리에 모았다 인코딩하므로 상한을 좁게 잡음)·영상 녹화 fps(`KEY_VIDEO_FPS` = `video_fps`, 1~30·기본 15)·영상 최대 길이(`KEY_VIDEO_MAX_SECONDS` = `video_max_seconds`, 10~3600초·기본 600초 — 영상은 디스크에 즉시 흘려써 메모리 제약이 없어 GIF보다 넉넉하게 열어둠, 2026-08-04) 설정. ⚠ 이 4개 키는 `main.py`의 `_start_gif_recording`/`_start_video_recording`가 이미 `db.get_setting()`으로 읽고 있었지만(영상 쪽은 기존에 `fps=15, max_seconds=600`으로 하드코딩돼 있던 것을 이번에 DB 읽기로 전환), 설정창을 다시 열 때 값을 채우는 `main._open_settings()`의 `current` dict에 처음엔 누락돼 있었다 — 저장은 되는데 재오픈 시 항상 기본값으로 보이는 버그였고, 다른 PC 세션이 추천한 후속 프롬프트로 대조 검토하다 발견해 그 자리에서 수정(4개 키 전부 `current` dict에 추가). **`GIF/녹화 설정` 그룹 분리**(2026-08-04): 위 5개 항목(커서 표시+GIF fps/최대길이+영상 fps/최대길이)은 `일반 설정`과 별개 `QGroupBox("GIF/녹화 설정")`로 분리돼 있다 — 처음엔 두 그룹 경계를 코랄 색깔선(`_gen_sep_accent`)으로 표시했으나 카드 하나 크기에 비해 색이 과하게 튀어(사용자 피드백) 그룹박스 분리로 대체했다. `일반 설정` 내부는 `Windows 시작 시 자동 실행`·`복사 시 우하단 알림 표시` 체크박스가 맨 위, 구분선, 그 아래 히스토리 최대 개수·순차 큐 자동 초기화·캡처 저장 폴더 순. **탭 구성/배치(v1.28.0 그룹 개편 · v1.56.0 탭 분리 → 2026-07-29 재통합)**: 설정이 늘며 단일 스크롤이 화면을 넘어 `QTabWidget`으로 나눴다. ⚠ **현재는 탭 2개(`일반`/`AI`)뿐이다** — v1.56.0에서 한때 `단축키`/`AI`/`일반` 3개였으나, "일반"은 일반 설정 + 단축키를 한 탭에 통합한 것(2026-07-29 요청)이라 이후로는 `일반` 탭이 `기본 단축키 (고정)`+`기능 단축키 (변경 가능)`까지 함께 담는다(코드: `_make_tab("일반")`/`_make_tab("AI")` 둘뿐, `tab_general.insertWidget(0, general_group)`으로 일반 설정을 단축키 그룹들 위에 삽입). 이 문단 나머지의 `단축키` 탭 언급은 실제로는 `일반` 탭을 가리킨다(어두운 탭 바 + 활성 탭만 코랄 밑줄, 버튼 바는 탭 밖에 둬 항상 노출, `_finalize_size`가 창 높이를 '가장 큰 탭 페이지'에 맞춰 고정해 스크롤이 사실상 사라짐 — `_tab_pages` 목록에 각 탭 콘텐츠 위젯을 모아 최대치 산정, `_make_tab(title)`이 스크롤 래핑까지 함께 처리). **`일반` 탭의 단축키 부분** = `기본 단축키 (고정)`(복사/붙여넣기/순차붙여넣기) + `기능 단축키 (변경 가능)`(패널·경로 붙여넣기류·영역 캡처류 3개 하위 묶음만 — AI 호출류(Gemini 호출·Gemini(캡처)·OCR·음성 입력(STT))는 2026-08-04에 `AI` 탭으로 옮겼다, 사용자 요청); **`AI` 탭** = `OCR·STT 연동 (API 프로필)` + `빠른 검색 (Alt+` 자유질문 목적지)`(2026-07-29 개편 — 이전 "AI 팔레트 타겟"에서 개명, 그룹 설명 문구는 삭제. `_PaletteSiteRow` 행 리스트: 드래그 손잡이·번호·라벨·keyword·URL·삭제. **종류 선택 드롭다운은 없다** — Google AI는 `ensure_google_ai()`가 보장하는 고정 행(삭제 버튼 없음·"Google AI · 고정" 배지·URL 칸 없음, 순서만 드래그로 이동), 사용자가 추가하는 나머지는 전부 URL 종류뿐이라 고를 게 없다(`_on_add_palette_site`가 항상 URL로 생성). 순서=질문창 Tab 순환 순서(예전엔 Alt+숫자 즉시전송 단축키와도 대응했으나 그 단축키는 2026-08-03에 제거 — 아래 `ai_query.py` 항목 참고). `KEY_AI_PALETTE_SITES`=`ai_palette_sites`로 JSON 저장 — 데이터 모양은 `ai_palette.py` 참고. 각 행은 `_LINE` 색 테두리(투명 배경)로 서로 구분한다 — ⚠ **`QWidget` 서브클래스는 `WA_StyledBackground`를 명시적으로 켜지 않으면 setStyleSheet의 배경·테두리가 그려지지 않는다**(순수 `QWidget()` 인스턴스는 예외적으로 자동 적용됨 — 오프스크린 렌더로 실측·확인한 Qt 함정, 2026-07-29). URL 칸이 비어 있으면 `"URL 입력 (예: https://example.com/search?q={q})"` placeholder로 형식을 안내한다(옛 헤더 줄·그룹 설명문에 있던 사용법·예시는 실사용 자리인 이 placeholder와 `ai_query.py` 질문창 입력란으로 옮겼다). **`AI` 탭 맨 아래에는 `AI 단축키 (변경 가능)`**(2026-08-04 도입, 사용자 요청) 그룹이 있다 — Gemini 호출·Gemini(캡처)·OCR·음성 입력(STT) 4개로, 원래 「일반」 탭 `기능 단축키`의 ④⑤ 하위 묶음이었던 것을 그대로 옮겼다(위젯·툴팁·`listening_changed` 연결 등 로직은 불변, 배치만 이동). 회색 테두리는 그룹박스 전역 스타일이라 자동으로 따라오고, OCR↔STT 사이 구분선(`_hk_sep()`)도 그대로 유지했다. 배치 순서는 `OCR·STT 연동` → `빠른 검색` → `AI 단축키`(맨 아래 — 크리덴셜·팔레트가 우선이고 단축키는 참고용); **`일반` 탭** = 히스토리 제한·큐 자동초기화·자동시작·복사알림. ⚠ **v1.6x에서 `구글 드라이브 (선택)` 그룹을 통째로 제거했다**(연결/연결 해제 버튼·`KEY_GDRIVE_*`·`gdrive.authorize()` 워커 호출 전부 삭제) — PasteFlow 앱 자체가 더 이상 드라이브를 검색하지 않는다(우클릭 "AI에게 질문"과 함께 사라짐. `gdrive.py` 모듈은 `webchat/`이 여전히 쓰므로 남아 있다 — `gdrive.py` 항목 참고). **`AI` 탭 이름이 `OCR 연동`이 아니라 `AI`인 이유**: 팔레트 타겟 그룹도 이 탭에 있어(질문을 Google AI 등으로 보내는 것도 "AI"라 부를 만한 기능), OCR 전용으로 좁혀 부르면 팔레트 그룹과 안 맞는다. **OCR 전용 그룹은 제거**(과거형, v1.28.0 결정 — 지금도 유효) — OCR은 별도 엔진·인식 언어 선택 없이 항상 AI(Gemini/Mindlogic) API로 처리하므로(WinRT 제거) OCR 단축키를 `AI OCR` 이름으로 기능 단축키 맨 아래로 옮기고, 엔진/언어 콤보(`_ocr_engine_combo`/`_ocr_lang_combo`)·`_on_engine_changed`를 삭제했다. `_on_save`는 `KEY_OCR_ENGINE="gemini"` 고정 + Gemini 키/모델/캐시를 항상 저장. 기능 단축키는 **4개 하위 묶음**(①패널 ②경로류 ③영역 캡처·핀·GIF 녹화·영상 녹화 ④Gemini 호출·Gemini(캡처)·OCR)을 얇은 구분선으로 분리, 라벨은 `패널 불러오기`·`경로 붙여넣기`·`영역 캡처`·`영역 캡처 핀`·`GIF 녹화`·`영상 녹화`·`Gemini 호출`·`Gemini(캡처)`·`OCR`로 통일(저장 KEY는 불변, 표시만 — `Gemini 호출`은 2026-07-29에 `AI 호출`에서 개명, 옛 우클릭 "AI에게 질문" 기능을 가리키던 툴팁의 낡은 문장도 함께 제거했다. **2026-08-03 재배치**: 옛 "캡처 후 질문"을 `Gemini 호출` 바로 아래로 옮기고 `Gemini(캡처)`로 개명(같은 Gemini 호출 기능끼리 나란히), 옛 `AI OCR`을 그 아래 → `음성 입력(STT)` 바로 위로 옮기고 `OCR`로 개명(텍스트 인식·음성 인식이 나란히) — 사용자 요청). 그룹 제목은 **코랄**(강조), 단축키 행 라벨엔 `•` 불릿. **단축키 표시 대문자화**: `HotkeyEdit.format_hotkey`가 저장값(소문자 canonical — 파서용)을 표시만 `Ctrl + Shift + P` 형태로 변환(기본 단축키 표기와 통일). **레이아웃**: 전체 콘텐츠를 `QScrollArea`(`_finalize_size`가 창 크기를 콘텐츠+화면에 맞춰 산정·넘으면 스크롤)로 감싸 고정 크기가 콘텐츠를 압박해 word-wrap 라벨 heightForWidth가 진동하던 **드래그 떨림**과 작은 화면 오버플로를 해결. 버튼(취소/저장)은 스크롤 밖. ⚠ **창 크기 조절 가능**(2026-07-29 사용자 요청 — Base URL 등 긴 값이 입력칸에서 잘려도 창을 늘려 볼 수 없었다): `_finalize_size`가 `setFixedSize` 대신 `setMinimumSize`(콘텐츠 기준 최소치) + `resize`(시작 폭을 최소치보다 60px 더 넓게)를 쓴다 — 사용자가 가로·세로를 자유롭게 더 늘릴 수 있고, 최소치 밑으로는 못 줄어든다(오프스크린 렌더로 리사이즈·최소치 클램프 둘 다 실측). ⚠ **연결 테스트 버튼(`_on_test_api`)은 v1.6x부터 OCR 모델 하나만 확인한다** — 우클릭 "AI에게 질문"·비교가 사라져 콤보가 `OCR 모델` 하나뿐이므로(옛 v1.44.0의 "AI 모델(1·2·3)까지 다중 확인"은 그 모델 콤보들과 함께 제거됐다). **크레딧 확인 병합(2026-08-12 사용자 요청)**: 원래 별도 버튼이던 크레딧 확인(`ocr_engine.get_credit_balance`)을 이 버튼에 흡수했다 — 둘 다 "지금 이 키로 뭐가 되는지" 그 자리에서 확인하는 성격이라 한 클릭으로 묶었다(모델조회는 콤보를 채우는 별개 준비 동작이라 그대로 분리 유지). 워커 스레드가 `probe_connection` → `probe_ocr_model` → `get_credit_balance`를 순서대로 돌리고, 단계마다 `_probe_done(run_id, slot, status, detail)` 시그널로 결과를 제 자리에 쓴다: `conn`→상태 줄(`_test_status`), `ocr`→`_ocr_model_probe_status`, `credit`→`_credit_status`(셋 다 콤보 바로 아래, `_stack()`으로 세로 배치). 앞 단계가 실패하면 OCR 모델 테스트는 `skip`("연결이 안 돼 건너뛰었습니다")으로 남겨 원인을 흐리지 않지만, **크레딧 조회는 연결 성패와 무관하게 항상 시도한다**(연결 프로브가 막혀도 크레딧 엔드포인트는 따로 열려 있을 수 있어서). `__end__` slot이 버튼을 복구한다(`finally`라 예외에도 복구). 색은 `_PROBE_STYLE`: `ok`=✓초록 / `weak`=⚠코랄 / `fail`=✗빨강 / `retry`=⏳코랄. ⚠ **`retry`(429·503)를 `fail`과 같은 색으로 칠하지 말 것** — 서버 사정으로 판정 못 한 것을 "나쁜 모델"로 오해하게 만든다. **회차 무효화(`_probe_run_id`)**: 모델을 바꾸면 결과 줄을 지우고(`_on_model_text_changed`) 회차 번호를 올린다 — 뒤늦게 도착한 옛 회차 결과는 `_on_probe_done`이 버린다. 무효화로 워커의 `__end__`도 버려지므로 버튼은 `_on_model_text_changed`가 되살린다. 비용은 연결 1콜 + OCR 1콜(텍스트 전용 모델일 때만 그 모델의 질의가 2콜 — 이미지 실패 → 텍스트 재시도) + 크레딧 조회 1콜(가벼운 GET) — 사용자가 명시적으로 누를 때만. **모델 콤보는 이제 `OCR 모델` 하나뿐이다**(v1.6x — 옛 v1.39.0 OCR/AI 분리 → v1.44.0 여러 모델 비교로 질의 콤보 3개까지 늘었던 `_model_combo`/`_compare_model_a_combo`/`_compare_model_b_combo`와 그 프로브·저장 로직(`KEY_OCR_GEMINI_MODEL_GATEWAY`/`KEY_AI_COMPARE_MODEL_{A,B}`, `_COMPARE_UNUSED`, `_fill_compare_combo`/`_compare_value`/`_init_compare_slots`/`_compare_slot_widgets`)을 전부 제거했다). 저장 키는 `KEY_OCR_MODEL_GATEWAY` 하나. 콤보는 게이트웨이 모델 캐시(`_cached_models()`)로 채운다(`_init_model_slots`). **아무것도 걸러내거나 회색 처리하지 않는다**(v1.42.0 — 옛 🚫/⚠/📝/❓ 상태 아이콘과 `setEnabled(False)` 차단은 `model_matrix.json`과 함께 제거. 낡은 판정이 멀쩡한 모델을 영구히 못 고르게 만들었다). 어느 모델이 되는지는 위 `연결 테스트`가 실호출로 답한다. 캐시가 없는 첫 실행은 **콤보를 비운 채** placeholder `"↻를 눌러 모델 목록을 불러오세요"`를 띄운다(빈 값이 저장돼도 엔진이 기본 모델로 폴백해 안전). **행 순서**: `Base URL` → `API 키`(+`모델조회`) → `OCR 모델`(+`연결 테스트`, 모델 콤보 오른쪽에 나란히 — 예전엔 아래 별도 줄이었다). **모델조회 버튼**(Qt 표준 아이콘 `SP_BrowserReload` + "모델조회" 텍스트 — 아이콘만으로는 뜻이 안 와닿는다는 2026-07-29 사용자 피드백으로 텍스트 추가, 고정폭 제거해 자연스러운 크기)으로 `OcrEngine.list_gemini_models()` 호출 → OCR 콤보에 반영(`_refill_model_slots`)하고 `KEY_OCR_GEMINI_MODEL_CACHE_GATEWAY`(JSON list)에 저장 → 다음 실행 시 캐시 로드. **연결 테스트 버튼**도 한때 "테스트"로 줄였으나(칸이 좁던 시절 글자가 잘림) 창을 넓힌 뒤 전체 문구로 되돌렸다 — ⚠ 둘 다 `setFixedWidth`를 쓰지 않는다(전역 `QPushButton` padding보다 좁게 고정하면 글자가 잘린다, 실측 확인됨). ↻는 **'어떤 모델이 있는지'만 안다**(그게 API가 주는 전부) — 상태 줄도 그렇게 말한다(`✓ 모델 45종을 불러왔습니다. 고른 모델이 실제로 되는지는 [연결 테스트]로 확인하세요.`). ⚠ 이 버튼은 **`setFocusPolicy(NoFocus)` 필수**: 클릭 시 `setEnabled(False)`로 꺼지는데, StrongFocus면 Qt가 포커스를 다음 위젯(editable 모델 콤보)으로 넘기고 editable 콤보는 포커스를 받으면 텍스트를 전체 선택한다 → 조회 중 모델명이 파랗게 반전돼 "선택됐다 사라지는" 것처럼 보인다. 네트워크 호출은 `threading.Thread` + 내부 시그널 `_models_fetched(list, str)`로 UI 스레드 안전 통신. 옛 새로고침 실패 `QMessageBox` 모달 2종은 상태 줄(`_set_status`)로 흡수. **크리덴셜은 한 시점엔 한 벌**(API 키 + Base URL)이지만 **`API 프로필`(v1.56.0)로 여러 API를 이름 붙여 저장하고 드롭다운으로 전환**한다. Mindlogic 월 토큰 소진 시 구글 직결(OpenAI 호환 `.../v1beta/openai`)로 base_url만 바꿔 갈아타는 등 3개 API(구글·게이트웨이 2개)를 오가기 위한 것. **프로필 = (라벨 + base_url + api_key + OCR 모델 + 모델캐시) 스냅샷**(`KEY_AI_PROFILES = ai_profiles` — JSON list, **api_key를 품어 `main._SECRET_KEYS`로 통째 DPAPI 암호화** / `KEY_AI_ACTIVE_PROFILE = ai_active_profile` — 마지막 선택 라벨, 평문). ⚠ v1.6x 이전엔 프로필이 AI 질의 모델 1·2·3(`model`/`compare_a`/`compare_b`)까지 품었는데, 그 필드는 스키마에서 빠졌다(`_capture_current_profile`/`_google_preset`/`_apply_profile` 축소) — 옛 프로필 JSON에 그 필드가 남아 있어도 무시될 뿐 오류는 안 난다. 드롭다운에서 고르면 칸들이 그 값으로 채워진다(`_apply_profile`) — **연결 테스트는 자동으로 돌지 않는다**(v1.57.0에서 제거: 드롭다운을 훑을 때마다 네트워크 테스트가 튀어 불편했다. 확인은 `연결 테스트` 버튼으로). **콤보는 `currentIndexChanged`가 아니라 `activated`(사용자 선택 전용)에 연결한다** — 이미 활성인 프로필을 다시 골라도 발화해야 값이 채워지기 때문(활성 프로필과 필드=라이브 크리덴셜이 어긋난 상태에서 그 프로필을 재선택하는 경우. 같은 인덱스라 `currentIndexChanged`는 침묵). **구글 AI Studio 프리셋(v1.57.0)**: `_init_profiles`가 `Google AI Studio`라는 관리형 프리셋(base_url=`https://generativelanguage.googleapis.com/v1beta/openai` 고정·빈 키·기본 모델 `gemini-2.5-flash`)을 드롭다운에 항상 시드한다 — 구글 직결 URL을 매번 외우지 않게. 같은 이름이 이미 있고 그 base_url이 **구글이 아니면**(옛 게이트웨이 설정이 이 이름으로 잘못 저장된 오염 상태) 그 프로필을 URL 기준 이름(`_guess_profile_label`)으로 **개명해 데이터를 보존**하고 캐노니컬 프리셋을 새로 넣는다(구글 URL이면 사용자가 키를 채운 것이라 그대로 둠 → 자가치유·idempotent). ⚠ **엔진이 읽는 라이브 키(`KEY_OCR_GEMINI_*`/`KEY_OCR_MODEL_GATEWAY`)는 그대로**라 `ocr_engine`·`_resolve_gemini_cfg` 변경 0 — 프로필은 'UI 칸을 채우는 계층'일 뿐이다. `+ 저장`(현재 입력값을 이름 붙여 스냅샷·같은 이름이면 갱신, `_capture_current_profile`)·`삭제`, 둘 다 하단 `[저장]`을 눌러야 DB 반영(다른 설정과 같은 규칙). ⚠ **그룹 설명 문구는 없다**(2026-07-29 사용자 요청으로 삭제) — 대신 `+ 저장` 클릭 시 뜨는 `QInputDialog`의 라벨 자체가 "지금 입력된 Base URL·API 키·모델을 이 이름으로 저장합니다:"로 "아래를 채우고 위 버튼을 누른다"는 흐름을 전달한다(버튼만 눌러선 이름 입력칸만 보여 그 앞뒤 맥락이 빠지기 쉬웠던 문제). **자동 이관**(`_init_profiles`): 저장된 프로필이 없으면 현재 라이브 크리덴셜을 base_url로 이름 추정(`_guess_profile_label` → `구글`/`마인드로직`/`프로필 1`)해 첫 프로필로 시드(기존 설정 보존). `_loading_profiles` 가드로 콤보를 프로그램이 채우는 동안 선택 핸들러(자동 연결 테스트)가 오발동하지 않게 막는다. **라벨 중립화**: `Mindlogic API 키`/`Mindlogic Base URL` → `API 키`/`Base URL`(placeholder에 구글 `.../v1beta/openai`·게이트웨이 주소 병기) + API 키 옆 `보기`/`숨김` 토글(`_on_key_reveal_toggled`, Password↔평문 — 여러 프로필의 '무슨 키가 들었나' 확인용. ⚠ 이모지 `👁`는 Qt 버튼에서 컬러 이모지 폴백으로 깨져 텍스트로 둠). v1.50.0에서 **Google AI Studio(공식) 백엔드와 backend 개념 자체를 제거**했다 — 사용 빈도가 낮은 데 비해 backend 분기가 설정창·main·엔진 전반에 퍼져 있었고, Gemini 모델은 게이트웨이에도 그대로 있어 잃는 것이 없다. 옛 official 키·backend 키는 `main._migrate_drop_official_backend`가 1회 삭제한다. `_fill_model_combo(combo, candidates)`가 **계열 헤더 + 모델명**으로 채운다: `_add_group_header(combo, text)`가 `Gemini (6)`·`Claude (9)`·`GPT (19)` 등 헤더 행을 넣고(순서·정렬은 `ocr_engine.group_models`에 위임), 그 아래 모델명이 이름순으로 온다. **모델 행 들여쓰기**(`_ModelIndentDelegate`, `_MODEL_INDENT_PX`=16): 계열(상위)과 모델(하위)이 같은 x에서 시작해 상하 관계가 안 보이던 문제를 팝업 view 전용 델리게이트로 해결. 헤더는 `_HEADER_ROLE`로 구분해 제자리에 둔다. ⚠ **텍스트에 공백·불릿을 넣어 들여쓰면 안 된다** — editable 콤보라 그 공백이 저장되는 모델명을 오염시킨다(아래 헤더와 같은 이유). 투명 아이콘(`DecorationRole`)으로 미는 대안은 **닫힌 콤보의 현재 모델명 앞에도** 빈 아이콘 자리를 만들어 텍스트가 밀리므로 쓰지 않는다. 부수효과: 선택 하이라이트도 함께 들여써진다(트리뷰 관용, 의도적 수용). `_adjust_model_popup_width`는 모델 행에 이 들여쓰기 폭을 가산해 계산한다. ⚠ **헤더는 반드시 비활성 항목**(`model().item(idx).setEnabled(False)`): 이 콤보는 `setEditable(True)`라 **표시 텍스트가 곧 저장되는 모델명**(`_on_save`가 `currentText()`를 그대로 씀)이므로, 헤더를 고를 수 있으면 `Gemini (6)` 같은 문자열이 API 모델명으로 저장돼 호출이 깨진다. 같은 이유로 **텍스트 접미사 배지도 달면 안 된다**(모델명 오염). `clear()` 직후 Qt가 0번(헤더)을 자동 선택하는 것은 `_select_first_enabled(combo)`가 되돌리는데, **첫 실제 항목이 아니라 `_FALLBACK_DEFAULT`를 우선 고른다** — 계열 안이 이름순이라 그냥 첫 항목을 잡으면 기본값이 `gemini-2.0-flash` 같은 구형이 된다. `_adjust_model_popup_width(combo)`로 드롭다운 팝업(view) 최소 폭을 최장 모델명에 맞춰 넓혀(콤보 본체·설정창 폭은 불변) 긴 이름의 가운데 생략을 막는다. **창 동작(v1.28.1)**: 창 플래그는 `Dialog | WindowCloseButtonHint`지만 Windows HWND엔 `WS_MINIMIZEBOX`/`WS_MAXIMIZEBOX`가 남아 최소화 버튼이 그려지므로(Qt→Win32 매핑 특성), `showEvent`가 `_strip_min_max_buttons()`로 그 스타일 비트를 제거해 '눌러도 안 되는 최소화 버튼'을 없앤다(`WS_SYSMENU`는 유지 → 닫기 버튼만 표시. 둘 중 하나만 지우면 남은 쪽이 회색으로 남아 min/max box 둘 다 제거. 네이티브 창 재생성 대비 show마다 재적용). `changeEvent`의 최소화 되돌림은 strip 실패·시스템 메뉴 경로 대비 안전망으로 유지. **모달 범위 = 창-모달**(v1.39.1): 설정창은 `exec()`로 띄우되 그 직전에 `setWindowModality(WindowModal)`를 걸어 **부모(패널)만 잠그고** 부모 없는 최상위 오버레이는 잠그지 않는다 → 설정창이 열린 채로도 영역 캡처(Alt+F2)·AI OCR이 정상 동작한다. `exec()`의 기본값인 **application-modal**은 앱의 모든 창 입력을 막아 오버레이가 떠도 조작이 불가능했고(실측: 부모 없는 `Tool` 위젯에 SendInput 좌클릭 → ApplicationModal에서 `mousePressEvent` 미수신, WindowModal에서 수신), 그래서 옛 코드는 `_is_settings_open()` 가드로 오버레이를 아예 안 띄우고 '설정창을 닫은 뒤 사용하세요' 토스트만 보여줬다 — 이 회피책은 제거됨. 대신 `_open_settings`에 **재진입 가드**가 필요하다: 창-모달이라 트레이 메뉴와 API 키 오류 자동 오픈(`QTimer.singleShot(300, self._open_settings)`)이 막히지 않아 중첩 `exec()`가 실제로 발생할 수 있으므로, `self._settings_dialog`가 살아 있으면 새 창을 만들지 않고 `raise_`/`activateWindow`만 하고 반환한다. 부수 효과: 설정창이 열린 동안 미리보기·핀·AI 질문창 등 **다른 최상위 창은 입력을 받는다**(패널만 잠김). **빈 여백 축소(v1.28.1)**: 세 폼(기능 단축키·OCR 연동·일반)에 `setVerticalSpacing(4)`+그룹 여백 축소·바깥 레이아웃 간격/여백 축소로 설정창 세로 길이를 줄였고, 옛 `💡 가장 저렴` 모델 힌트 라벨(`_update_model_hint`)은 제거. **음성 입력(STT) UI**(2026-08-02 도입, `AI` 탭 기능 단축키 맨 아래): 단축키 필드(`KEY_STT_HOTKEY`=`hotkey_stt`, 기본 `ctrl+win`)는 유일하게 `HotkeyEdit(allow_mod_only=True)`로 생성돼 일반키 없이 수식키만으로 된 조합(Ctrl+Win 등)도 캡처 가능 — 다른 단축키는 이 옵션을 안 켠다(백엔드 파서가 수식키 전용 조합을 이해 못 해 영구히 안 눌리는 상태가 될 위험). **마이크 콤보**: `stt_engine.list_input_devices()`로 채우고(`KEY_STT_MIC_DEVICE`=`stt_mic_device`, 빈 값=시스템 기본) 새로고침 버튼 제공. **STT 모델 콤보**: OCR 모델 콤보와 달리 `ocr_engine.family_of(name) == "Gemini"`로 걸러 Gemini 계열만 표시(오디오 입력이 계열 단위로만 되기 때문 — `stt_engine.py` 항목 참고). ⚠ **STT 모델은 `연결 테스트`가 확인하지 않는다** — STT 프로브 기능은 없다(이 문서가 한때 있는 것처럼 적혀 있었으나 실제 코드와 불일치해 2026-08-12 정정). STT 모델·마이크가 맞는지는 실제 단축키(기본 `ctrl+win`)로 녹음해 확인해야 한다. 크레딧 확인은 위 "크레딧 확인 병합" 항목대로 `연결 테스트` 버튼에 흡수됐다(별도 버튼 없음).
- **`ocr_overlay.py`** — 모니터별 분리 오버레이. `OcrOverlay`는 매니저(QObject 베이스, QWidget 아님)이고 실제 위젯은 `_ScreenOverlay`로 각 QScreen마다 1개씩 생성. `start()` 호출 시 모니터 수만큼 `_ScreenOverlay`를 만들고 각각 자기 화면을 `screen.grabWindow(0, 0, 0, w, h)`로 캡처해 표시. 한 모니터에서 드래그 시작되면 `drag_started` 시그널로 매니저가 다른 오버레이를 `deactivate()`(마스크만 표시·입력 차단). ESC/우클릭은 어느 오버레이에서든 전체 취소. **다중 DPI 모니터 대응**: 가상 데스크톱 전체를 단일 위젯으로 덮으면 Qt 백킹 스토어 DPR이 하나로 고정돼, DPR이 다른 모니터에 진입할 때 좌표·크기가 어긋나 고DPI 노트북 화면이 좌상단 일부로 축소되는 증상이 발생한다. 모니터별 분리 위젯 + `setScreen()` 명시 바인딩으로 Qt가 모니터별 DPR을 독립 처리하므로 문제 자체가 발생하지 않는다. 공개 API(`region_captured(QPixmap)`, `cancelled()`, `start()`)는 호출부 변경 없이 유지.
- **`ai_query.py`** — `AiQueryDialog`(질문 입력창)와 그 안의 `_QuestionEdit`(Ctrl+V·드롭 이미지 첨부 텍스트칸, 최대 1장). **타겟 팔레트**: `sites`(main이 `ai_palette.load_sites()`로 DB `ai_palette_sites`에서 읽어 넘기는 목적지 목록 — 기본은 Google AI 하나, 설정창 "빠른 검색"(2026-07-29 이전엔 "AI 팔레트 타겟")에서 사용자가 URL 타겟을 추가 가능)를 칩 로우로 렌더한다(번호 없는 라벨만 — 2026-08-03부터, 아래 참고). 선택 방식: `Tab`/`Shift+Tab`(칩 하이라이트 순환), 등록된 `keyword`+공백 접두어(예: `"yt 고양이"` → 그 타겟으로 자동 전환, 접두어는 질의에서 제거 — `ai_palette.match_keyword`). `Enter`/`Ctrl+Enter` 둘 다 지금 하이라이트된 타겟(`_effective_index`)으로 실행한다. ⚠ **`Alt+1~9` 즉시전송 단축키는 2026-08-03에 제거했다**(`_on_alt_number`·`_QuestionEdit`의 `on_alt_number` 콜백·칩 라벨의 번호 접두사·해당 툴팁 전부 삭제) — 타겟이 사실상 Gemini(Google AI) 하나로 통합돼 있고 Tab 순환만으로 충분해 번호 단축키가 군더더기였다(사용자 판단: "ai질문창은 gemini 하나로 통합된거나 마찬가지고 tap으로 전환이 되니까 alt 단축키는 그쪽은 없어도 될듯"). 부수 효과: 이 단축키가 전역 단축키(`Gemini 호출`=Alt+1, `Gemini(캡처)`=Alt+2, 위 `paste_interceptor.py` 항목 참고)와 번호가 겹쳐, 질문창이 열린 채로 안쪽에서 Alt+1/2를 누르면 전역 후킹이 먼저 그 키를 가로채 안쪽 선택이 씹힐 수 있었던 잠재적 부작용도 함께 해소됐다. 실행 결과는 `get_result() -> (site_dict, query) | None`으로 알린다 → main의 `_open_ai_dialog`가 그 `site['kind']`(`google_ai`/`url`)로 브라우저 경로(`_open_in_browser`)·`_open_palette_url`(`ai_palette.build_url`+`web_open.open_url`)로 분기한다. ⚠ **v1.6x에서 모델 드롭다운·비교 체크박스·기록 버튼을 전부 제거했다** — 우클릭 "AI에게 질문"과 함께 PasteFlow 자체 답변(`api` kind) 자체가 사라져 그 UI를 붙일 자리가 없어졌다(옛 `_model_combo`/`_compare_check`/`open_history` 콜백·`compare_models`/`fetch_all_models` 생성자 인자 삭제 — 생성자는 이제 `__init__(self, parent=None, sites=None)` 하나뿐). **왜 구글 AI가 기본 1번인가**: 실시간 수치·최신 정보는 API 검색 경로가 구조적으로 못 잡고(`web_open.py`의 2026-07-14 실측), 이미지 질의도 구글 입력칸이 Ctrl+V로 이미지를 받아 주입 경로로 커버된다. **이미지 첨부는 1장만 유지한다**(`_add_image`가 새로 붙이면 이전 것을 교체) — Google AI 모드가 이미지를 1장만 받아 준다고 사용자가 실사용으로 확인했다(2026-07-29). 오케스트레이션(브라우저 열기 등)은 `main.py`가 담당하고, 이 파일은 다이얼로그 자체의 위젯 구성·이벤트만 갖는다. **parent=None·비모달**(v1.49.4) — 부모(패널)를 주면 Windows가 이 창을 패널의 "소유 창"으로 취급해 패널이 그 위로 못 올라오므로 완전 독립 창으로 둔다. **＋ TOPMOST 그룹으로 승격**: 소유 관계를 끊자 이번엔 반대로 패널이 항상 이겼다(패널은 `panel._set_always_on_top`으로 TOPMOST라 Windows Z-order상 일반 창보다 무조건 위 → 질문창을 클릭해도 앞으로 못 나옴). 질문창에도 `WindowStaysOnTopHint`를 줘 **미리보기 팝업과 같은 TOPMOST 그룹**에 올리면, 그룹 안에서는 활성화 순서가 통해 '클릭한 창이 앞으로' 온다. **2026-07-29 UI 정리**: 창 제목을 `"AI에게 질문"`→`"Gemini에게 질문"`로 개명(어차피 Google AI/Gemini만 쓰므로). 취소 버튼(`cancel_btn`)을 없앴다 — 우상단 네이티브 X와 `QDialog` 기본 `Esc`(→`reject()`)가 이미 같은 역할이라 중복이었고, 그 버튼 행이 쓰던 세로 공간만큼 `setMinimumSize`를 200→160으로 줄였다. 입력칸(`_editor`) placeholder에 사용법(Tab/Enter/이미지 첨부)과 키워드 예시(`"예: yt 강아지"`)를 불릿(`•`)+들여쓰기로 정리해 넣었다 — 설정창 "빠른 검색" 그룹의 설명 문구를 없앤 대신, 실제로 그 사용법을 쓰는 이 자리로 옮긴 것(멀티라인 `QPlainTextEdit.placeholderText`가 줄바꿈까지 그대로 렌더되는지 오프스크린 렌더로 확인).

### 단축키 체계

| 단축키 | 동작 | 감지 방식 |
|--------|------|-----------|
| Ctrl+Shift+V | 순차 붙여넣기 (suppress) | WH_KEYBOARD_LL (paste_interceptor) |
| ctrl+space *(기본값, 설정 가능)* | 패널 토글 (suppress) | WH_KEYBOARD_LL (paste_interceptor) |
| ctrl+shift+s *(기본값, 설정 가능)* | OCR 영역 선택 시작 (suppress) | WH_KEYBOARD_LL (paste_interceptor) |
| ctrl+shift+p *(기본값, 설정 가능)* | 클립보드 이미지를 임시 PNG로 저장 후 경로 텍스트로 자동 Ctrl+V (suppress) | WH_KEYBOARD_LL (paste_interceptor) |
| ctrl+shift+[ *(기본값, 설정 가능)* | 순차 경로 붙여넣기 — 큐에서 다음 항목을 꺼내 이미지면 경로 텍스트로(Ctrl+Shift+V와 큐 공유) (suppress) | WH_KEYBOARD_LL (paste_interceptor) |
| alt+f3 *(기본값, 설정 가능)* | 클립보드 이미지/텍스트를 화면에 핀(떠 있는 창)으로 띄우기 (suppress) | WH_KEYBOARD_LL (paste_interceptor) |
| alt+shift+f3 *(기본값, 설정 가능)* | 순차 핀 — 큐에서 다음 항목을 꺼내 화면에 핀(Ctrl+Shift+V와 큐 공유) (suppress) | WH_KEYBOARD_LL (paste_interceptor) |
| alt+f2 *(기본값, 설정 가능)* | 영역 캡처 → 클립보드(DIB)+파일 저장 (suppress). 오버레이 안에서 Space=실제 커서 포함 토글 | WH_KEYBOARD_LL (paste_interceptor) |
| PrintScreen *(설정 체크박스, 기본 꺼짐 — 2026-08-12)* | 영역 캡처(Alt+F2)의 대체 트리거 — 수식키(Ctrl/Shift/Alt/Win) 없이 단독으로 눌렸을 때만 동일한 `on_capture` 콜백 발동, Alt+PrtScn·Win+PrtScn 등 OS 조합은 그대로 통과. 켜면 OS 기본 PrtScn(전체화면 클립보드 복사) 동작을 suppress로 대체 | WH_KEYBOARD_LL (paste_interceptor) |
| alt+1 *(기본값, 설정 가능 — 2026-08-03부터, 예전엔 alt+\`)* | AI 자유질문(Gemini 호출) — 컨텍스트 없이 즉석에서 AI 질문 입력창 (suppress) | WH_KEYBOARD_LL (paste_interceptor) |
| alt+2 *(기본값, 설정 가능 — 2026-08-03부터, 예전엔 win+\`)* | Gemini(캡처) — 영역 캡처 + Gemini 질문창 첨부, 캡처(Alt+F2)와 동일하되 이미지를 곧장 질문창에 첨부 (suppress) | WH_KEYBOARD_LL (paste_interceptor) |
| ctrl+shift+g *(기본값, 설정 가능)* | GIF 녹화 — 영역 선택 후 라이브 녹화 → GIF 저장·경로 복사 (suppress) | WH_KEYBOARD_LL (paste_interceptor) |
| ctrl+shift+r *(기본값, 설정 가능 — 2026-08-03)* | 영상(MP4) 녹화 — GIF 녹화와 같은 영역 선택 흐름, 프레임을 즉시 파일에 기록 → mp4 저장·경로 복사 (suppress) | WH_KEYBOARD_LL (paste_interceptor) |
| ctrl+win *(기본값, 설정 가능)* | 음성 입력(STT) 푸시투토크 — 누르는 동안 녹음, 떼면 인식 후 자동 붙여넣기 (keydown+keyup 처리, suppress 안 함) | WH_KEYBOARD_LL (paste_interceptor) |
| 트레이 좌클릭 | 패널 토글 | Qt 이벤트 |

> ⚠️ `Alt+1~9` 직접 붙여넣기, `Ctrl+Shift+X` 큐 초기화, `Ctrl+Shift+Z` 실수 복구는 **의도적으로 제거**됨.

### 화면 핀 · 영역 캡처 (Snipaste식, v1.8.0~)

Snipaste를 대체하는 캡처 기능군. 단축키 추가는 모두 기존 패턴(`set_*_hotkey` + 훅 감지 블록 + 브리지 시그널 + 설정 배선) 복제.

- **화면 핀 (Alt+F3)** — `main._on_pin_hotkey`: 클립보드 이미지를 읽어(`_read_image_from_clipboard`) `ImagePreviewPopup.open_new(..., native=True)`로 화면에 띄움(원본 1:1 크기, 화면 초과 시만 축소 / 다중 창·ESC 닫기·Space 주석 편집은 image_preview가 이미 제공). **경로 텍스트 → 원본 이미지 복원**(v1.39.0): 클립보드에 이미지가 없고 그 텍스트가 **직전 이미지→경로 단축키가 올려둔 임시 PNG 경로**(`self._last_pasted_image_path`, `Ctrl+Shift+P`·`Ctrl+Shift+[`가 기록)와 정확히 일치하며 파일이 존재하면, 경로 문자열을 렌더하지 않고 **그 PNG를 읽어 이미지로 핀**한다. Alt+F2 캡처 → Ctrl+Shift+P 경로 붙여넣기 → Alt+F3 하면 경로 글자가 핀되던 문제 해결(임시 파일 경로를 이미지로 띄워봐야 쓸모가 없다). `_pin_place_rect`가 self-triggered `_set_clipboard`로 무효화되지 않아 **캡처했던 자리에 1:1로 덮인다**. 진짜 텍스트를 복사해 핀하는 Snipaste 동작은 경로가 일치할 때만 갈라지므로 그대로 유지. 이미지가 없으면 클립보드 **텍스트를 다크 배경 PNG로 렌더**(`_render_text_to_png` — 맑은 고딕 18px, 워드랩, 배경 `theme.BASE`+글자 `theme.TEXT`로 앱 다크 테마와 통일)해서 띄움(텍스트도 주석 가능 = 사실상 이미지화). **배치(v1.23.0)**: 방금 Alt+F2로 캡처한 이미지면(중간에 다른 걸 복사 안 함) `place_rect`로 **캡처한 그 자리에 1:1로 정확히 덮고**(줌=사각형폭/픽맵폭이라 DPI 무관) **등장 시 테두리 1회 반짝**(`_flash_border` — 흰→코랄, `_flashing` 플래그로 활성화 이벤트가 안 덮게 가드), 아니면 커서 우측에 배치(폴백). **`image_preview._bg_item.setTransformationMode(Smooth)`**: QGraphicsPixmapItem 기본 Fast(nearest)가 뷰의 SmoothPixmapTransform을 덮어써 비정수 배율에서 이미지·텍스트가 거칠게 보이던 것 수정(모든 미리보기 공통 개선).
- **순차 핀 (Alt+Shift+F3, v1.53.0)** — `main._on_seq_pin_hotkey`: 화면 핀의 **'큐 버전'**. 순차 붙여넣기(Ctrl+Shift+V)·순차 경로 붙여넣기(Ctrl+Shift+[)와 **같은 큐·포인터를 공유**하며(`queue.get_next()`), 누를 때마다 다음 항목을 꺼내 화면에 핀한다 — 이미지는 그대로, 텍스트는 `_render_text_to_png`로 이미지화(Alt+F3와 동일). 캡처(Alt+F2)를 여러 장 찍어 두고 이 키를 눌러 캡처1·2·3을 차례로 화면에 펼쳐 참고하는 용도. 큐 소진 시 토스트만(현재 클립보드 폴백은 Alt+F3가 담당 — '순차/일반'을 키로 구분하는 원칙 유지). ⚠ **핀은 '보기'라 `place_rect`(캡처 자리 1:1 덮기)를 쓰지 않고 커서 옆에 띄운다** — 항목마다 커서를 옮겨 배치를 손으로 정하게 하려는 것(제자리에 겹쳐 쌓으면 여러 장을 나란히 볼 수 없다). 팝업 시그널 연결(복사·OCR·AI·경로·주석)은 Alt+F3와 동일. 끝에 `_update_paste_ui()`로 진행 HUD 갱신, 큐 소진 시 `_on_paste_queue_done()`. 설정 키 `hotkey_seq_pin`(= `settings_dialog.KEY_SEQ_PIN_HOTKEY`), 인터셉터 `set_seq_pin_hotkey()`/`on_seq_pin` 콜백 → `_bridge.seq_pin` 시그널. ⚠ **공유 포인터라 핀이 큐를 소비한다** — 같은 항목을 핀하고 또 붙여넣을 수는 없다(수용한 트레이드오프).
- **영역 캡처 (Alt+F2)** — `main._on_capture_region(pixmap, rect)`: 마그네틱 캡처 오버레이(`_capture_overlay` = `CaptureOverlay`)가 선택 영역 QPixmap + **캡처한 논리 전역 사각형**을 `region_captured(QPixmap, QRect)`로 emit → **DIB**(`_qpixmap_to_dib` — 24bpp BI_RGB, 붙여넣기 호환 최광)로 변환 → `interceptor._set_clipboard`+`_persist_clipboard_item`(OCR 결과와 동일 무중복 경로: 클립보드+히스토리+큐) → `_save_image_to_folder`(설정 `capture_save_folder`, 기본 `_default_capture_folder()` = `<Pictures>\PasteFlow`, 없으면 생성). **토스트는 일반 복사와 동일한 `toast.show_copy_toast(saved_item, queue_total)`로 통일한다**(2026-08-03 — 예전엔 `"캡처됨: {파일명}"` 텍스트+`image_path` 썸네일을 별도로 만들었으나, 텍스트/이미지 복사와 다른 UX로 보였고 몇 번째 큐에 걸렸는지도 안 보였다는 사용자 피드백으로 교체. `Q{n}` 배지+썸네일만 뜨고 "캡처됨" 문구·파일명은 더 안 보여준다 — 파일명이 타임스탬프 기반이라 사용자에게 의미 있는 정보가 아니었다). 캡처 사각형은 `self._pin_place_rect`에 기억해 **직후 Alt+F3 핀이 제자리 덮기**에 쓰고, 외부 복사(`_on_new_clipboard_item`)가 들어오면 무효화(=방금 캡처한 이미지일 때만 적용). 설정에 캡처 단축키 행 + 저장 폴더 행(`QFileDialog`). **PrintScreen 대체 트리거**(2026-08-12, 설정 체크박스 `PrintScreen 키로도 실행`, 기본 꺼짐): Alt를 누르면 사라지는 메뉴 등을 캡처하고 싶다는 요청으로 도입 — `interceptor.set_capture_via_printscreen(bool)`이 켜지면 `paste_interceptor.py`가 `VK_SNAPSHOT`이 수식키 없이 단독으로 눌렸을 때만 이 `on_capture` 콜백을 그대로 호출하고 suppress한다(Alt+PrtScn·Win+PrtScn 등 OS 고유 조합은 수식키 매칭 조건에서 걸러져 건드리지 않음). suppress가 OS 기본 PrtScn 동작(전체화면 클립보드 복사)까지 대체하는 것을 격리된 `WH_KEYBOARD_LL` 훅 인스턴스에 `SendInput`으로 물리 키와 동일한 이벤트를 주입해 실측 확인(클립보드 마커 텍스트가 캡처 후에도 그대로 유지 = OS가 개입 안 함).
- **마그네틱 캡처 (v1.23.0 재작성 — 입력-소유 + 얼린 최상위창 + 창-스코프 요소 스냅)** — `uia.py`(`rect_in_window_at(hwnd,x,y)` = 특정 창 안 커서 아래 최말단 요소의 물리픽셀 사각형 — **MSAA `AccessibleObjectFromWindow`(창-스코프)로 IAccessible 루트를 얻고 `accHitTest`를 반복 하강**. 점-기반 `AccessibleObjectFromPoint`/UIA `ElementFromPoint`는 최상위 오버레이를 짚으므로 **창-스코프로 대체**. 크롬 최대화 북마크 개별 인식 = MSAA만 정확, Snipaste도 OLEACC. 옛 점-기반 `rect_at`은 남아 있으나 캡처 미사용. ⚠ **루트는 최상위 창이 아니라 `_deepest_child_at()`이 찾은 '커서 아래 최말단 자식 HWND'다**(v1.53.0) — `accHitTest` 한 단계가 크로스프로세스 COM 호출(~4ms)이라, 최상위 창부터 내려가면 리본·주소창 같은 껍데기 계층을 전부 걸어 **탐색기에서 13단계 = 59ms**가 걸렸다(hover 버벅임의 정체). 이는 oleacc가 `AccessibleObjectFromPoint` 안에서 먼저 하는 `WindowFromPoint`를 빠뜨린 것으로, 자식(`DirectUIHWND`)에서 시작하면 **2단계 = 5ms**에 같은 사각형이 나온다(격자 48점 48/48 동일). 요소를 못 짚으면 최상위 창 루트(`_hit_test_from(hwnd,…)`)로 폴백해 품질 후퇴 0. ⚠ **MSAA 오배치 쓰레기 걸러내기(HWP 대응, v1.54.0)**: HWP는 편집창(`HwpMainEditWnd`)이든 리본 툴바(WPF 최상위 창에 직접 그림)든 MSAA가 문서/캔버스를 **담긴 창 밖으로 크게 벗어난 과대·오배치 사각형**으로 준다(실측: 편집창 1035×1626인데 MSAA는 x=2922의 1552×2439). 그대로 최상위 창에 클램프하면 **우측 세로 슬라이버**만 잡힌다. 그래서 MSAA 요소를 **담긴 창**(콘텐츠 자식이면 자식 HWND, 아니면 최상위)과 `_mostly_within`으로 대조해, 크게 벗어나면 **콘텐츠 자식(`HwpMainEditWnd`)이면 그 자식 창(=편집영역, Snipaste와 동일 granularity)**을, **최상위 창(툴바 등 크롬)이면 `None`→호출부 창 전체 폴백**을 반환한다 — 편집영역 스냅(문서)과 창 전체 스냅(크롬)이 한 규칙으로 공존. UIA 창-스코프 폴백은 HWP 텍스트 위에서 `ElementFromPoint`가 0×0을 줘 기각. ⚠ **`uia`는 전용 `WinDLL("user32")` 인스턴스를 쓴다** — 공유 `ctypes.windll.user32`는 프로세스 전역 캐시 객체라 여기서 `GetWindowRect.argtypes`를 걸면 같은 함수를 쓰는 `capture_overlay`의 argtypes 설정과 서로 덮어써, `uia._window_rect`가 자기 `_RECT`로 호출하는 순간 `ArgumentError` → `rect_in_window_at`이 `None` 반환 → 캡처가 창 전체로 폴백(프로브(단일 모듈)에선 안 터져 함수는 통과하는데 앱에서만 편집영역 스냅이 깨지던 원인). 전용 인스턴스로 argtypes를 격리해 차단. ⚠ **`ChildWindowFromPointEx` 플래그에 `SKIPDISABLED|SKIPTRANSPARENT`가 없으면 안 된다** — 순수 기하 hit-test라 크롬의 입력-투명 자식(`Chrome_RenderWidgetHostHWND`·`Intermediate D3D Window`)을 짚어 그 창의 별개 접근성 트리에서 엉뚱한 사각형(최악은 창 전체)이 나온다. 이 플래그를 주면 크롬은 최상위 창이 루트로 남아 옛 동작과 48/48 일치. 자식만 뒤지므로 별개 최상위 창인 오버레이를 짚을 위험은 없다) + `ui/capture_overlay.py`(**입력-소유(비클릭-통과) 오버레이** — 클릭-통과면 커서 밑 실제 창이 `WM_SETCURSOR`를 받아 HWP 등 커스텀 커서가 십자를 덮으므로, 오버레이가 입력을 소유해 `setCursor(CrossCursor)`로 어떤 앱 위에서든 십자 보장(Snipaste 모델). **옛 `WindowTransparentForInput`(클릭-통과)+`SetSystemCursor`(십자 전역변조)+`WH_MOUSE_LL`(마우스 훅)은 전부 폐기**). 캡처 시작 시 오버레이 show *전에* `GetTopWindow`+`GW_HWNDNEXT`로 **최상위 창 `(hwnd,사각형)`을 Z-order로 1회 얼림**(`_enum_top_windows` — 최소화·클로킹·자기 오버레이 제외). QTimer ~16ms로 `GetCursorPos`→얼린 목록에서 커서 밑 최상위 창→그 창에 한정해 요소/창 스냅을 정한다. ⚠ **커서가 비클라이언트(제목표시줄·창 테두리)면 요소 하강을 건너뛰고 창 전체로 스냅**(v1.55.0 — `_client_rect_screen()`이 `GetClientRect`+`ClientToScreen`로 클라이언트 화면 사각형을 조립해 커서가 그 밖이면 `win_rect`(제목표시줄 포함 창 전체) 반환). 상단바 위에서 MSAA가 캡션 띠 조각을 비결정적으로 짚어 "가끔만 창 전체"가 되던 문제 해결 — 이 판정은 MSAA가 그 자리에서 뭘 짚든(캡션 띠·시스템 버튼·창 루트) 무관하게 구성상 항상 창 전체라 결정적이다(Snipaste식 "상단바=창 전체"). 클라이언트 안(내용 위)이면 `uia.rect_in_window_at`로 요소 하강(못 짚으면 창 전체 폴백). 그다음 물리→논리 변환(`MonitorFromPoint` 물리원점+QScreen DPR)→하이라이트, ESC 폴링. **hover hit-test는 커서가 움직인 tick에만 부른다**(`_last_cursor` 비교 — 안 움직이면 결과가 같아 유휴 비용 0). 옛 `_UIA_MIN_INTERVAL`≈30ms 스로틀은 **제거됨**(v1.53.0) — 호출 하나가 59ms이던 시절의 방어막인데, 아래 hit-test 루트 교체로 5ms가 된 지금은 하이라이트를 33Hz로 묶어 "영역이 건너뛰며 뒤늦게 따라오는" 체감만 남겼다. **`_capture_overlay`로 main에 연결**(Alt+F2). 동작:
  - **클릭 캡처**: 오버레이가 입력을 소유하므로 Qt `mousePressEvent`/`mouseReleaseEvent`로 받음(**옛 WH_MOUSE_LL 데몬 훅 폐기** — 전역 마우스 훅 제거로 시스템 마우스 끊김 위험도 소멸, suppress 춤도 불필요). 좌클릭=현재 하이라이트 요소/창 캡처, 우클릭=취소(둘 다 매니저 플래그만 세팅하고 실제 처리는 tick).
  - **자유드래그 폴백(3c)**: 좌버튼 누른 채 `_DRAG_THRESHOLD`(4px) 이상 이동하면 클릭 대신 자유 사각형(시작점~현재 커서, 요소 무시). 안 움직이고 떼면 요소 클릭. 빈 영역을 드래그 없이 클릭하면 무시(`_capture(None)`). **드래그 시작점은 press 이벤트의 전역 좌표로 확정**(v1.34.0 — `_on_left_down(start_pos)`) — 옛날엔 첫 tick에서 샘플해, Explorer처럼 UIA hit-test가 느린 창 위에선 tick 지연 동안 커서가 이동해 시작점이 밀렸다(빠른 드래그 시 잘못된 위치에서 시작). 또 **버튼을 누른 동안은 hover 요소 hit-test(무거운 MSAA)를 스킵**해 드래그가 매끄럽고, 클릭 캡처는 놓는 순간 `_target_rect_logical()`로 재계산하므로 요소 스냅 품질은 그대로.
  - **크로스 모니터 합성(`_crop_global`)**: 선택 영역이 단일 모니터면 그 화면 스크린샷에서 바로 crop, 여러 모니터에 걸치면 가장 높은 DPR을 타깃으로 빈 캔버스에 각 화면 조각을 제 위치에 그려 합성(배율 다른 조각은 타깃 DPR로 스케일 — 기하학 정확, 저DPI 조각은 약간 소프트). 100/125/150% 트리플 모니터 실조건 검증 완료.
  - 결과 QPixmap + 캡처 사각형 → `region_captured(QPixmap, QRect)` → `_on_capture_region`(DIB+클립보드+히스토리+파일+토스트 + `_pin_place_rect` 기억).
  - **실제 커서 포함 (Space 토글, 2026-08-04)** — 캡처 결과물에 마우스 포인터를 함께 굽는 기능("여기를 가리키는 화면"이 필요할 때). **오버레이가 뜨기 전에** `gif_recorder.sample_cursor()`로 진짜 커서(아이콘 핸들+물리좌표)를 1회 얼려두고(`_sample_and_locate_cursor` — `start()`가 창 생성보다도 먼저 호출), Space를 누르면 그 자리에 **실제 커서 아이콘 픽셀 그대로** 미리보기 마커를 띄운다(`_build_cursor_preview` → `blit_icon_at`으로 작은 패치에 합성, 지연 생성). 영역 선택(드래그)은 이 마커와 무관하게 자유롭고, 캡처 확정 시 Space가 ON이면 `composite_cursor(..., cursor=self._frozen_cursor)`로 그 얼려둔 커서를 굽는다. ⚠ **얼려두는 것이 핵심** — 오버레이는 입력을 소유해 자기 십자선을 강제로 씌우므로, 캡처 시점에 `GetCursorInfo`를 부르면 진짜 앱 커서(I빔·손가락)가 아니라 **캡처 도구의 십자선**이 찍힌다. GIF/영상 녹화는 오버레이가 꺼진 뒤 녹화하므로 계속 라이브 샘플링(`cursor=` 미지정)을 쓴다. **커서 숨김 폴백**: Windows "입력 중 포인터 숨기기"(기본 ON)에 걸려 있으면(키보드만 쓰다 Alt+F2를 누른 흔한 경우) `sample_cursor()`가 None을 주는데, 그때는 표준 화살표(`IDC_ARROW`)+`GetCursorPos`로 대체해 기능이 통째로 먹통 되지 않게 한다. 매 캡처 세션마다 토글은 기본 OFF, `select_only`(GIF/영상 녹화)엔 이 UI가 아예 안 뜬다(그쪽은 `gif_show_cursor` 설정이 담당).
  - **Space·ESC 억제 훅 + 그 키의 판정(2026-08-04)** — 오버레이는 마우스는 소유하지만 **키보드 포커스는 원래 앱에 그대로 둔다**(`WA_ShowWithoutActivating`). 그래서 Space·ESC를 폴링만 하고 삼키지 않으면 그 키가 배경 앱으로 그대로 새어(크롬에서 스크롤·버튼 클릭 발생 — 2026-08-04 사용자 리포트) 캡처 중에 엉뚱한 동작이 일어난다. 캡처 세션 동안만 사는 **전용 저수준 키보드 훅**(`_install_key_suppression_hook`/`_on_kb_hook`, `start()`에서 설치·`_close_all()`에서 해제)이 두 키의 물리 keydown/keyup을 삼킨다. ⚠ **억제와 `GetAsyncKeyState` 폴링은 양립 불가** — 훅이 삼킨(`return 1`) 키는 비동기 키 상태 테이블에 반영되지 않아, 폴링으로 판정하면 Space 토글·ESC 취소가 **영영 안 걸린다**(2026-08-04 실측: 훅은 keydown을 정상 수신했는데 같은 순간 `GetAsyncKeyState`는 훅 콜백 안에서조차 False. 억제 훅을 넣은 직후 "Space를 눌러도 OFF에서 안 바뀐다"는 리포트의 정체가 이것). 그래서 **판정을 훅 콜백으로 옮겼다**: Space keydown → `_space_toggle_pending`(auto-repeat 연타 방지는 `_space_down` 가드), ESC keydown → `_cancel_pending`, 실제 처리는 `_tick`이 소비(콜백은 짧게 유지). 훅 설치가 실패한 경우에만 `_tick`의 옛 폴링이 폴백으로 유효해진다(그땐 억제도 없으므로 폴링이 정상 동작). ⚠ 이 훅은 `paste_interceptor.py`의 상시 전역 훅과 **별개**다 — 그쪽은 IME 마스킹·Alt 레이스 등으로 세밀하게 튜닝돼 있어 건드리면 회귀 위험이 크다. 콜백 객체(`_kb_hook_proc`) 참조는 언훅 후에도 놓지 않는다(ctypes 콜백 수명 관리 표준 관행).
  - **십자 커서 (v1.23.0)** — 커서는 `setCursor(CrossCursor)` 하나로 처리(입력-소유라 위젯이 커서를 정함 → **HWP 포함 모든 앱에서 십자 유지**). 옛 `SetSystemCursor` 전역 커서 변조·`atexit` 복원은 폐기(전역 상태 변조 제거 = 크래시 시 십자 잔존 없음). **한계**: HWP는 MSAA 요소를 안 내줘 창 전체 스냅만 되고 요소 스냅은 안 됨(폴백).
  - **깜빡임 제거 (v1.23.0)** — 진입 시 화면 한 번 깜빡이던 것을 `WA_OpaquePaintEvent`(전 픽셀 직접 칠함 → 배경 지우기·반투명 레이어드 첫-프레임 검은 프레임 제거)+`show_overlay`의 `repaint()`(매핑 직후 동기 첫 페인트)로 제거. **원인 분석**: `WA_ShowWithoutActivating`으로 비활성 표시라 첫 페인트가 비동기로 밀려 한 프레임 빈 화면이 보였고, 동기 `repaint()`가 그 갭을 닫는 게 실제 해법(OCR 오버레이는 활성화하며 표시돼 이 증상 없음 — 그래서 미수정).
  - **패널이 열려 있으면 Alt+F2 캡처가 안 되던 문제(2026-07-29 사용자 보고)** — 원인: `show_overlay()`가 `show()`+`raise_()`만 했는데, `WA_ShowWithoutActivating`(비활성 표시)이라 패널처럼 이미 `SetWindowPos(HWND_TOPMOST)`로 떠 있는 창이 있으면 Qt의 `raise_()`가 그 TOPMOST 창 순서까지는 못 뒤집는다 — 클릭이 오버레이 대신 패널로 간다(`WindowFromPoint`로 실측 확인: 클릭 좌표의 실제 소유 창이 패널이었음). **수정**: `_CaptureScreen._claim_topmost()`가 panel.py의 `_set_always_on_top`과 동일한 명시적 `SetWindowPos(HWND_TOPMOST, SWP_NOACTIVATE)`를 오버레이 자신에게도 호출 — `show_overlay()`에서 1회, `CaptureOverlay.start()`가 60ms 뒤 `_reclaim_topmost()`로 한 번 더(실측: 두 TOPMOST 창이 거의 동시에 자기 자리를 주장하면 5회 중 1회꼴로 순서가 뒤집히는 레이스가 있어 단발 호출은 불충분했고, 재확인을 더하니 8/8 성공). GIF 녹화(`select_only`)도 같은 `_CaptureScreen`을 쓰므로 함께 해당. ✓ 실제 PasteFlow 앱에서 확인 완료(2026-07-29, 이 PC) — 패널 열림·설정창 열림 두 경우 모두 Alt+F2 캡처 정상. 단, 최초 증상은 **설정창 열림 상태**에서 보고됐고(다른 PC) 이 커밋의 진단·수정 대상은 **패널** TOPMOST 경합이었다 — 설정창은 패널(TOPMOST)의 owned window라 같은 z-order 밴드에 걸렸을 가능성으로 추정할 뿐, 그 인과관계 자체는 미검증. 다른 PC에서의 원래 재현은 못 함(일시적/환경적 문제였을 가능성 배제 못 함).
  - **드래그 부드러움** — 어두운 마스크를 입힌 딤 스크린샷(`_dimmed`)을 `prepare()`에서 1회 사전합성하고, 하이라이트 변경 시 `set_highlight_global`이 이전∪현재 영역(+테두리 여유 `_INVAL_MARGIN`)만 `update(dirty)`로 부분 repaint → 프레임당 전체화면 알파합성 제거. `_POLL_MS`=16(~60fps)과 결합해 Snipaste급 드래그.
  - **미구현(선택)**: HWP처럼 MSAA 빈 창에 UIA 트리 하강 폴백(단 UIA는 크롬도 거칠어 효과 불확실), 휠/방향키로 부모 요소 확장. **엣지**: 배율 다른 두 모니터에 걸친 자유드래그는 합성으로 정상. (드래그 시작점은 v1.34.0부터 press 이벤트 좌표로 확정돼 tick 지연과 무관.) 상세 이력은 plan 파일 `tidy-doodling-taco.md`.

### 순차 붙여넣기 핵심 동작 (가장 중요)

```
사용자 복사 → WM_CLIPBOARDUPDATE → ClipboardMonitor
  → database.save(item)
  → paste_queue.add_item(item)
       리셋 조건: (pointer>0) OR (직전 복사로부터 idle_reset_sec 경과 = idle 만료)
       그 외에는 누적. 매번 _last_copy_time = now, 포인터 0
  → panel이 열려 있으면 갱신 · 복사 토스트 표시(notify_on_copy 시) · 진행 HUD 정리

사용자 Ctrl+Shift+V (키다운) → PasteInterceptor._on_ctrl_shift_v()
  → paste_queue.get_next()
  → 큐 소진이면 → 아무것도 안 함 (suppress만, OS 기본 동작 없음)
  → 항목 있으면 → (필요 시 DB에서 전체 데이터 로드) → win32clipboard로 클립보드 교체
                → Ctrl+V SendInput 주입 → OS 기본 Ctrl+V가 교체된 내용 붙여넣기
  → _on_paste_from_hook() → paste_happened 시그널 emit
                           → pointer>=total이면 paste_queue_done 시그널 emit

사용자 일반 Ctrl+V (키다운, 물리 키) → 훅 통과(suppress 없음)
  → LLKHF_INJECTED 검사: 주입 키면 무시(자체 Ctrl+Shift+V·direct_paste 분 제외)
  → on_plain_paste() 콜백 → _bridge.plain_paste.emit (훅 스레드 → 메인 스레드)
  → _on_plain_paste(): _clear_queue_ui() (큐 clear + tray/panel 큐 표시 초기화)
  → OS 기본 Ctrl+V 동작 (앱이 paste 처리)

paste_happened   → _update_paste_ui() → PasteHud.show_progress()로 진행 HUD 표시·갱신
paste_queue_done → _on_paste_queue_done(): _clear_queue_ui() + PasteHud.finish() → 1.2초 후 HUD fade-out
```

### 설계 규칙

- **색상 테마**: 전체 UI에 중립 차콜 다크 테마 적용(`theme.py` — 배경 `BASE #121212` near-black, `MANTLE`/`CRUST`는 더 어둡게). **강조색은 코랄(`PEACH`) 단일 액센트**(v1.18.0~) — "무채색=수동·기본, 코랄=활성·선택·주목"의 2톤 체계. 패널 항목 테두리는 큐밖/완료=무채색(`SURFACE2`), 큐 안=코랄(`PanelItemWidget`의 `in_queue` 파라미터로 생성 시점부터 구분). 옛 민트(teal)는 패널·설정창·AI질문창·OCR/캡처 오버레이·트레이에서 전부 코랄로 흡수하고 `theme.py`에서 `TEAL`/`TEAL_HOVER`/`COLORS['teal']`를 제거(버튼 hover용 `PEACH_HOVER` 추가). 단, 텍스트 미리보기 마크다운 요소색(제목 파랑/코드 파랑/볼드 코랄/기울임 초록)은 답변 가독성용 별도 체계라 액센트와 무관(건드리지 않음). **설정창은 예외**: 폼 가독성·정돈을 위해 전역 테마와 분리한 전용 팔레트를 쓴다(`settings_dialog.py` 상단 `_PAGE`/`_CARD`/`_INSET`/`_LINE`/`_BTN`/`_TITLE` — 어두운 페이지 위 한 톤 밝은 카드, 입력칸은 카드에 박힌 inset, 제목은 카드 안쪽 배치로 테두리 검정 얼룩 제거, 강조색 coral로 통일). `COLORS['base']`/`['mantle']`로 되돌리지 말 것.
- **프레임리스 창**: 투명도 지원, Panel에 드래그 이동 구현.
- **Windows 전용**: 클립보드 접근에 `pywin32`와 `WM_CLIPBOARDUPDATE` 사용.
- **설정값**은 SQLite `settings` 테이블(키/값 형태)에 저장.
- **단일 인스턴스**: `main()`에서 Windows 뮤텍스(`PasteFlow_SingleInstance`)로 보장. 핸들은 `app._single_instance_mutex`에 저장(GC 방지). 두 번째 실행 시 Named Pipe(`\\.\pipe\PasteFlow_IPC`)로 첫 번째 인스턴스에 패널 토글 신호 전달 후 즉시 종료.
- **자동 닫기(Auto Close)**: 기본값 OFF(항상 위에 ON). `Qt.WindowType.WindowStaysOnTopHint`를 재설정하지 않고 ctypes `SetWindowPos(HWND_TOPMOST/NOTOPMOST)`로 TOPMOST 플래그만 조작하여 깜빡임 방지. `_auto_close`가 `False`이면 `changeEvent` 자동 닫기 조건에서 제외됨.
- **PanelItemWidget 표시 규칙**:
  - 각 항목은 최대 5줄까지 표시한다. 6줄 이상 word-wrap되는 경우 상단 5줄을 보이고 나머지는 하단 클립.
  - 높이 공식: `label_h = visual_lines * fm.lineSpacing() + 8`, `widget_h = label_h + 12`. `fm.lineSpacing()`을 사용해야 하며(`fm.height()` 사용 시 줄 간격 오차 발생), **레이블(`_text_label`)과 위젯 양쪽에 `setFixedHeight`를 모두 설정**해야 클리핑이 없다. 위젯에만 설정하면 Qt 레이아웃이 레이블 높이를 독립적으로 결정해 텍스트가 잘린다.

---

## TDD 적용 범위

### TDD 적용 모듈 (테스트 필수)

| 모듈 | 이유 |
|------|------|
| `models.py` | 순수 데이터 구조, 외부 의존 없음 |
| `database.py` | CRUD 로직, 인메모리 SQLite로 격리 테스트 가능 |
| `paste_queue.py` | 큐 포인터 상태 관리 순수 로직, UI/OS 의존 없음 |

### 수동 확인 적합 모듈

| 모듈 | 이유 |
|------|------|
| `clipboard_monitor.py` | WM_CLIPBOARDUPDATE Windows 이벤트 의존 |
| `paste_interceptor.py` | Ctrl+Shift+V 키 감지 + 클립보드 교체, 실제 환경 필요 |
| `hotkey_manager.py` | 글로벌 단축키 OS 레벨 등록 |
| `ui/*` | GUI 렌더링, 수동 시각 확인 필요 |
| `main.py` | 통합 오케스트레이션 |

---

## 작업 규칙

### 기본 원칙

1. **한 번에 하나의 기능만 구현**한다.
2. 구현 전 반드시 **계획을 설명하고 승인을 받은 후** 진행한다.
3. 기능 완료 후 **진행 상태를 즉시 보고**한다.

### TDD 대상 모듈 작업 순서

```
1. Red   — 실패하는 테스트 먼저 작성
2. Green — 테스트가 통과하는 최소 구현
3. Refactor — 코드 정리 (테스트는 계속 통과 유지)
```

### 수동 확인 대상 모듈 작업 순서

```
1. 구현 계획 설명 → 승인
2. 구현
3. 실행 후 수동 동작 확인 항목 명시
```

---

## ⚠️ 이전 버전 실패 원인 & 반드시 지켜야 할 사항

### 절대 하지 말아야 할 것

1. **Ctrl+V 키 이벤트를 차단(block/suppress)하지 않는다** — 이전 버전에서 순차 붙여넣기가 전혀 동작하지 않은 핵심 원인. keyboard 라이브러리의 `suppress=True`나 `block_key()` 등을 사용하면 안 됨.
2. **키 이벤트를 먹는(consume) 방식으로 구현하지 않는다** — 키를 가로채고 대신 붙여넣기를 실행하는 방식은 타이밍 문제를 일으킴.
3. **패널 드래그에 `QDrag`(OLE D&D)를 사용하지 않는다** — `Qt.WindowType.Tool | WindowStaysOnTopHint` 창에서 Windows OLE 등록이 불완전해 모든 드롭 대상에 금지커서가 표시됨.
4. **드래그 붙여넣기에 백그라운드 스레드에서 `SetForegroundWindow` + `SendInput(Ctrl+V)` 조합을 사용하지 않는다** — 백그라운드 스레드에서 Windows 포그라운드 잠금에 막혀 실패함. **예외**: Qt 메인 스레드에서 `AttachThreadInput`으로 포그라운드 잠금을 우회하는 경우, Electron/Chromium 앱 전용 fallback으로 허용한다.
5. **OCR 결과를 클립보드에 넣을 때 `_self_triggered` 플래그 설정을 누락하지 않는다** — `interceptor._set_clipboard(item)` 호출이 내부적으로 `monitor.mark_self_write(item)`를 처리하므로 반드시 이 경로를 사용할 것. 직접 `win32clipboard`를 쓰면 클립보드 모니터가 재감지해 동일 항목이 큐에 중복 추가됨. `mark_self_write`는 **시간창(0.5초 `_ignore_until`)과 해시 백스톱(`_last_hash`)을 함께** 건다 — 직접 저장 경로(`_persist_clipboard_item`)는 `_last_hash`를 갱신하지 않아, 늦게 도착한 `WM_CLIPBOARDUPDATE`가 0.5초 창을 넘기면 해시 방어가 무력이라 히스토리에 이중 저장됐다(Alt+F2 캡처 등에서 간헐 재현). `mark_self_write`가 쓴 항목의 해시를 미리 등록해 늦은 이벤트도 걸러낸다.
6. 요청하지 않은 기능 임의 추가 또는 수정.
7. 여러 기능 동시 구현.
8. TDD 대상 모듈에서 테스트 없이 구현.
9. 다른 모듈에 영향을 줄 수 있는 변경을 사전 보고 없이 진행.

### 반드시 지켜야 할 것

1. **클립보드 교체 방식만 사용** — Ctrl+Shift+V 키다운 시점에 `win32clipboard`로 클립보드 내용을 교체하고, Ctrl+V를 SendInput으로 주입한다.
2. **`_self_triggered` 플래그** — PasteFlow가 클립보드에 쓸 때 반드시 이 플래그를 설정하여 자체 모니터가 재감지하지 않도록 한다.
3. **모든 클립보드 형식 보존** — 텍스트만이 아니라 HTML, RTF, 이미지 등 원본 형식을 그대로 클립보드에 복원해야 노션 등에서 서식이 유지된다.
4. **패널 드래그 → 외부 앱 붙여넣기 방식 (앱 종류에 따라 분기)**
   - **이미지 항목 + Explorer(`CabinetWClass`) / 바탕화면(`Progman`, `WorkerW`)**: `_save_image_to_folder()`로 PNG 파일 저장. 서브폴더 아이콘 위 드롭 시 해당 폴더에 저장(크로스 프로세스 `LVM_HITTEST`). 저장 성공 시 클립보드 경로 생략. 바탕화면은 저장 직후 `_position_dropped_desktop_icon()`이 `LVM_SETITEMPOSITION32`로 저장된 파일 아이콘을 드롭 좌표로 재배치(가짜 드래그라 Explorer가 실제 드롭 좌표를 몰라 기본 배치를 쓰던 문제 보완 — "아이콘 자동 정렬" 켜짐 시 무효, 자세한 내용은 `main.py` 항목 참고).
   - **Win32/WinUI3 앱** (메모장 등): `SendMessage(hwnd, WM_PASTE, 0, 0)`. 흐름: fake drag(DragCopyCursor) → 마우스 업 시 `_set_clipboard` → 재귀적 `ChildWindowFromPoint`로 최하위 자식 컨트롤 탐색 → `SendMessage(WM_PASTE)`.
   - **Electron/Chromium 앱** (노션, Slack 등): `AttachThreadInput` + `SetForegroundWindow` + `SendInput(Ctrl+V)`. 창 클래스명(`Chrome_*`, `CEF*` 등)으로 판별. 금지 항목 4의 예외에 해당.
5. **`_SPECIAL_KEY_MAP`은 `hotkey_manager.py`에 단일 정의** — `paste_interceptor.py`에서 import해 재사용. 중복 정의 금지.
6. **시크릿은 DPAPI로만 저장** — DB에 평문 API 키·토큰을 직접 넣지 않는다. 새 시크릿 키를 추가하면 `main.py`의 `_SECRET_KEYS` 화이트리스트에 등록하고, 쓰기는 `crypto.protect()`, 읽기는 `self._get_secret()` 헬퍼를 사용한다. DPAPI blob은 현재 Windows 계정에만 묶여 타 PC 복호화 불가 — 이로 인해 다중 PC 자동 동기화는 폐기됐다(설정은 PC별 독립).
